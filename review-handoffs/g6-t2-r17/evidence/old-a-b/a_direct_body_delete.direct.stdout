  ✓ conforming trace: all six behavior dimensions MATCH
  ✓ R-G6-01: a BLOCKED handoff on a COMPLETED chain DIVERGEs (never MATCH)
  ✓ dim 9: a missing handoff triple refuses to judge (never a silent MATCH)
  ✓ dim 9: the expected artifact ref missing → DIVERGE
  ✓ dim 9: a reason drift → DIVERGE
  ✓ dim 2: a round missing its scan binding entry → DIVERGE (counts would have passed)
  ✓ dim 2: a misattempted binding entry → DIVERGE
  ✓ dim 7: forward progress after a FAIL verdict → DIVERGE (admission violation)
  ✓ dim 7: the reflow jumping to the wrong node → DIVERGE
  ✓ dim 9: ABSENT expectation with a fabricated reason → DIVERGE
  ✓ dim 9: ABSENT expectation with a fabricated ref → DIVERGE
  ✓ dim 9: the over-limit ABSENT triple (all null) MATCHes
  ✓ real run: the entry's handoff is BLOCKED/'code review closure review not done' while the chain COMPLETED (R-G6-01 observed)
  ✓ real run: the comparator DIVERGEs on that handoff (no MATCH on BLOCKED)
  ✓ real run: the WP-1 record is journal-verified in the trace (FEEDBACK_DRIVEN_CHANGE/CLASSIFIED/gen 1→2, trigger solution-gate@1)
  ✓ dim 7: the declared + journal-verified WP-1 generation restart to requirement-intake MATCHes (F post-gate)
  ✓ dim 7: the same restart WITHOUT the declared WP-1 trigger DIVERGEs
  ✓ R3-H1: the real trajectory with the WP-1 record evidence stripped → DIVERGE
  ✓ R3-H1: a wrong-record-type WP-1 entry → DIVERGE
  ✓ R3-H1: a non-CLASSIFIED WP-1 record → DIVERGE
  ✓ R3-H1: a wrong-generation WP-1 record (gen 2→3 against a run at gen 2) → DIVERGE
  ✓ R3-H1: a WP-1 record with no generation binding → DIVERGE
  ✓ R3-H1: a WP-1 record attributed to the wrong trigger round → DIVERGE
  ✓ R3-H1: a WP-1 record attributed to the wrong trigger capability → DIVERGE
  ✓ real run: the generation-2 intake is observed at attempt 2
  ✓ R3-H1: the new-generation intake re-dispatched at attempt 1 → DIVERGE
  ✓ R4-H2: a declared restart whose next dispatch is FORWARD (no WP-1 record) → DIVERGE (no forward mask)
  ✓ R4-H2: a declared restart jumping to a non-intake node → DIVERGE
  ✓ R4-H1: the legal dual-generation wave (records gen 1 and 2, run at gen 3) MATCHes
  ✓ R4-H1: the legal dual-generation trace passes all six behavior dimensions
  ✓ R4-H1: the first wave's record carrying the wrong generation → DIVERGE
  ✓ R4-H1: the last wave's record carrying the wrong generation → DIVERGE
  ✓ R4-H1: the run's final generation drifting past the last declared wave → DIVERGE
  ✓ R4-H1: the second declared wave with no journal record → DIVERGE
  ✓ R5-H1: the legal gate→review wave (records gen 1+2, run at gen 3) MATCHes
  ✓ R5-H1: the legal gate→review trace passes all six behavior dimensions
  ✓ R5-H1: the gate→review chain with the REVIEW (last) wave's record stripped → DIVERGE
  ✓ R5-H1: the gate→review chain with the review wave's attribution blanked → DIVERGE
  ✓ R5-H1: the gate→review chain with the final generation drifted 3→4 → DIVERGE
  ✓ R5-H1: the legal review→gate wave (records gen 1+2, run at gen 3) MATCHes
  ✓ R5-H1: the review→gate chain with the REVIEW (first) wave's record stripped → DIVERGE
  ✓ S-CRASH: a re-entry against a tampered published manifest fails closed (MANIFEST_CORRUPT_STOP) — the crash-resume path never silently continues
  ✓ R8-H4: the self-digest-tampered library is refused (MANIFEST_CORRUPT_STOP) and never rewritten — the refused bytes survive the driver
  ✓ R8-H4: the self-digest refusal reports no vacuous crash facts (lostWrite/redrive stay false)
  ✓ R8-H4: the entry digest-tampered library is refused (MANIFEST_CORRUPT_STOP) and never rewritten — the refused bytes survive the driver
  ✓ R8-H4: the entry digest refusal reports no vacuous crash facts (lostWrite/redrive stay false)
  ✓ R8-H4: the sealed cursor ahead of the journal-tampered library is refused (JOURNAL_MANIFEST_MISMATCH_STOP) and never rewritten — the refused bytes survive the driver
  ✓ R8-H4: the sealed cursor ahead of the journal refusal reports no vacuous crash facts (lostWrite/redrive stay false)
  ✓ R9-H2: a refusal after the lost-write rollback is terminal — the double-resume branch does not run and the refused bytes survive the driver
  ✓ R12-H1: the exact frozen register passes the ledger guard
  ✓ R12-H1: a register that LOST an entry fails with the missing-ID diagnostic
  ✓ R12-H1: a register with a DUPLICATED entry fails with the duplicate diagnostic
  ✓ R12-H1: the sealed ledger loads with the frozen sizes (50/4/6 scenarios, 54/38 negative assertions)

==== g6 behavior negative summary: behavior negative: 53 passed, 0 failed (frozen coverage: 54 assertions across 10 groups) ====
(R1-H1: the real handoff triple is the verdict basis; a BLOCKED handoff never prints MATCH)
