G6-T2-REVIEW-R17 复审任务 —— R16-H6/H7/H5 修复闭环 + 旧形态回返 + 事件流元层次（全量、只读、根因合并式评审）
0. 判定纪律（先读，约束全文）
你是本轮唯一判定者。整改方修订说明与自验矩阵只证明其声称属实，不进入判定链；R16-H6/H7/H5 是否闭环、仪器面是否可信，一律以你对三项阻塞修复的独立复核、真实形态对照与推演复跑为准。
前十六轮教训递进适用：R13-H1——自述不是证据；R14-H1——改核执行痕迹（退出码/行数/ID）；R15-H5——印行仍是输出文本，改核求值点事件；R16-H6/H7——事件核验自身还有两漏：成功/失败路径的流捕获不对称、印行与事件只核数量不对应，且解析器对损坏行「静默丢弃」本身就是一种自述信任。本轮验收同时要求：R16-H6/H7/H5 逐项闭环、R15 六指定形态与 R14 三形态回返不回归、R1–R15 全部已 CLOSED 项不回归、以及你对仪器任意位置构造新例的自由裁量（不限于已列形态）。
最终结论以一行总判定开头：PASS（R16-H6/H7/H5 闭环、仪器面可信、可进入合并授权裁决）或 FAIL（列阻塞项）。同一问题合并编号（R17-H#…）；R1–R16 已判定事项不得重复上报或回退；§6 清单不得升格为问题——除非指出具体是哪条验收要求使其成为范围内问题。
措辞纪律：本任务属仪器审计语义——请使用中性词（异常 / 识别 / 受控修改 / 错误通过 / 构造不一致），勿用安全攻防类词汇。
1. 范围与基线（先核对，再审查）
开工前逐项核对并报告：复审副本 HEAD 精确为 7ec2dc6（分支 feat/g6-t2-m2-scenarios，已推送 origin）、副本干净、npx tsc --noEmit 退出 0、Node v24.x。任何一项不符，先报告差异再决定是否继续。

复审工作区：自建独立副本（git worktree add /tmp/g6-t2-r17-review 7ec2dc6，node_modules 可 symlink），全程只读：不改 tracked 代码、不提交/推送、推演脚本只放副本内未 tracked 临时目录。主工作区归整改会话，不得触碰。
主审对象（数据基线 = 代码提交 2ec9ecb，其后 7ec2dc6 仅 docs）：tests/g6-negative-coverage-audit.test.ts（spawnSync 双路 + 严格解析 + 逐条对应）、tests/g6-parity/event-stream.ts、ledger-guard.ts、两矩阵（场景事件带 msg）、tests/g6-parity-instrument-presence.test.ts、四入口、报告 §6.2/§6.4/§9、PR #197 正文、交接 R16 节。产品运行时代码必须零 diff（核对 git diff b8923fc..7ec2dc6 对 core/、execution/、runtime.ts 零触及；diff 应仅涉 tests/ + docs/），发现即为越界阻塞。
评审边界：R16 三项阻塞的闭环复核——R16-H6（成功路径 stdout-only）、R16-H7（计数-only + 静默丢弃损坏行）、R16-H5（基线引用与边界措辞）；同时核验 R1–R15 已 CLOSED 项不回归（含 R15 六指定形态与 R14 三形态）。
CI 现状（已知事实，不必作为新发现上报）：ci-tests 在该分支持续失败 = ① tests/g6-parity-behavior-matrix.test.ts 按 A′ 设计红（退出码 1，R-G6-01 生产修复前必然）；② tests/hermes-cli-executor-contract.test.ts CI 并行满载失败（整改方本地隔离 21/0 恢复）。typecheck / standards / loop-patch-mutations 均绿。
2. 方法要求（先建两张清单，再统一输出发现）
事件核验机制清单：逐项独立复算——① spawnSync 对成功/失败均取 status+stdout+stderr（构造「退出 0 且仅 stderr 有红行」入口验证不可见性已消除）；② 解析器对非 JSON/非事件对象/缺失流文件的行为（行号报出、审计失败）；③ 印行与事件的逐条对应算法（多重集全等；同数不同 ID/同数不同文本均须失败）；④ guard handle 封闭与 pin 事件内置不回归。
文档-实测一致矩阵：逐条核对报告 §6.2 归属表（R15-H5 行边界措辞、R16-H6/H7/H5 三行）、§6.4 R16 行、§9 十六轮、页首数据基线 2ec9ecb/64（PR head 7ec2dc6 = 65）、§2 审计器 11/0、文件数 173，与 PR #197 正文、交接 R16 节、以及你的实测结果是否逐字吻合；特别核对 PR/报告中的每一个 commit 引用都是冻结 HEAD 的祖先。
3. 一、三项阻塞修复闭环验证（逐项 CLOSED / NOT_CLOSED + 独立证据）
验证工程规范：负例与推演施加于独立副本；每次施加后以「退出码 + 定位 + 痕迹」三要素判定；逐项还原后字节比对确认。以下形态你须独立构造并运行（整改方的 worktree 痕迹不构成你的证据）：

R17-H1 退出 0 + 仅 stderr 红行（R16-H6 同类）
入口保持全部 54 条真实（绿、事件 backing）断言，settle 之后经 console.error 补打一条红行，入口仍退出 0。期望：审计器拒绝（两路均捕获 → 红行 1 对事件红 0 → MISMATCH），退出 1 且显示红行 1。整改方实测：10 passed 1 failed，退出 1。

R17-H2 同数不同 ID / 同数不同文本（R16-H7-a 同类）
产物入口保持 50 条绿行但改写其中一条的 ID/文本（事件仍记录原始值）。期望：审计器拒绝（逐条对应 MISMATCH），退出 1。整改方实测：10 passed 1 failed，退出 1。

R17-H3 合法 settle 后追加损坏事件行（R16-H7-b 同类）
入口保持全部真实断言与 settle，向事件流追加一条损坏 NDJSON（非 JSON 或非事件对象）。期望：审计器拒绝（严格解析按行号报出），退出 1。整改方实测：10 passed 1 found… 实为 10 passed 1 failed，退出 1。

R17-H4 旧形态回返验证（R15 六指定 + R14 三形态）
a) 直调 body+删断言；b) settle 打桩；c) 删场景+注销钉；d) 53 实跑+1 伪造绿行；e) 49 场景+1 伪造 ID 行；f) settle 后真实红断言。期望：全部审计退出 1。整改方实测：d/e/f 各 10/1、a 10/1、b 5/6（本轮复跑）。

R17-H5 表述对账（R16-H5）
重新读取实时 PR #197 正文与报告 §6.2/§6.4/§9：① 所有 commit 引用（2ec9ecb/64、head 7ec2dc6/65）均为冻结 HEAD 祖先且与实测一致；② 事件流边界措辞是否已收窄为「单仓内补写个别一致记录仍可能通过（已声明的同仓同步修改边界）」而非「须伪造整个流」；③ git diff --check b8923fc..HEAD 是否 clean。

4. 二、回归排查（不限于修复点）
docs-only 边界：git diff b8923fc..7ec2dc6 对 core/、execution/、runtime.ts 零触及，改动面 = tests/ + docs/；git diff --check clean；
R12-H1 封签账本三层不回归：单改账本数据、双改数据+重封、loader 桩+改账本、缩组表四类仍硬失败；
负向矩阵冻结覆盖不回归：两套各删一条 → 事件数 53/54、37/38 且 settle 失败，退出 1；
A′ 四要素不回归：产物 50/0、合并 0/50 按设计、桶 50/50 零新因、非 dim-9=0、超限 4/0、崩溃 6/6；
存在性 pin：删除审计器/账本/守卫/事件流/任一入口 → 全量回归显式失败；
全量回归复跑：并行 173 文件，期望逐文件断言全绿、文件级失败 = 按设计红矩阵（+ 已知并行抖动 codex-adapter / checkpoint-store 时隔离复跑恢复——codex 0 失败、checkpoint 268/0）；tsc 0。
5. 三、事件流与审计器自身元层次推演（本轮最重要裁量区）
事件流由被审代码在求值点发出、由审计器指定路径并核验——请独立压测（不限于下表）：

推演门	方法	期望
事件流停用	子进程不带 G6_EVENT_STREAM（或入口内令 emitEvent 空转）	审计器失败（事件 0 / settle 缺失 / 流缺失报出）
事件流被补写	被审入口直接向流文件补写与真实执行不符的事件（含与印行互洽）	陈述单仓共谋边界：补写个别一致记录可通过是已声明边界（R16 实测确认）；请指出具体哪条验收要求使范围内问题成立
审计器被改松	四入口事件判据改恒真 / 关停逐条对应	你的独立事件复算（§2 清单）与之矛盾——识别为审计器失效；属同仓同步修改边界
解析器被改松	恢复「静默丢弃损坏行」	R17-H3 形态应重新穿过——即该修改使仪器退化，应识别
handle 封闭被删	去掉 guard 的 after-settle 抛错	R17-H4-f 形态（settle 后真实红断言）应仍被退出码或双路红行捕获；若不能，指出缺口
流路径被利用	入口向审计器临时目录写额外文件 / 篡改他入口的流文件	说明审计器的隔离假设是否被违反及后果
每处不一致都是仪器缺陷；无法覆盖的语义明确标注人工核验边界。

6. 有意为之的实现选择与已知事实（不得作为缺陷上报）
A′ 现实声明（Current User 2026-09-23 R1 裁决）：R-G6-01 生产缺陷使行为层合并判定恒 0/50 按设计红、桶 50/50 surfaced；完成门第 1 项等生产修复，不判 PASS 不掩盖。修复需 decision record + 生产代码，已单独路由。
已知口径：产物层只判 3/9 维；超限 4 场景 runner 单列；maxDesignRounds 默认 2→3 与 journal 回流显式轮数上限为挂账项（Current User 2026-09-21 裁决，不在 G6 内改）；S-INIT 规格-实现边界发现（§3.13 如实单列）；BU×Re-Gate 剔除理由实证张力（§7 单列）。
已知环境项：ruby@3.3 PATH 前置（README 已文档化）；并行抖动类（codex-adapter / checkpoint-store / hermes-cli-executor 隔离复跑恢复，均不 import G6 harness）。
已声明的同仓同步修改边界：补写个别一致事件记录 / 审计器与账本同步改松——报告 §6.2 与 event-stream 文件头均已声明；不得作为新缺陷上报，除非指出具体验收要求被违反。
既有分工：Exchange transport-only、PKB 归档镜像、CP STATE 动态登记均不在本任务内。
7. 验证证据基线（整改方声称——仅供核对，不构成关闭证据）
HEAD 7ec2dc6 已推送 origin，工作区干净，b8923fc..7ec2dc6 65 commits（数据基线 = 代码提交 2ec9ecb = 64 commits 处）。整改方声称：tsc 0；覆盖审计器 11 passed / 0 failed 退出 0（自算封签 2 + 包装合成体探针 5 含 settle 后 handle 封闭 + 四入口执行事件核验 4）；存在性 pin 8/0；三新形态（退出 0 的 stderr 红行 / 同数不同 ID / settle 后损坏事件行）隔离 worktree 复刻全部 10/1 退出 1（专属诊断：MISMATCH / MISMATCH / corrupt stream lines 1）；旧形态回返：53+伪造绿行 10/1、settle 后红断言 10/1、settle 打桩 5/6（均退出 1）；行为负向 54/0、比较器负向 38/0；产物矩阵 50/0；行为矩阵 A′ 四要素 + 崩溃 6/6；T5 32/0、YAML 150/0；全量并行 173 文件，文件级失败 = 按设计红矩阵 + 两个已知并行抖动（隔离复跑 codex 0 失败 / checkpoint 268/0 恢复）。对以上逐项核对存在性与关键行；核对通过不减少你的独立执行义务。

8. 输出要求
总判定一行：PASS / FAIL；FAIL 时给出合并后的阻塞列表（每项四要素齐备：具体机制矛盾或表述失实、位置、修正边界、回归核验——缺一不成项）。
逐节判定表：§3 五项（R17-H1～H5）+ §2 两张清单 + §5 推演表（每门：拒绝/穿过 + 证据锚点）。
§4 回归清单逐项结论（含 diff 边界与 diff --check 实测输出）。
建议项单列（不阻塞），注明依据。
不属于本任务（明确排除）：R-G6-01 生产修复本身；合并与 Control Plane 登记；真实 Agent CLI / run8 / Exchange / PKB 发布；产品运行时代码修改；挂账两项的处置。
若 R16-H6/H7/H5 闭环、仪器面可信且无阻塞：明确给出 PASS 判定，并声明可进入合并授权裁决（合并授权本身仍待 Current User）。
