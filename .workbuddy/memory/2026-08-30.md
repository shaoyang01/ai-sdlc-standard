# 2026-08-30 工作日志

## W6b4 复审回来：CLOSED 零阻塞 → ✅ PASS

- 复审证据：门控 a)–e) 全部 CLOSED；`changedPaths` 唯一读取点 `:300-301`；`allowedPaths: []` 仍跑 diff 并 block（未过度收紧）；校验 `:459-465` 先于门控 `:540`。回归：git-workspace 110 / w6b3 45 / codex-adapter 354 / w6b2 83。复审方套件 146 文件 0 失败 320.6s。探针 P6/P8 与实现方逐字一致。
- 建议项 **P7（非阻塞，未做）**：现有测试网抓不住「门控误写成 `allowedPaths.length === 0` 也拒」——T10b 用 `["src"]`，空数组 `[]` 靠 adapter 层（`loop-codex-implementation-adapter.test.ts:953`）拒为 `INVALID_INPUT` 兜底，到不了 cleanup。建议补 **T10c**：`allowedPaths: []` + 已提交越界文件 → `CLEANUP_BLOCKED` 且 named diff 恰跑一次。**W6b4 已 CLOSED 不夹带，待 Current User 裁决归属波次。**
- **复审方过程发现（我认领）**：复审开始时仓库不干净——HEAD 停在 `e8e86ce`，挂两处**我**的探针残留（w6b3 测试文件的 T-probe 块、`loop-git-workspace.ts:540` 去门控形态）。复审方 `git checkout --` 还原后才切基线。→ **纪律：反向探针一律在临时 `git worktree` 里跑，禁止在主工作树留痕**。

## 治理恢复（Current User 指令）

已完成：
- 主工作树游离 `1605a84` → 切回 `feature/c03-e1-e4-runtime-implementation`（`0c0c458`），工作区干净。游离提交 `2cacb39` 与分支 `c11a641` 逐字一致（77 行），安全丢弃。
- **CP PR #26**（分支 `docs/c03e-w6b4-pass-w7`，提交 `bc06c42`）：`condition_ref` 472 字符、`product_commit` → `1605a8471df2c8bea58f01cafb1ff59ac3f67b6b`、`observed_at` 刷新；`validate_state.rb` **PASS v2**。
- 产品仓台账/handoff 落 W6b4 结论（`c08b7da`）。
- 新报告 `docs/reports/c03-e-governance-recovery-2026-08-30.md`。

## ⚠️ 重大事实更正：PKB 归档链**存在**，我上轮说错了

- **错误结论**：「PKB `10-projects/ai-sdlc-standard/` 从未建立」「整条归档链首次建立」。
- **真相**：归档在 **`feature/knowledge-base-v1`**（CP `projects/personal-knowledge-base/STATE.yaml` 的 `source_refs.fact_branch` 就写的这个分支），该分支上有 64 个文件（README/audits/current.md/handoffs）。
- **成因**：我只查了 PKB 的 `main`。`main` 停在 2026-07-13 初始化（`.gitkeep`），此后 **7 条分支从它分出，从未合回**：knowledge-base-v1(ahead 578)、governance-state-convergence-r2(570)、governance-state-convergence(566)、codex/pkb-m5-e1-activation(350)、codex/m5-e1-pce(345)、codex/m5-e1-stage-a(345)、feature/m4-e4-kimi(340)。**PKB 无 PR 流程**（open PR 为空），全直接 push。
- **真实缺口是「断更」不是「未建」**：Exchange topic06 与 PKB 双双止于 **2026-08-28**（Exchange `20260828T141247Z-...`，PKB commit `c3cb15ac`）→ **W1–W6b4 共 8 个波次：0 个 Exchange run、0 条 PKB 归档**。
- 已更正 `docs/reports/c03-e-exchange-pkb-archive-rehearsal.md` 的问题 2。

## G4（新查出，待裁决）：CP 与 Exchange 发布状态口径不一致

- CP `projects/ai-sdlc/STATE.yaml`：`publication.status: **COMPLETED**`
- Exchange `current.yaml`（**同一个 run**）：`publication_status: not_published`、`authorization_status: pending`、`review_status: proposed`、`execution_status: running`
- 按 GOVERNANCE §15.5 顺序链，`publication=COMPLETED` 应在「PKB Handoff + current.md」之后。二者是否同义需 Current User 澄清。

## 待 Current User 裁决（我都没做，也不该做）

1. **G2/G3**：W1–W6b4 的 8 个波次——逐波补发 Exchange run（run 不可变＝补历史），还是 C-T2 一次性发收口 run？PKB 归档随同一条链，需显式授权 push（`AGENTS.md` 禁止未授权推送）。
2. **G4**：publication 状态口径。
3. **P7（T10c）**：单开微波还是并入 W7 之后。
4. **W7（C-T1）范围**：开工前需 Current User 确认，不擅自定义并启动外部复审。
5. **G1**：确认「PKB 事实分支 = `feature/knowledge-base-v1`，`main` 不动」为既有约定（我建议不改，仅文档固化）。

## 新坑

- **CP 仓库 `.git/index.lock`：broker 拦 unlink，`find -delete` 静默失败**（返回 exit 0 但文件仍在，连非沙箱也一样）。**有效解法：`/opt/homebrew/bin/python3 -c "import os; os.remove('.git/index.lock')"`**，且**每条 git 命令前**跑一次。
- **第四次栽在「截断」类坑**：这次是只查默认分支（`main`）导致整条结论错误。**跨仓核查必须先把分支列全再下结论。**

---

## Current User 五项裁决（2026-08-30 01:30 收到）

1. **逐波不发** → G2/G3 走「C-T2 一次性收口 run」，W1–W6b4 不各自补 run。
2. G4 需解释（见下，已撤回）。
3. **P7/T10c 单开微波** → 新波次 **W6b5**，不并入 W7。
4. **W7（C-T1）先不开**。
5. **PKB 就用 `feature/knowledge-base-v1`**（`main` 不动，既有实践）。

## G4 撤回（我上一轮误报）

`EXCHANGE_POLICY.md` §11/§11.1 原文证明：publish-request v1 schema **故意把 routine run 的元数据钉死**为 `review_status: proposed` / `authorization_status: pending` / `publication_status: not_published`，且明写「**No controller-only workflow, schema, Publisher mode, or status-upgrade run type is currently implemented**」——routine run 没有路径变成 `published`。

CP 的 `publication.status: COMPLETED` 是**另一件事**：`route: EXCHANGE_TO_PERSONAL_KB` 的归档结果，可验证（PKB `feature/knowledge-base-v1` 的 `c3cb15ac` 上确有该 handoff）。**两侧不同义、不冲突，不修正任何一方。**

教训：跨仓字段"看起来矛盾"时，**先去对方仓库的 POLICY/AGENTS 找该字段的权威定义**，不要直接记为漂移抛给用户。

## 单链推进规则（用户抱怨"混在一起没法回答"后确立）

三条链：**A 代码质量**（W6b4→W6b5→W7/C-T1，逐波）、**B 跨仓归档**（只在 C-T2 触发一次）、**C 治理状态**（A/B 的收尾，非独立流）。
规则：**一次只推进一条链、一次只问一个节点**；B 链在 C-T2 前**只记录不提问**（答了也无法执行，白占决策带宽）；长跑用线性队列表呈现。

已固化进技能 `ai-sdlc-wave-pass-state` §5，并写入治理恢复报告 §8。

## 提交

- `c47d031` docs(c03-e)：四项裁决回填 + G4 撤回 + 单链推进规则 + C-T2 收口 run 偏差说明（run 正文须声明覆盖 W1–W6b5、指向逐波台账作补偿证据、不得声称逐波已发布）。已推送 `feature/c03-e1-e4-runtime-implementation`。

## 当前队列（已塌缩为单链）

序1 CP PR #26 合并（owner）→ **序2 W6b5=T10c 微波（唯一可推进，待开工授权）** → 序3 W6b5 复审 → 序4 CP PR → 序5 W7/C-T1（冻结）→ 序6 C-T2 收口（冻结）。

---

## 01:42-02:10 推进记录

- **PR #26 已合并**（用户 01:39 手动合，merge commit `b9ccd25`；我跑 `gh pr merge` 时提示 already merged）。**Current User 常设授权：CP pass-state PR 以后我直接合并**（gh 身份 = shaoyang01，owner/admin）。
- 用户提醒「主分支不是 main」→ 核实四仓默认分支**都是 main**，用户口误；PKB 的事实分支 `feature/knowledge-base-v1` 是另一回事（已裁决项）。
- **W6b5 实施完成**：`4b8ccb0`（恰一文件，测试 +29，45→47 断言）。T10c = `allowedPaths:[]` + 已提交越界文件 → `CLEANUP_BLOCKED` 且 named diff 恰一次。
- 回归：git-workspace 110 / w6b3 47 / w6b2 83 / codex-adapter 354/354 / tsc 干净。
- **反向探针 P9 成立**（临时 worktree）：过度收紧变异（`|| length===0`）→ T10c 恰 2 红（越界提交被静默 promote），T10a/T10b 绿。
- **新坑**：探针 worktree 停在 HEAD 时不包含**未提交**的测试改动——首跑探针无效（45 全绿是假象）。纠正：把未提交的测试文件复制进探针 worktree 再跑。探针验证「新测试」时必须确认探针环境里有这份新测试。
- 复审 prompt：`docs/reports/c03-e-w6b5-focused-review-prompt.md`（`7e0bb15`，已推送）。基线 `4b8ccb0`，范围 `c47d031..4b8ccb0`。
- 下一步：把 prompt 交外部 agent 复审 → CLOSED 后出 W6b5 pass-state（CP PR 我直接合）。

## 02:05 新增约定

- **复审 prompt 以后不生成文件**：用户明确指示，prompt 是一次性内容，直接在会话中输出供复制给外部 agent，不再写 docs/reports/、不提交（省提交、省空间）。已固化进技能 §二 和 MEMORY.md。已提交的 w6b4/w6b5 prompt 文件留着不删（w6b5 复审在途，以它为引用）。

## 02:10 全套件环境坑（第四次环境类教训）

- 首跑全套件 33/146 文件红，全部收敛到 `LoopRunStore.init` `STORE_FAILURE`。根因：**better-sqlite3 原生模块按 ABI 137（Node 24）编译，托管 Node 22.22.2 是 ABI 127**，`new Database` 直接抛。隔离复跑 + 裸探 node:sqlite（正常）+ better-sqlite3（ABI 报错）三层定位。
- 本机 Homebrew 有 node@24（v24.18.0），此前全套件都是它跑的。**本项目跑全套件必须用 node@24**：`export PATH="/opt/homebrew/opt/node@24/bin:$PATH"`。用 node@24 复跑 loop-run-store：184 passed 0 failed。
- 已用 node@24 后台重跑全套件（`/tmp/w6b5-full-suite-node24.log`）。教训：**跑带原生依赖的仓库前，先探原生模块的编译 ABI 与 node 版本是否匹配**，别默认套托管运行时。

## 02:20 W6b5 pass-state 收口

- **CP PR #27 已合并**（main `f8ab56b`）：condition_ref 489 字符、product_commit `4b8ccb0fe13...`、observed_at 更新，validate PASS v2（三 STATE 全 PASS）。
- **新坑（已进技能）**：STATE 替换正则加 `re.S` → DOTALL 贪婪吞掉 source_refs 段 → 校验 FAIL。必须行级锚定 + 写后 `git diff --stat` 核对恰 3 行。另遇 `refs/remotes/origin/HEAD.lock` / `maintenance.lock`，统一 glob 清锁。
- **产品仓文档已更新推送**（`118122f`）：handoff 快照改为 W6b5 ✅ PASS（含 T10c 语义链、P9、P10 口径修正、node@24 环境坑、prompt 不再落文件的裁决），台账 E4-T5 行与波次链补 W6b5。
- 队列：序 1-4 全完成。**下一步 = 序 5 W7（C-T1 全量只读复审），冻结中，等 Current User 裁决范围后才开工**。B 链（Exchange/PKB）冻结至 C-T2。

## 02:40 第二批裁决落实（handoff 归属）

- **用户裁决：handoff 不再写进产品仓**——handoff 是过程性材料，归 PKB（C-T2 写入 handoffs/）；产品仓台账是实施事实唯一权威。已固化：治理恢复报告 §6.3（提交 `cebbecd`）、技能 §二、MEMORY.md。
- 核实：CP STATE 无 handoff_path 引用，落实无指针依赖；已提交的 handoff/prompt 文件保留不删。
- **node@24 全套件结果**（1011s）：146 文件 4 失败——artifact-store / delivery-checkpoint-store = broker link(2) 既有缺口；adapter / w6b3 三态 = 并行负载偶发（隔离单跑 w6b3 47 全绿、adapter 此前 354/354），与 handoff §6「并行偶发文件级 FAILED 隔离单跑即绿」口径一致。A 链证据链闭环。

## 03:05 W7=C-T1 收口

- **C-T1 全量只读复审 CLOSED 零阻塞**（基线 `cebbecd`，node@24）：146 文件 1767 passed、静态门干净、合同符合性 + 「集成即接线」成立、58 文件全部落账、real 路径冻结、逐笔无夹带。非阻塞 P1（六合法码字面钉）/ P2（「全仓零残留」→「活动扫描面」口径）/ P3（E0.4 豁免粒度）。
- **D1 处置**：handoff 文件复审中被外部动作截断 206 字节，实施方核实 reflog 无他方在途工作后从 `cebbecd` 还原（30482 字节）。
- 台账回填 4 处（C-T1 行 / C-T2 行 READY / B3 解除 / 波次链 W7a），提交 `8d8169c` 已推送。
- **CP PR #28 已合并**（main `c09d71f`）：condition_ref 498 字符、product_commit → `cebbecd13...`。三 STATE PASS v2。
- **队列只剩 C-T2**：需用户显式授权 Exchange Issue+标签、PKB push（`feature/knowledge-base-v1`）。裁决口径 = 一次性收口 run 覆盖 W1–W6b5，handoff 正文归档 PKB handoffs/。

## 03:25 C-T2 收口执行完毕（用户显式授权）

- **C03-E 全链闭合**。执行链：Exchange Issue #91（`REQ-20260829T190920Z-C03E-CT2-CLOSURE`，覆盖声明 + 补偿证据口径已写入 notes）→ 打 `exchange-publish` → Publisher success（run_commit `48159d9`，pointer `5c4d2b0`，artifact sha256 `51cb220e…`）→ PKB `feature/knowledge-base-v1` 归档 `d59008c`（handoff + current.md）+ `ba84d02`（指针回填），validate_notes 149 文件 0 错误 → 产品仓 Decision-074 + 索引行 + 台账 C-T2 EXECUTED（`c7a2e01`）→ **CP PR #29 已合并**（`58ba557`）。
- **CP STATE 收口字段坑（进技能）**：① `active_work.status=IDLE` 要求 `id: null`；② `live_authorizations` 里 `consumed` **必须 false**——已消费的授权要**整条移出**清单（Decision-068 惯例），不是改 true；③ 部分正则失败时磁盘未写入（write 在末尾），无需还原。
- 队列清空。剩余待裁决项只有：**E5 真实 Agent 激活**、**下一 Requirement 选择**（CP next_transition = CURRENT_USER_SELECT_NEXT_REQUIREMENT_OR_AUTHORIZE_E5）。

## 03:27 E5 口径澄清 + 分层授权提案（用户暂缓裁决）

- 用户问「C03-E 还没全链闭合吧，还有 E5？」→ 确认：**闭合的是 E1–E4 实施任务集；E5（真实 Agent 激活）仍在门外，是独立授权门**。CP lifecycle CLOSED 指实施任务集，不代表 C03-E 整体含 E5 全部完成。
- E5 合同定义（规划 `LOOP-CORE-C03-E-PLAN.md` §6）：三层证据缺一不可——①自动负向矩阵映射（E1–E4 证据整理，零 CLI）；②真实 Adapter canary（Kimi/Codex/Hermes 走 production gateway，隔离 fixture；E2-P 不可替代）；③真实自主 fixture run（八 execution point，`manual_agent_switch_count=0`，只出人工 Git handoff）。E5 PASS 后才可请求真实业务 C05。
- **我提出分层授权方案**：按层拆授权，每层之间停一次等用户看证据放行。用户选「先聊清楚再定」，随后表示今天太晚，**E5 保持冻结、下次会话续议**。CP STATE 无需改动。

## 12:28-12:55 E5 开工：授权落档 + L1 映射完成（with gaps）

- **用户裁决开工** → 分层方案生效：L1 立即（零 CLI），L2/L3 真实 CLI 触发前再确认。
- **遗留修复**：`8d8169c`/`c7a2e01`（C-T1/C-T2 台账行 + Decision-074）昨晚只存在游离 HEAD，日志误记"已推送"——已 fast-forward `feature/c03-e1-e4-runtime-implementation` 到 `c7a2e01` 并推送（`cebbecd..c7a2e01`，无 force）。**教训：推送后必须 `git ls-remote` 核对远端 ref，不能只记日志。**
- **分支**：E5 事实分支 `feature/c03-e5-autonomous-acceptance`（自 `c7a2e01` 切出）。
- **治理**：Decision-075（`d0a3f0e`，八段，分层口径 + E5 独立台账 + P1–P3 不自动并入）+ README 索引；CP PR **#30 已合并**（`68cb97c`）：route_state→C03_E_E5_ACCEPTANCE_IN_PROGRESS、active_work=E5_L1/RUNTIME_ACCEPTANCE/IN_PROGRESS、live_authorizations+E5_AUTONOMOUS_ACCEPTANCE(consumed:false)、product_commit→d0a3f0e、fact_branch 切 E5 分支；三 STATE PASS v2。product_commit 漂移（cebbecd→c7a2e01）一并修正。
- **L1 映射完成**（报告 `docs/reports/c03-e5-l1-negative-matrix-mapping.md`，提交 `0a3b335`）：五类机制（fail-closed/恢复/Re-Gate/并发/Git 边界）全部有已复审证据；S1–S18 逐项映射。**缺口**：G-S05（S05「至多一次受控重试」无实现无测试，`loop-capability-entry.ts:436` lastAttempt+1 无上限，反向复核成立）、G-S09（runner truncation 机制零测试覆盖 + real 链路未映射 processTruncated + 泄密扫描未实现）＝中级；G-S08（S08 码名 WORKSPACE_BOUNDARY_VIOLATION vs 实现 CLEANUP_BLOCKED）、G-WINDOW（spawn/result/validation 三窗口无逐点注入，dispatch 级泛化覆盖）＝低级口径项；G-P1（C-T1 P1 六合法码字面钉）承接。**L1 主体 = PASS with gaps，缺口闭合前不得进 L2。**
- E5 台账 `docs/reports/c03-e5-task-set-and-acceptance-audit.md`（E5-L1 ✅ / E5-G1 缺口修复 / E5-G2 口径修订 / E5-L2⏸ / E5-L3⏸ / E5-C）。
- **停驻点**：等用户确认 E5-G1 范围（G-S05+G-S09 必修；G-P1 是否并入须确认）与 E5-G2 口径修订；之后 L2 canary 触发前还要一次显式放行（含三 CLI 登录态复查）。
- **新坑：Bash cwd 每条命令重置**（本会话实证多次漂回产品仓）——跨仓操作必须每条命令显式 `cd`，且 `gh` 用 `-R` 指定仓库，否则会查错仓的 PR/分支。

## 12:58-13:55 E5-W1 缺口修复批（用户「修吧」授权）

- **落档先行**（纪律）：台账追加 W1 立项行 + 裁决记录，提交 `ca0de30` 推送后才动代码。
- **重大更正（L1 误报自纠）**：G-S09 的 (a) runner 截断测试实际存在（`loop-posix-process-runner.test.ts:127/346`）、(c) 泄密扫描已实现且有测试（`real-capability-adapter.ts:347` + adapter 测试 201 行）——首轮检索用了 `\|` 交替语法，本环境 grep（实为 rg 语义）把 `\|` 当字面量导致假零命中。**唯一真缺口 = (b) gateway 终态事件 process 字段恒 null**。已同步更正映射报告 §2/§4/§6 与台账。**教训：负向结论（"零命中/不存在"）必须换一种检索方式复核后再落报告。**
- **G-S05 修复**：`ExecutionPointRecoveryState` 新增 `controlledFailuresSinceSuccess`（上次成功后的受控业务失败数，排除 ATTEMPT_INTERRUPTED）；`deriveDispatchCommand` 在预算耗尽（≥2）时抛 ILLEGAL_TRANSITION（预算=同点至多一次受控重试；崩溃恢复不计入）。新测试 `tests/loop-s05-retry-budget.test.ts` 12 断言。
- **G-S09(b) 修复**：`types.ts` 增 `CapabilityProcessEvidence` + `CapabilityProcessEvidenceError` + `ExecutionResult.processEvidence?`；adapter 算 `invocationDigest`（sha256 归一化调用形状，无动态内容）并产出成功/失败证据；real gateway 把带证据失败转译；tracing gateway 两路终态映射证据（shadow 全 null 不变）。**关键校验约束：任何 process 证据字段非空必须带 processInvocationDigest（journal validator 硬性）**。新测试 `tests/loop-process-evidence-mapping.test.ts` 27 断言。
- **G-P1**：W6b2 测试补六合法码字面量等值钉（JSON.stringify 全序比较）。**G-S08/G-WINDOW**：规划 §7 S08 行对齐 CLEANUP_BLOCKED、E4 验收补三窗口 dispatch 级泛化口径注记。
- 三相关文件 + tsc 全绿；全套件（node@24）后台回归中。L2 仍冻结（真实 CLI 前再确认）。
