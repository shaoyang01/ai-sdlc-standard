# 2026-08-29

- 应用户要求做了一次仓库全面梳理（代码 + 项目管理规则），产出会话内总结。关键结论：
  - 仓库双产品面：Standard Package（治理文档/skills/templates/validators，入口 manifest.yaml）+ Runtime（TypeScript 单轨链执行器，入口 runtime.ts）。
  - 治理核心：v2 单轨七节点链（requirement-intake → solution-design → solution-gate → task-planning → implementation → code-review → knowledge-sync → Manual Git Handoff），Decision-044/045 重基线。
  - 动态状态权威在远端 ai-project-control-plane STATE.yaml。**该仓为私有仓库**（同属私有的还有 project-governance-exchange、personal-knowledge-base）；匿名 WebFetch 会 404，必须用本机已登录的 `gh` CLI（`gh api repos/shaoyang01/ai-project-control-plane/contents/...`）读取。默认分支 main。本仓 docs/CURRENT_STATUS.md 只是阶段边界冻结快照。
  - 本地 clone 路径（后续直接读本地，不必走 gh）：
  - 产品仓 /Users/eric/meicai/projects/ai-sdlc-standard（主干 loop-runtime-v1，非 main；另有 d05/d07/d08 三个旧 worktree）
  - CP /Users/eric/meicai/projects/ai-project-control-plane；Exchange /Users/eric/knowledge/project-governance-exchange（本地落后，main 停在 62a209a，远端 6b558f0）；PKB /Users/eric/knowledge/personal-knowledge-base（已同步到 c3cb15a）
  - 跑测试必须先切 Node v24（better-sqlite3 ABI），否则 sqlite/store 测试全假性 STORE_FAILURE
- **W6a 外部独立复审 PASS（2026-08-29）**：零阻塞，可进 W6b。我做了独立复核：diff 构成与申报一致（生产 4 文件 +99/+71/+66/+55、台账 +6、12 测试文件纯补 null、新测试 313 行）；PROCESS_SIGNALS(:106-110)、exactFields(:147-156)、canonical 固定序(:488-500)、store append 门(:1927)、事务内链校验(:2156)、canonicalize 内校验(:447) 引用全部对得上；classifyCapabilityRecovery 仅 loop-recovery.ts:745 一处调用（单一权威成立）；**Node v24 下实测 `npx tsx tests/loop-w6a-process-evidence-recovery.test.ts` = 68 passed**。
- 5 条观察项（非阻塞）：I3 锚定只覆盖 5 个进程事实字段；旧 v7 库无迁移、重开即 STORE_CORRUPT；SQL 列无 CHECK 兜底；测试计数口径；rejectsAtBothLayers 不能区分三道写门。前三条属合同外加固，需先指明条款才可做。
- **口径更正（重要）**：`scripts/run-tests-parallel.mjs` 只按**文件 exit code** 判定成败，自身不输出总数；handoff/STATE 里的 "1767 passed" 是**最后一个测试文件的内部计数**，不是全套件断言总数。此前我引用的"143 文件/1767 断言"应读作"143 文件全部 exit=0"。
- **W6a pass-state 已登记**：CP 分支 `docs/c03e-w6a-pass-w6b`（提交 9680a3d，仅改 STATE.yaml 三行：condition_ref / product_commit→d70b23c / observed_at），已 push 并开 **PR #22**。注意 CP 有校验器 `ruby tools/validate_state.rb <file>`，**condition_ref 上限 500 字符**（超限直接 FAIL），提交前必跑。
- **W6b 三条裁决（Current User，2026-08-29）**：① 拆成 W6b1=E4-T3 / W6b2=E4-T4 / W6b3=E4-T5 三个子波**各自独立复审**；② E4-T4 **新增 artifact kind**（改 loop-artifact-store.ts 联合类型 + KINDS 数组两处）；③ E4-T5 的「未知副作用→block」**本轮就接 wip digest 越界检测**，不留 C-T1。
- **CP PR #22 已合并**：main = `16cc5e6`，STATE 正式进入 W6a PASS / 下一步 W6b，`ruby tools/validate_state.rb` PASS v2。
- 台账改动已单独提交：产品仓 `d4ee31e`（docs-only，父提交 d70b23c），已 push。
- **W6b1（E4-T3）已实现并交付复审**（提交 `5f2bcd8` + prompt 提交 `ab5f78c`，已 push；复审范围 `d4ee31e..5f2bcd8`）：
  - 取证：`runProduction`(runtime.ts:961) 委托 `run()`(:1033)，`run()` 整体包在 `withResumeLease`(:394)，窗口**结构性已覆盖**；缺的是强制点（`claimNextCapabilityExecution` 只保证 claim 原子性，不保证 lease）。
  - 变更：生产 3 文件 +82/-8 —— `loop-resume-lock.ts` 新增 `isResumeLeaseHeld(journalPath)`（身份按 leasePathFor 重算，ENOENT→false、其余抛出）；`loop-capability-entry.ts` 新增可选 `requireResumeLeaseJournal`，在 recoverRunContext/bootstrap/claim **之前** fail-closed `STORE_BUSY`，构造期校验空串/空白串/非字符串；`runtime.ts` 把 entry 构造上移到 resumeJournalPath 之后并装配该选项。
  - 守卫**可选而非无条件**是有意权衡（避免 8 个直接构造 entry 的既有用例被迫改写入 lease），已作为裁决点写进复审 prompt；注意 P2 探针（删 runtime 装配行）无法自动转红，需人工/diff 确认。
  - 测试 `tests/loop-w6b1-resume-lease-window.test.ts` 22 断言；自测反向探针（守卫置 false）→ T1 AssertionError 转红，已还原。
  - 验证：新测试 22 passed；全套件 144 文件 failed_file_count=0 exit=0；tsc 干净；3 个 ruby validator exit=0。
- **W6b1 独立复审 PASS（零阻塞）**。复审实测更正：`runtime.ts` 是 **+11/-8**（我提交信息与 handoff 早期版本写的 +19/-8 是错的，把 19 行总改动当成新增行）。复审取证确认窗口结构性已覆盖、无旁路入口；**P2 实证删掉装配行后全套件仍全绿**（含 run-production、c03d wiring）——没有任何测试能捕获"生产路径静默失去防火墙"。
- **S1/S2 已补**（用户裁决"现在补"）：`scripts/validate-skill-contracts.rb` 新增 **B-8 装配机械锁定**（三条断言：runtime.ts 装配存在 / 与 `withResumeLease()` 用同一变量 / entry 内仍有 `isResumeLeaseHeld(...)`）；探针实证删装配行→exit=1、改成 `workspaceRoot`→exit=1。T6 扩展覆盖非字符串 `as any`。断言 22→27。产品提交 **`d9a7517`** 已 push。
- **W6b1 pass-state**：CP 分支 `docs/c03e-w6b1-pass-w6b2`（提交 `cd2a8f2`），**PR #23** 待合并。CP 校验器要求 `product_commit` 必须是**完整 40 位 SHA**（短 SHA 直接 FAIL，已踩）。
- 全套件出现 1 个 FAILED（`loop-codex-implementation-adapter`）时按规程隔离单跑 3/3 全绿 → 定性为既有 sqlite runner 竞争偶发，非缺陷。
- **W6b2（E4-T4）已实现并交付复审**：实施提交 `99c9df3`（+ prompt 基线固化 `f82cc5a`），复审范围 **`d9a7517..99c9df3`**，已 push。
  - 取证：**没有任何生产代码给 `humanActionRef` 赋非 null**（gateway 五处写入点全 null），唯一消费者是 `classifyCapabilityRecovery`(loop-recovery.ts:101)。故本波**只立契约、不接生产者**（与 real 休眠一致，待复审裁决）。
  - 新增 `core/loop-human-action-artifact.ts`：六合法码 allowlist（默认拒绝，SWITCH_AGENT_REQUIRED/SHADOW_FALLBACK_REQUIRED 非法）、构造/序列化/读回三处校验、固定键序序列化、put/read 封装（读回先用正则验 ref kind）、模块边界 failure result。
  - **新增 kind 必须同步三处**：`loop-artifact-store.ts:14` 联合类型 + `:34` KINDS 数组 + `loop-artifact-revision.ts:46` REVISION_KINDS（否则 `:65-67` 编译期漂移检查报错）。并导致 **3 个既有测试的注册表规模断言 17→18**（artifact-revision / artifact-store 两处 / governance-tail-result）——并行套件中后两者曾**稳定失败 3/3**（非 flake）。
  - 有意取舍：**ref kind 强制只放在读回路径**，因 W6a 冻结的 T2-A6/A7 用 kind=`human_action` 的 ref，事件层强制会推翻已 PASS 波次。已作为裁决点写入复审 prompt。
  - 测试 `tests/loop-w6b2-human-action-artifact.test.ts` **82 断言**；自测探针 P1（塞入 SWITCH_AGENT_REQUIRED→T1/T2 红）、P4（放宽字段数检查→T5 红）已还原。
  - 验证：全套件 145 文件，唯一 FAILED（loop-codex-implementation-adapter）隔离 3/3 全绿 = 既有 flake；tsc 干净；3 个 ruby validator exit=0。
- 坑记录：TS 在本仓配置下**不能靠 `!result.ok` 判别联合类型**，需显式类型守卫（已导出 `isHumanActionArtifactFailure`）。
- 待办：W6b2 外部独立复审（prompt `docs/reports/c03-e-w6b2-independent-review-prompt.md`）→ PASS 后出 W6b2 pass-state（CP）→ 进 W6b3（E4-T5）。CP PR #23（W6b1 pass-state）待合并。
  - 决策索引 docs/decisions/README.md，最新 Decision-073（canonical runtime B 为唯一生产路径，链 A 冻结退役）。
  - 当前主线：LOOP-CORE-03 进行中，C03-E E0 已收口、E2-P provider 预检 PASS、E1~E4 已授权且接线进行中（Decision-072），E5/真实激活/C05 未授权。
- **W6b2 独立复审结论：PASS、零阻塞**（外部 agent 在精确基线 `99c9df3` detached HEAD 执行；分支 tip `f82cc5a` 只是 docs-only pin 提交，代码逐字节一致）。
  - 两份清单（I1–I10、失效模式→可判定原因）全成立；契约 a)–h) 全 CLOSED。**探针 P4 未变红**是唯一发现 → 记为非阻塞 **F1**：T7 的异 kind fixture 内容本身是非 JSON，删掉前置正则后仍被 parse 层以「非 JSON」拒掉，前置检查有真实防守价值但无断言钉住（一次性修复＝fixture 改存合法 human-action 内容）。另一建议项 **S2**＝`LOOP_ARTIFACT_REVISION_KINDS` 名实分离，合同外重构。
  - 复审认可两个裁决点：① ref kind 强制只放读回路径（事件层强制需改 `loop-capability-execution.ts:373` 并重写 W6a 已冻结 T2-A7/A10，等于推翻已 PASS 波次）；② `human_action_required` 进 REVISION_KINDS 可接受——`validateLoopArtifactRevision` 的 nodeId+projection 双重校验使该 kind 作为 revision 不可达（fail-closed），只剩编译期漂移检查与 finding 白名单作用。
- **CP pass-state 链路推进**（用户裁决：先合并 #23 再开 #24，每波一个 PR）：
  - PR #23（W6b1 PASS）已 merge → CP `main = 26d53ba`。
  - 新建 CP 分支 `docs/c03e-w6b2-pass-w6b3`（提交 `678796e`），STATE 改三处：`condition_ref`→W6b2 PASS 记录 + W6b3 定义、`product_commit`→`99c9df30816c69f2c5e411dac94b7f2d7d6f4e13`、`observed_at`。`ruby tools/validate_state.rb` → PASS v2。**PR #24 待合并**。
  - 坑：CP `condition_ref` 上限 **500 字符**，第一稿 610 字符需压到 491 才过；写之前先在 /tmp 用 python 量长度，别提交再改。
- 产品仓台账同步（docs-only 提交 `4f9eaad`，已 push）：`c03-e-e1e4-task-set-and-gate-audit.md` 的 E4-T4 行改 PASS 并补三处注册表事实、波次链标注 W6b1/W6b2 PASS 与 W6b3 NEXT；`c03-e-e1e4-integration-handoff.md` 补复审结论、F1/S2 与「kind 同步三处」更正。
- **W6b3 = E4-T5 已实施**（用户指令：开工 W6b3 + 顺手做 F1/S2；ponytail 阶梯已固化到 `~/.workbuddy/MEMORY.md`，BOOTSTRAP/IDENTITY 用户要求暂不动）。
  - 取证：`cleanup()` 本波前**零生产调用方**（全仓只有测试调），且既有断言是**字段级**不是整对象比较 → 给 `CleanupResult` 加字段不会破坏既有 30+ 处调用。
  - 三态（落点 `core/loop-git-workspace.ts`）：options 新增 `outcome?: succeeded|failed|unknown` 与 `allowedPaths?: readonly string[]`；result 新增 `decision` / `outOfBoundsPaths` / `evidenceRetained`；导出纯函数 `classifyWorkspaceCleanup()`（越界 → unknown → failed → succeeded 的判定顺序，越界压过声称的成功）。
  - **promote 不 merge、不 push、不碰 base branch**（E5 未授权）；isolate 不抛异常、以结果返回并保留 worktree+分支；block 抛 `CLEANUP_BLOCKED`（因此 `result.decision==="block"` 在 cleanup 返回里不可达，只能从纯函数读到——已写进复审 prompt 作裁决点）。
  - 越界输入 = **已提交**改动（`diff --name-only -z <base>...HEAD`）+ 未提交/未跟踪（`status --porcelain=v1 -z -uall`，解析包含重命名的第二条原路径记录）。只在显式给 `allowedPaths` 时启用；不给则沿用既有 `WORKSPACE_DIRTY` 语义，避免推翻既有测试的错误码。
  - 新测试 `tests/loop-w6b3-attempt-workspace-three-state.test.ts` **35 断言**（真 git + 真 runner + 真 worktree；T5 组是无 git 的纯函数单测）。四个反向探针 P1–P4 **全部如期转红并还原**：P1 删越界分支→T4 红；P2 前缀匹配放宽为裸 startsWith→T5「sibling directory」红；P3 failed 走 promote→T2 捕获到 WORKSPACE_DIRTY；P4 丢掉已提交 diff→T4 红。
  - **F1**：w6b2 T7 的 foreign fixture 改存**合法** human-action 内容（`buildHumanActionRequiredArtifact().content`），删掉 `readHumanActionRequiredArtifact` 的 ref kind 前置检查后 T7 **转红**（修复前不红）。断言 82→83。
  - **S2**：不复制第二份数组（复制即漂移），改为导出同一对象的具名别名 `LOOP_ARTIFACT_CANONICAL_KINDS` 给"是否 canonical kind"的消费点（`loop-finding-lifecycle.ts:346`），revision 白名单仍用 `LOOP_ARTIFACT_REVISION_KINDS`，**编译期漂移哨兵保留未削弱**。注意 `loop-finding-lifecycle.ts` 是 import-pure（禁引带文件系统的 store 模块），所以不能改指 `LOOP_ARTIFACT_KINDS`。
  - 坑记录：① 我插入 `changedPaths` 常量后，verdict 行仍写着旧参数 `changedPaths: statusPaths(status)` —— tsc 不报错（类型都对）、T1/T2/T3 全绿，只有 T4 暴露。**改完必须 grep 一遍新常量是否真被引用**。② 本仓 bash `grep "a\|b"` 在 zsh 下会静默返回空（转义失效），用 Grep 工具。
- 环境提醒：复审把产品仓留在 detached HEAD 时，收尾记得 `git checkout feature/c03-e1-e4-runtime-implementation`（本轮已切回）。
- **全套件两个文件级失败的定性结论（关键，避免重复排查）**：`tests/loop-artifact-store.test.ts`（6 条失败）与 `tests/loop-delivery-checkpoint-store.test.ts`（3 条）在**本机确定性失败**，全部落在**跨进程并发**段落，**先于 W6b3 存在，不是本波回归**。
  - 根因取证：本机 `link`(2) 硬链接在目标**已存在**时返回合成错误 `CODEBUDDY_BROKER_DENY`，而不是内核的 `EEXIST` → `core/loop-artifact-store.ts:359-363` 并发 put 的落败者进不了 EEXIST 赢家赛分支（`:361`），被兜底为 `ARTIFACT_IO_FAILURE`。三进程同路径 `mkdir→open wx+→write→close→link` 探针 **3/3 稳定 1 成功 / 2 失败**，失败码恒定且 `finalExists=true`；沙箱模式与非沙箱（dangerouslyDisableSandbox）**结果一致**，排除沙箱因素。
  - **基线对照方法（可复用）**：`git worktree add /tmp/ai-sdlc-baseline 99c9df3` + `ln -s` 主仓 node_modules，在干净基线上单跑同一测试 → 失败集合与本机分支**逐字一致**（artifact-store 三处 `runConcurrentPuts` 各 2 条；checkpoint `passed=265 failed=3`）。用完 `git worktree remove --force`。这比 stash 安全，不动工作树。
  - 结论口径：属**环境能力缺口**，不是产品缺陷，也不应通过"把未知 errno 当 EEXIST"来修（会削弱 fail-closed）。已写进 W6b3 复审 prompt（三节末段 + 已知事实 ⑩），要求复审者如实记录自己环境的结果，不得据本机结果判本波。
  - **方法教训（又踩一次）**：zsh 下 `grep "✗\|FAIL"` 静默返回空，害我一度以为没失败行——**这类带 `\|` 的 grep 一律用 Grep 工具**。另外「先建假设再验证」很重要：我猜的是并发 `mkdirSync(recursive)` 撞 EEXIST，写探针证伪后才去查 link，避免把错根因写进台账。
- **W6b3 已提交并 push**：代码提交 `02b642a`（三态 + F1 + S2，35/110/83 断言全绿，tsc 干净），文档提交 `952d9da`（复审 prompt 回填基线 SHA `02b642a`、复审范围 `99c9df3..02b642a`；台账 E4-T5 行 PENDING→实现完成；handoff 快照节改名为 W6b3）。**复审已交、结论未回；CP pass-state 等复审结论回来再写**。
- **W6b3 独立复审结论：NOT_CLOSED（1 项阻塞 B1）→ 已修复 `05d12d2`，聚焦复审待回**。
  - B1 根因：`core/loop-git-workspace.ts:525-526` 的 `git diff --name-only -z <base>...HEAD` 走 git **默认 rename 检测**，rename 只输出目的路径。attempt `git mv secret/b.ts src/b.ts` 并提交后越界源不进 `changedPaths` → 工作区干净 + 声称 succeeded + `allowedPaths:["src"]` → 判 **promote**，worktree 回收、**证据销毁**。
  - 我自己的复现（真 manager + 真 git，两场景对照，比复审方多拆一层）：
    - **场景 A（越界源在 base commit 中已存在）**：`diff default = ["src/b.ts"]`，`--no-renames = ["secret/b.ts","src/b.ts"]` → 修复可覆盖 ✅
    - **场景 B（越界源由 attempt 自建再 mv 走）**：两种 flag 都只输出 `["src/b.ts"]`，因为 `<base>...HEAD` 的最终 diff 里根本没有该路径 → **修复覆盖不到**，属更宽的「中间提交痕迹不出现在最终 diff」问题（不限于 rename：提交后再删除同样成立）。规划 `:443-444` 不变量按 `<base>...HEAD` 定义，故严格说不违反 I7 → 如实记为**已知边界/范围外**，不假装修了。
  - 修复：`:533` 加 `--no-renames` 单 flag（本轮唯一生产改动），rename 按 delete+add 上报。方向一致性佐证：未提交半段 `statusPaths`（`:263-265` 注释 "those bytes were touched too"）本就把 rename 原路径计入，已提交半段漏了同一保守方向。
  - 回归矩阵 T9（35→41 断言）：T9a 越界源→允许汇 block／T9b 允许集合内 rename 仍 promote（防过度收紧）／T9c 越界纯删除 block。`setupRepo(pre)` 新增预置 base 文件能力（`git add f.txt` → `git add -A`）。
  - 反向探针 P5：去掉 `--no-renames` → T9a 两条转红（`39 passed, 2 FAILED`），**T9b/T9c 仍绿** → 证明新断言真实承载。还原后 41 全绿。
  - 复审两项形态裁决（非阻塞）：① cleanup 零生产调用方下交付三态契约 = 合同内正确形态；② promote 不 merge/push = 合同内最小形态（D-071 + E5 未授权）。
  - 产出：`docs/reports/c03-e-w6b3-b1-focused-review-prompt.md`（只审 B1 与回归影响，不重开其余已 CLOSED 项）。
- **复审 prompt 的清单设计被验证有效**：B1 正是由我上一轮 prompt 里「一.d) 已提交部分用 `diff --name-only -z <base>...HEAD` 是否存在漏判场景（**重命名**、子模块、模式变更、二进制）」这条追问挖出来的。教训：**写复审 prompt 时，对「自己用的命令/flag 有没有隐含默认行为」要单列一条让复审者逐一实测**，不能只描述意图。
- **W6b3-B1 聚焦复审 CLOSED（零阻塞）→ W6b3 ✅ PASS，CP pass-state 已出**。
  - 复审核心证据：同一探针在 `02b642a`（修复前）`decision=promote / worktreeRemoved=true / 证据销毁`、`05d12d2`（修复后）抛 `CLEANUP_BLOCKED` + worktree 与 `src/b.ts` 证据保留；`git show 05d12d2` 恰为两文件无夹带；复审方核验备选写法（`-M0`／`--diff-filter`／`diff-tree -r`）后裁定**单 flag 最小且正确**；逐条排除 chmod／二进制／子模块／copy／空目录新漏判；T9a/T9b/T9c 全部真实承载；探针 P1 恰 `39 passed, 2 FAILED`（T9b/T9c 绿）、P2 复现已知边界并**判定划为范围外成立**、P3 证明 `pre` 参数真实承载。
  - **复审方环境全套件 146 文件 / failed=0 / exit=0 / 278.8s**；两个在实现方本机失败的文件在其环境隔离单跑**全绿**（266／268）→ 以**他机对照**坐实本机失败是 broker 拦截 `link`(2) 的既有环境缺口，非本波回归。这比"基线 worktree 对照"更强一层：跨环境对证。
  - 建议项（1 条，非阻塞，须新波次）：中间提交的越界痕迹不出现在 `<base>...HEAD` diff，判定需**逐提交 diff-tree 遍历**的新机制。
  - **CP**：PR #24 已合（main `c3a31f4`）；PR #25 已开（分支 `docs/c03e-w6b3-pass-w7`，提交 `b3d8474`，改 `condition_ref` 494 字符 + `product_commit` → `05d12d2bd37e9957e9242946aff795a79d1e1862` + `observed_at`），校验 PASS v2。
  - **Current User 两项裁决**：① CP 落地＝**先合 #24，再从新 main 开 #25**（延续逐波独立 PR；因为两波都改 STATE.yaml 同一批字段，叠加会冲突）；② W7＝**先出 pass-state，等我确认再开工**。
- **坑：CP 仓库 `.git/index.lock` 被 broker 拦截 unlink**（新坑，会卡死所有 git 操作）。
  - 现象：任意 git 命令后残留 `.git/index.lock`（git 自己创建但 unlink 失败），下一条 `git checkout`/`pull` 即报 "may have crashed in this repository earlier: remove the file manually to continue"；`git pull` 还会 "unable to update local ref"，导致 `origin/main` 不更新。
  - 解法：**必须在非沙箱下执行**（沙箱里 `rm` 也 `Operation not permitted`），且每条 git 命令前先 `find .git -name "*.lock" -delete`；ref 未更新时补 `git fetch origin --prune` 再 `git reset --hard origin/main`。产品仓 ai-sdlc-standard 目前没这个问题，只有 CP 仓有。
- **坑（复发确认）**：zsh 下 `grep "a\|b"` 仍会静默返回空，我又踩了一次（查 prompt 文件结构时得到空结果）。**带 `\|` 的 grep 一律改用 Grep 工具**——已第二次中招。
- **condition_ref 压缩经验**：本轮从 684 字符压到 494 共 7 稿（684→614→584→537→516→503→494）。规律：先删冗余修饰（"ind. review"、"non-blocking"），再合并同义（"promoted and destroyed evidence"），最后砍可推导的尾巴（"Ref: E4-T5"、把 "-> C-T2 closeout" 缩成 "-> C-T2"）。**务必先在 /tmp 用 python 量长度再写文件**。
- **本机 `e92eea3` 全套件回填（59 分钟）：1767 passed / 0 failed（断言级），文件级 failed=3**。
  - 比复审方（146 文件 / failed=0 / 278.8s）慢约 12.7 倍；两个已知环境缺口文件照旧，多出第三个 `tests/loop-codex-implementation-adapter.test.ts`。
  - 第三个的定性：**非 W6b3 回归，属负载/环境型偶发**。证据链：隔离单跑 `354/354 passed`；同文件 3 路并行 3/3 过、**8 路并行 8/8 过**；文件内无 `Date.now`/`setTimeout` 等时序断言；`cleanup()` 调用点（`:982`）**未传** `outcome`/`allowedPaths`，走 W6b3 之前既有分支。
  - **教训（新）**：全套件跑之前别用 `| tail -25`，失败文件的细节会被截掉，事后无法归因。要落盘到文件再 grep。已重跑一版完整落盘（`/tmp/full-suite-e92eea3.log`）等回填。
- **新发现（W6b3 引入、性质为"多余的新失败面"，待裁决是否单开微波）**：`core/loop-git-workspace.ts:532-533` 的 `git diff --name-only -z --no-renames <base>...HEAD` 是**无条件执行**的，但 `changedPaths` 只被 `classifyWorkspaceCleanup` 用，而越界检查又要求 `allowedPaths !== null`（`:300`）。即**未传 allowedPaths 的 30+ 既有调用方每次 cleanup 多跑一个 git 子进程，且结果完全没用**。
  - 后果不只是性能：`_gitR(...,[0])` 对 timeout / signal / 非零退出 / 输出截断一律抛 `GIT_COMMAND_FAILED`（`:626-632`），**新增了一个此前不存在的失败面**——尤其在本机这种 fork 开销被 broker 放大的环境。
  - 最小修法（ponytail，行为等价）：把 diff 门控到 `allowedPaths !== null` 才跑，否则 `committed = ""`。T9 三个用例都传了 allowedPaths，不受影响。
  - **定性纪律**：这是 PASS 之后发现的新东西，不能夹带进已 CLOSED 的 W6b3，也不该塞进 W7 的只读复审；要么单开微波，要么明确记入台账挂到 W7 之后。
- **坑（新）**：GitHub MCP（`mcp__github__*`）对本仓相关私有仓库一律 **404**（`pull_request_read` / `list_pull_requests` 都 404），尽管 `gh auth status` 显示 token 有 `repo`/`workflow` scope。**改用 `gh` CLI 即可正常工作**（`gh pr list --repo shaoyang01/ai-project-control-plane`）。CP PR #25 当前 OPEN。
- **Current User 两项新裁决**：① CP PR #25 **现在合并**（已合，main `2d2ff53`）；② 新发现「committed diff 无条件跑」**单开微波 W6b4**，不夹带进已 CLOSED 的 W6b3、也不塞进 W7 的只读复审。
- **W6b4 已实施并提交 `1605a84`（门控 committed diff，41→45 断言）**。
  - 生产改动就一行：`:540` `const committed = allowedPaths === null ? "" : (await this._gitR(...))` + 8 行注释。
  - 断言 T10a/T10b：用 `Pick<LoopPosixProcessRunner,"run">` 的 spy（manager 的 `runner` 字段只要 `Pick<...,"run">`，所以 spy 天然可注入，不需要 cast）记录每次 git 调用的 args。
  - **踩坑（新）**：一开始断言写成 `args[0]==="diff"`，T10a/T10b **双红**——因为仓库里还有两处 source-wip 的 `git diff --binary`（`:1101`/`:1104`）每次调用都跑。改用 `args[0]==="diff" && args.includes("--name-only")` 区分后才对。**写"某命令被调用几次"的断言前，先 grep 该子命令在文件里的全部出现点**。
  - 反向探针 P6：去掉门控 → T10a 恰转红（`saw 1`，`44 passed, 1 FAILED`），T10b 仍绿。
  - 关联实测：`loop-git-workspace` 110 passed、`loop-codex-implementation-adapter` 354/354（该文件 stderr 的 `fatal: ... is not a working tree` 是既有现象不是失败）、tsc 干净。
  - 复审 prompt：`docs/reports/c03-e-w6b4-focused-review-prompt.md`（基线 `1605a84`，范围 `05d12d2..1605a84`）。**结论待回**。
  - **CP 暂未动**：沿用 B1 先例（修完复审回来才出 pass-state），W6b4 复审回来后再开 CP PR。`condition_ref` 已起草好 497 字符的版本备用。
- **坑（新）**：后台跑全套件时**不能同时改代码**——跑到一半我改了 `core/loop-git-workspace.ts`，那次运行的结果就作废了（tsx 进程是边跑边读文件的）。已在改代码前 `TaskStop` 掉旧运行，改完提交后重跑。
- **重大环境事实（新）：独立复审方与本会话共用同一个本地 clone**（`/Users/eric/meicai/projects/ai-sdlc-standard`），会直接 `git checkout` 到复审基线、并在工作树里跑反向探针。
  - 已观测到：00:12:28 主工作树被 detached 到 `1605a84`（我新建的未合入该 commit 的文件因此从工作树消失）；随后 `core/loop-git-workspace.ts` 被改成 `allowedPaths === null || allowedPaths.length === 0 ? "" : ...` —— 那正是我 prompt 里的**探针 P7**，说明复审正在进行中。
  - **推论与纪律**：① 别在这个工作树里跑长任务（全套件 59 分钟）——已两次因复审方 checkout 而作废，必须停掉；② 别 `git checkout` 主工作树，会打断对方探针；③ 需要提交时用 **`git worktree add /tmp/xxx <branch>`** 在临时 worktree 里操作，动完 `git worktree remove --force`；④ 判断"文件消失了"之前先看 `git reflog`，多半是别人 checkout 了。
  - 另一个教训：`git show --stat <sha> | head -10` 会把第三个文件**截断掉**，我因此误判"新文件没进 commit"。**看 stat 不要加 head**。

## ⚠️ 事实修正（Current User 质疑后自查，覆盖上一条"共仓并发"结论）

上一轮我把主工作树的 detached + `core/loop-git-workspace.ts` 出现 P7 变体，断言为"独立复审方正在共用 clone 跑探针"。**该归因证据不足，撤销。**
- 复核查证（2026-08-30 00:2x）：主工作树 HEAD 停在游离提交 `2cacb39`（我自己 cherry-pick 留下的），`git status` 除 `.workbuddy/` 外**全干净**，P7 改动已不复存在。
- 也就是说：我拿"工作树里出现过一个恰好等于我 prompt 建议的改动"当证据，判定"外部复审在进行中"。**不可自证，属把推测当事实。**
- **纪律（新）**：有没有独立复审，只能以**复审结论文本本身**（Current User 粘回的 NOT_CLOSED/CLOSED 结论）为准，不能以工作树痕迹、reflog、时间戳推断。W6b3 与 W6b3-B1 那两次结论是 Current User 亲自粘回的，才是真外部复审；W6b4 至今没有结论。
- 我自己跑的 P6/P7 反向探针是**实施方自检**，只能证明断言承重，**不能替代复审、也不能作为"进展"汇报**。区分清楚。

## PKB / Exchange 实况核查（2026-08-30）

- **PKB `10-projects/` 下只有一个 `.gitkeep`**，`10-projects/ai-sdlc-standard/`（AGENTS.md §4 写的 `current.md`/`handoffs/`/`audits/`/`prompts/`）**目录根本不存在**，从未建立过。
- **Exchange** 里有 `projects/ai-sdlc-standard/topics/04-*/runs/...`（2026-07 的 topic04 closure/audit 系列），说明发布通道本身通；但 **C03-E 这条需求从 W1 到 W6b4 一次 Exchange 发布都没跑过**。
- **合规判定**：波次 PASS 不是归档触发点，所以"一直没改 PKB"是**对的**，不是遗漏。依据 `GOVERNANCE.md` §15.5：归档只在 **Current User closure event（C-T2）** 之后；且硬规则 **"AI-SDLC MUST NOT write Personal-KB directly"**，必须走 Publisher → PKB-controlled consumer。
- **到 C-T2 时的真实工作量**：整条链要首次建立（Exchange publish + PKB 目录 + `current.md`），不是"补个文件"。要提前排。
- **handoff 放产品仓合规**：task-level Handoff 是生产地的 append-only 记录（§15.3.1），PKB 只是下游镜像。

## ⚠️ 再修正：撤销撤销（Current User 已确认）

上一条我把"独立复审方共用 clone"的归因自行撤销了。**撤销错了，Current User 已确认：他确实拿了 W6b4 prompt 交给别的 agent 在跑。**
- 所以上上轮观察到的 detached + P7 变体改动，**就是那个独立 agent 留下的**，我的原始观察正确。
- **真正该改的纪律（这条才对）**：无法自证时，**不要自行推翻自己的观察**，应标记"待确认"并**向 Current User 求证一句**。我这次跳过求证直接撤销，等于用一次臆断去纠正一次正确判断。
- 保留的有效部分仍然成立：**我自己跑的 P6/P7 是实施方自检，不是复审**；**复审结论只能以结论文本为准**。区别在于——工作树痕迹可以佐证"有人在跑"，但不能证明"复审已通过"。

## Current User 三项新裁决（2026-08-30）
1. W6b4 复审：外部 agent 已在跑，**等结论回来**。
2. **主工作树先不动**（仍游离在 `2cacb39`），等复审结果回来再收拾。任何 git 操作走 `git worktree add /tmp/xxx`，**禁止动主工作树的 HEAD/工作区**。
3. C-T2 的 Exchange + PKB 归档链：**现在就预演一次**（PKB `10-projects/ai-sdlc-standard/` 目录从未建立、C03-E 从未发布过）。

## Exchange / PKB 归档链离线预演（Current User 裁决"现在就预演"）— 已完成，未发布

产物：`docs/reports/c03-e-exchange-pkb-archive-rehearsal.md` + `docs/reports/c03-e-w6b3-exchange-issue-body.md`（分支 `0c0c458`）。
预演是**离线**的：没开 Issue、没打标、没推送 Exchange/PKB。

### 机制要点（复用价值高）
- Publisher 是 **Issue 驱动**：Issue body 含 `---BEGIN EXCHANGE REQUEST---`（YAML）+ `---BEGIN HANDOFF---`；**只有 owner 能写 Issue、打 `exchange-publish` 标签**（policy §8.2）。实施方无权触发。
- topic 允许列表在**仓库变量**（不在 Git 历史里）：`gh api repos/shaoyang01/project-governance-exchange/actions/variables/EXCHANGE_ALLOWED_TOPICS` → 只有 `04-repository-structure-and-root-governance` / `05-project-governance-publishing-personal-kb-integration` / `06-governance-artifact-exchange`。**C03-E 不能新建 topic，复用 06**。
- 三份 schema 的 const 陷阱：`request_version=v1`、`review_status=proposed`、`authorization_status=pending`、`publication_status=not_published` 全是固定值，不能自己创新。
- `manifest-v1` 有 `personal_kb_repository` / `personal_kb_commit` 锚点字段（Exchange→PKB 的正式挂钩），一直没填过。
- run 一旦进 main **不可变**（§5），所以"预演"绝不能真发。

### 预演结果
- Exchange：publish-request / manifest / current 三份 schema + allowlist 全 PASS（离线 jsonschema 校验）。
- PKB：真实克隆里建 `10-projects/ai-sdlc-standard/{handoffs/2026-08-30-c03-e-w6b3-pass.md, current.md}`，`validate_notes.py` 通过（3 文件 0 错误）；反向探针删 `confidence` → 恰转红。
- PKB 受管理笔记硬要求：front matter 必填 8 属性（type/status/created/updated/tags/sources/related/confidence）；type∈{project,concept,source,decision,prompt}；status∈{draft,active,stable,archived} 且 **AI 创建必须 draft**；confidence∈{low,medium,high}。PKB AGENTS.md **禁止未授权 git push**。

### 查出的三个问题（2 个是合规缺口）
1. **W1–W6b4 的 8 个波次在 Exchange 上没有任何 run**。C03-E 只有 3 个 run，全在 8/28 授权阶段。既有实践是逐包发（C01 11 个、C02 8 个），且 GOVERNANCE §15.3.1 要求"独立复审把结果向前传递时必须有 Handoff"。→ 需 Current User 裁决补发还是 C-T2 一次性发。
2. **PKB `10-projects/ai-sdlc-standard/` 从未建立**（`10-projects/` 只有 .gitkeep），尽管已有 42 个 Exchange run。传送在跑、归档一步没做。
3. topic allowlist 只有 3 个（约束，非缺陷）。

### ⚠️ 更正我自己的错误结论
我口头说过"C03-E 从未发布过"——**错**。它一直在 topic06 发布，断更发生在 W1 之后。

### 新坑
- **`head` 截断导致误判，第三次踩**（前两次：`git show --stat | head -10`）。这次是 `grep | head -20` 把 topic06 的 42 个 run 全截掉了，直接导致上面的错误结论。**列远程文件/目录先 `wc -l` 看总数，再下结论。**
- **shell 双引号里的反引号会被命令替换**：用 `echo "...\`generated_at\`..."` 生成 Markdown 会把反引号内容吃掉。改用 **python heredoc（`<<'PY'`）** 写含反引号的文本。
- 主工作树在 00:5x 被 checkout 到 `1605a84`（W6b4 复审基线）——**再次印证复审 agent 确实在用这个 clone**，且正按 prompt 的基线跑。我全程没动主工作树，提交走 `git worktree add /tmp/xxx` + 用完 `worktree remove --force`。
