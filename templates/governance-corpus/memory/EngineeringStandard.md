# 工程技术规范 (Engineering Standard)

> **Metadata**
> - **Version**: 0.1.0
> - **Date**: {{DOC_DATE}}
> - **Author**: {{AUTHOR}}
> - **Status**: Skeleton（初始化器生成的项目治理参考骨架；技术栈相关条款待 Owner 按实际栈填写）
> - **Summary**: 定义 {{PROJECT_NAME}} 的分层、一致性、集成契约与数据访问规范骨架。

## 1. 架构分层规范 (Architecture)

### 1.1 业务主链

- 对外入口 -> 服务编排 -> 领域层的职责边界：<pending-owner-confirmation: 按实际分层填写>
- 入口实现默认只做参数日志与服务委派；若承担状态判断或补偿决策，必须在文档中单独标注。
- 数据访问层仅负责持久化，不承载业务逻辑。

### 1.2 异步与外围执行

- 定时任务、消息监听等外围入口可能同时承担技术编排与业务状态推进，分析阶段必须按职责而非包名判断。
- 异步任务必须有稳定的任务类型或等价幂等标识。

## 2. 一致性与幂等约束 (Consistency & Idempotency)

- 写链路必须显式定义事务边界，禁止把远程调用包进长事务。
- 关键状态迁移链路必须明确锁策略、幂等键与重试策略。
- 状态迁移必须记录源状态、目标状态、失败补偿路径与回滚策略。
- <pending-owner-confirmation: 项目专属一致性约束>

## 3. 集成与契约规范 (Integration)

- 对外契约签名统一定义在契约模块，实现与契约分离。
- 消息发送统一经由收口工具，Topic/Key 常量统一收口到常量类。
- 缓存、分布式锁、配置读取必须通过统一工具类与常量访问，禁止硬编码。
- DTO/Entity/Model/Query 转换必须收口到显式转换工具。
- <pending-owner-confirmation: 项目专属集成约定>

## 4. 数据与性能约束 (Data & Performance)

- 禁止全表扫式的宽泛查询；禁止循环中的远程调用，优先批量。
- 核心查询必须显式评估索引、锁粒度与 N+1 风险。
- <pending-owner-confirmation: 项目专属数据约束>

## 5. 审计清单 (Audit Checklist)

- [ ] 需求与任务覆盖率 >= 100%。
- [ ] 非功能需求（性能/安全/可观测/补偿）已映射任务。
- [ ] 核心链路已覆盖真实状态推进点。
- [ ] 无跨层调用违规、无循环远程调用与 N+1 查询风险。
- [ ] 幂等键、锁与补偿策略有文档或代码映射。

## 修订记录

| 日期 | 版本 | 修订者 | 变更描述 |
| :--- | :--- | :--- | :--- |
| {{DOC_DATE}} | 0.1.0 | {{AUTHOR}} | 初始化器生成骨架，技术栈条款待 Owner 填写。 |
