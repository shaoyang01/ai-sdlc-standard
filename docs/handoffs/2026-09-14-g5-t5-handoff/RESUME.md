# G5-T5 跨机交接：R9 整改已推送（`b00ab7e`），待 R10 复审报告

> Date: 2026-09-16 · Author: AI_SDLC_PROJECT_CONTROLLER（实施会话，R9 整改后、换机收工更新）
> 用途：换机恢复现场。新会话恢复时**先读本文件**，再按 §6 推进。
> 本文件随 `feat/g5-t5-regression-parity` 分支入库——`git fetch` 后即可见，无需额外搬运任何材料。

## 0. 一分钟恢复（新机器，先读这段）

```
git fetch --all
git checkout feat/g5-t5-regression-parity && git pull     # 本文件所在分支
```

⚠️ **待评 / 待整改的代码不在此分支**。R7/R8/R9 三轮整改在叠放分支 `fix/g5-t5-r9-rework` @ `b00ab7e`（PR #164，base = 本分支）。本分支的**代码头**是 `0f3d4e5`（只含 R6 整改）。

## 1. 当前位置（精确）

- 授权：`[G5_MANIFEST_PROJECTION]` state=AUTHORIZED、consumed=false，覆盖 G5-T1..T5；`open_blockers=[]`。T1–T4 已收口；**T5 是 G5 最后一个 delta**。
- **待评对象 = `b00ab7e`（PR #164 头）**。R10 复审 prompt 已随本目录 `R10-review-prompt.md` 入库（同一提交），明日直接交独立复审方，无需再回会话记录里找。
- 分支叠放结构（重要，勿误判基线）：
  `0f3d4e5`（T5 集成分支代码头 = R6 整改经 PR #161 合并）
  → `7b6007c`（R7 整改，PR #162）
  → `e0da318`（R8 整改，PR #163）
  → **`b00ab7e`（R9 整改，PR #164，待评对象）**
  即：**待评对象领先 T5 分支代码头 3 个提交**。
- 未合并 PR：#159（T5 建设，head = 本分支 → base = `feature/loop-runtime-v1`）、#162（R7）、#163（R8）、#164（R9）。**四个全部 OPEN、全部未合并**——合并需 Current User 显式授权（GOVERNANCE §7）。合并 #164 会把 R7/R8/R9 三轮一起并入 T5 分支。
- 本轮（R9）整改 diff = `e0da318..b00ab7e`，恰 **2 文件 +72/−5**（`core/loop-manifest-yaml.ts`、`tests/loop-manifest-ls-ps-matrix.test.ts`）。累积交付面 `89e0b95..b00ab7e` = **10 文件 +2607/−146**。

## 2. 轮次总表（判定结论与整改锚点）

| 轮次 | 评审对象 | 判定 | 整改产出 |
| --- | --- | --- | --- |
| R1 | `490275b` | FAIL（3 阻塞） | → `888089f` |
| R2 | `888089f` | FAIL（6 阻塞；R1 三项判 NOT_CLOSED） | → `1aa774a` |
| R3 | `1aa774a` | FAIL（B3 坏八进制引号规则） | → `384aeec` |
| R4 | `384aeec` | FAIL（B1 NOT_CLOSED：N1–N4 四残口 + S7/S4） | → `c9d7b91` |
| R5 | `c9d7b91` | FAIL（B1 NOT_CLOSED：B1-Ⅰ~Ⅳ + S4 边界） | → `b4c2f14` |
| R6 | `b4c2f14` | FAIL | → `b51fc1c`（经 PR #161 合并为 T5 分支头 `0f3d4e5`） |
| R7 | `0f3d4e5` | FAIL（读取端行界：glued 吞键 / seq 项块；depth≥3 嵌套） | → `7b6007c`（PR #162） |
| R8 | `7b6007c` | FAIL（链头类型分派；折点列；尾断 LF 族） | → `e0da318`（PR #163） |
| R9 | `e0da318` | FAIL（**单阻塞** `T5-R9-RC1-1`：映射头首值静默丢值/腐化） | → **`b00ab7e`（PR #164）** |
| **R10** | **`b00ab7e`** | **待评（下一步）** | — |

**贯穿 R1–R9 的反复缺陷类型**：**"样例闭合而非类闭合"**（评审方九轮内反复点名）。因此每轮修复的验收口径是**同族形态穷尽**，不是报告里点名的样例。

## 3. R9 单阻塞与整改（本轮内容）

- **`T5-R9-RC1-1`**：`emitSequence` 映射头分支对**首值**的处理——非标量首值（嵌套序列 / 嵌套映射 / 空容器）被静默丢弃或腐化，而此前 R7 该点头值为**值正确**、R8 该点头部抛裸 `TypeError`。即本区域已连续三轮"修复即回归"。
- 整改：首值按类型分派发射（标量 → 行内 token；空容器 → 行内 `[]`/`{}`；序列 → 换行 + `emitSequence`；映射 → 换行 + `emitMapping`），兄弟项走 `entries.slice(1)`；`lineTerminatorFor(token)` 保证块标量 token 不再被追加换行。
- 同时把残留声明从注释升级为**可执行残留表**（4 族，每族三要素：形态边界 / 三向行为真相 / 可达性依据），使"声明与实现一致"由机器校验而非人肉比对。
- 本轮修掉了实施方自己的两个错误：测试注释里注入的裸 U+2028（TS 视为行终止符）→ 改为字面量 `\u2028`；残留族(3) 的形态边界误判 → 实测后更正为 `a<LS>b \n`（串中 LS + 收尾 LF 前空格）。

## 4. 已闭合主题（勿重开，R1–R9）

- 发射器判别链与 Psych `visit_String` 等价（`<<`、非法日历 rescue、块内 LS/PS、单尾 LF 折形、嵌套续行缩进、嵌套折行幻影宽度、`hh==24` 精确边界）。
- 读取器拒合法形态（空串 `''` 误判与死循环、发布器 `title: ''`、seq-item map 折叠与块标量续行、内层键下嵌套序列、首键下嵌套 map、首键块标量缩进、单引号折叠语义、`repair` 真实产物形态）。
- 声明与执行对齐（测试头注释场景数与 `main()` 严格对应）。
- 读取端行界对齐（glued 吞键、seq 项块）、depth≥3 嵌套、链头类型分派、折点列、尾断 LF 族。

## 5. 仍然声明的残留（有归属，非沉默遗漏）

- **keep-chomp 族**（R9 §6-A 批准入册）、**N8 与 blank-LF 族**（R9 §6-B 裁定入册）——现已全部写入可执行残留表（`tests/loop-manifest-ls-ps-matrix.test.ts`，4 族），登记忠实性由 R10 复核。
- D-21 / §7.4 跨面解析：T2 交付面无调用点；跨面 legal lag（manual OPEN vs store RESOLVED 端到端）在测试头注释显式标注未执行。
- R1/R2 建议项（`|x` 非法块头当纯文本、`...` 后尾随、keep-chomp `|+` 截读、`!!str` 家族滥用面、LS 非 emitter 形态）：均由 self-digest 网兜底，建议级。
- D05 全仓纯净守卫偶发 file-level 失败（已知运维项，见 §7）。

## 6. 恢复步骤与下一步

1. 执行 §0 两条命令；核验工作区干净。
2. `git checkout fix/g5-t5-r9-rework`——这才是待评/待整改代码。
3. 环境前置自检（见 §7）：`npx tsc --noEmit` 应 0；`npm test` 应全绿（R9 报 166 文件 / 1767 断言）。
4. **等 Current User 交付 R10 复审报告（评审对象 `b00ab7e`）**，然后二选一：
   - **FAIL** → 逐阻塞**先实测复现**（实现 vs Ruby 双向对拍）再修复；提交叠放在 `b00ab7e` 之上（开新一轮 PR）。每轮固定流程：复现 → 修复 → 受影响套件 → 全量回归（`tsc` / `npm test` / 三 Ruby 校验器 / 越界零 diff）→ 提交推送 → 开 PR → **等合并授权**。
   - **PASS** → T5 收口：收口报告 + 合并 PR（#164 → T5 分支，再 #159 → `feature/loop-runtime-v1`）+ Control Plane STATE `product_commit` 推进 → **G5 五任务全部完成**，进入 `G5_D09003_COMPLETION_REVIEW`（Current User 完成裁决）。
5. 中途再换机：只更新本文件 §1/§2（用户口径：**仅在上下班换机时**更新，日常整改轮次不更新）。

## 7. 环境与工具链前置（换机必读）

- Node **v24.x / ABI 137**（本机实测 v24.12.0，`require('better-sqlite3')` 通过）。若默认 node 为 v22（ABI 127），store 测试会以 `LoopRunJournalError: STORE_FAILURE` 失败——那是原生绑定 ABI 不匹配，**不是代码缺陷**。
- 若 `~/.nvm/alias/default` 指向 `lts/*` 而解析到的版本未安装，需确认 `~/.zshrc` 中 PATH 设置位于 nvm 加载块**之前**（历史修复点）。
- **ruby 3.3.12 + Psych 5.1.2**（本机实测）：金样复探基准；版本不同先报告再定对拍口径。
- `tests/loop-codex-implementation-adapter.test.ts` 的 D05 全仓纯净守卫：`npm test` 期间若向仓工作区并发写文件会翻转为 file-level 失败（断言实际 0 失败，已知运维项）。
- 复核工作区建议：`git worktree add /tmp/g5-t5-review-r10 b00ab7e`。

## 8. 证据资产位置

- 复审方每轮**自建探针**（不依赖前轮脚本）；上机 `/tmp/*-probes` 与 `/tmp/g5-t5-review-r*` 工作树**不随仓迁移**，换机后不存在——但各轮对象 SHA 均已推送，可按 §1 的 SHA 重建。
- R1–R9 复审报告正文只存在于上一会话的对话记录；本文件 §2/§3 与附录已覆盖其判定结论与修复边界，可作对照基线。
- R5 时代的旧版 RESUME（含 R1–R2 明细原表）见 git 历史 `d4e1c26`。

## 附录：历史轮次明细（R1–R2，自旧版 RESUME 保留，防信息丢失）

**R2 六项阻塞与修复**：

| 编号 | 内容 | 修复 |
| --- | --- | --- |
| RC1-1 | 嵌套序列折行**幻影宽度**：token 已含 `pad + "- - "` 前缀却再传 `columnBefore = indent+4`，前缀重复计列，每层嵌套早断一词（**实施方自写金样编码了这个 bug**，Ruby 真值为 16/4） | `applyFolding(..., 0, indent+4)`；金样改为 Ruby 真相 |
| RC1-2 | `hh <= 23` 写死；Psych 用 `Time.utc`，`24:00:00` 合法（归一到次日） | 精确规则：`hh == 24` 仅当 `mm == 0 && ss == 0` |
| RC2-1 | 发布器 **repair 真实产物被拒**（seq-item map 缺"内层键下嵌套序列"= `corrected_entries` 形态，及"首键下嵌套 map"） | 补两类嵌套容器分支；真实产物现与 Psych 逐值一致 |
| RC2-2 | 首键块标量缩进误用序列缩进 → 每行多两空格并**吞掉后续内层键**（拒绝退化为错读） | 改用 childIndent（与内层键路径一致） |
| RC2-3 | 单引号多行折叠未实现 Psych 语义 | 断前剥尾空格、单断折空格、n≥2 断折 n−1 断；引号标量不受缩进界定（以闭合引号终止；未闭合仍 EOF fail-closed） |
| RC4-1 | 头注释声称 "three scenarios" 而 `main()` 执行四个（**刚修完同类问题即复发**） | 头注释改为与 `main()` 严格对应的编号列表（现为六场景） |

**R1 三项阻塞的最终归属（重要：勿按 `888089f` 当作已闭合）**——R1 三项不是在 `888089f` 收口的，`888089f` 只做了样例级修复，R2 复审判定三项全部 **NOT_CLOSED**（"样例闭合而非类闭合"），剩余部分由 `1aa774a` 补完：

| R1 编号 | 内容 | `888089f` 做了什么 | `1aa774a` 补完的部分（类闭合） |
| --- | --- | --- | --- |
| RC1-1 | 发射器判别链不等价 | `<<`、非法日历 rescue、块内 LS、单尾 LF 折形、嵌套续行缩进 | 嵌套折行断点幻影宽度、`hh==24` 精确边界 |
| RC2-1 | 读取器拒合法形态 | 空串 `''` 误判+死循环、发布器 `title: ''`、seq-item map 折叠与块标量续行 | 内层键下嵌套序列与首键下嵌套 map、首键块标量缩进、单引号折叠语义 |
| RC6-1 | 声明与执行不符 | 删死代码 `scenarioAcceptedMixedV9`、头注释与执行对齐 | 头注释 "three scenarios" 同类复发 → 编号列表 |

**核验口径**：判"R1 三项是否闭合"时基线为代码头 `1aa774a`；判据是**类闭合**（同族形态穷尽），不是报告中点名的样例。
