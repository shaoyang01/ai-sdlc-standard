# G5-T5-R10 独立复审任务 —— T5-R9 整改（`b00ab7e`）收束确认式全量复审

> 本文件是**交付给独立复审方的任务书原文**（2026-09-16 生成）。与 `RESUME.md` 同一提交入库，换机后无需再从会话记录里取。

## 0. 判定纪律（先读，约束全文）

- 你是本轮唯一判定者。实施方自验（矩阵 38 断言、残留表可执行断言、回归全绿）只证明"实现与实施方的理解一致"，不进入判定链。唯一期望值来源：Ruby 3.3.12 / Psych 5.1.2 的真实行为（必须亲自复探）、冻结稿 v1.8.0、合同 v1.0.0 §6.2 族，以及 R1–R9 报告的既有判定（守恒基线，不重开）。
- **本轮性质是"收束确认"**：R9 已给出 §6-C 五条收束判据并声明"整改后满足判据、无需下一轮形态修补"。因此本轮不是再来一轮形态发现，而是回答：**五条判据是否真的全部达成**——达成即 PASS（T5 可交付、G5 五任务完成、可进入 `G5_D09003_COMPLETION_REVIEW`）；未达成则明确指出缺哪一条、缺在哪个形态族。
- **但请不要因此降低攻击强度。** 本主题已有三次连续回归，全部发生在这个区域的"修复"里：R7 头值正确 → R8 头抛裸 TypeError → R9 头静默腐化 → 本轮修复。实施方本轮的改动又落在 `emitSequence` 的映射头分支（前三次的同一处）。请对**改动点相邻的全部形态**做穷尽攻击，而不是只验证 R9 点名的那 9 个变体。
- 判据三（**零静默腐化**）是上一轮唯一未达项，也请作为本轮最高优先：发射器与读取器对**任何**输入都不得静默丢值或错读——不支持形态只能 fail-closed（`LoopManifestYamlError`）。请用你自建的 fuzz/组合语料去证伪，而不是接受"9 个变体已对"。
- **"测试通过"不构成符合性证据**；金样与残留表断言均须自探。特别地：本轮把多个残留族写成**可执行断言**（断言其"字节差/不自往返"等**实现现状**）——请独立裁决这些断言是否忠实反映实现行为，而不是把不一致"断言成合法"。
- 冻结稿与合同已冻结；T1–T4 已收口与 R1–R9 已 CLOSED 项不重开。
- 最终结论一行开头：**PASS** 或 **FAIL**。同根因症状合并编号（`T5-R10-RC#-N`）。

## 1. 范围与基线（先核对，再审查）

- 复审副本 HEAD 精确为 **`b00ab7e`**（PR #164 分支头；**评审对象为分支头**）。
- **分支叠放结构（务必先核对）**：
  - T5 集成分支 `feat/g5-t5-regression-parity` 的**代码头**为 **`0f3d4e5`**（R6 整改，经 PR #161 合并）。该分支之上另有**一个本目录文档提交**（`RESUME.md` 与本文件），**不含任何产品代码改动**——核验方式：`git diff 0f3d4e5..feat/g5-t5-regression-parity --stat` 应只列出本目录两个文档。
  - R7/R8/R9 三轮整改是**未合并的叠放分支**：`7b6007c`（R7 整改，父 `0f3d4e5`）→ `e0da318`（R8 整改，父 `7b6007c`）→ **`b00ab7e`（R9 整改，父 `e0da318`）**。
  - 即本轮对象**领先 T5 分支代码头 3 个提交**，合并时将快进这三轮。请确认这一点与你的复核对象一致。
- 相关 PR 状态（均 OPEN、均未合并）：#159（head = T5 集成分支 → base `feature/loop-runtime-v1`）、#162（R7）、#163（R8）、#164（R9，head = **本对象**，base = T5 集成分支）。#164 未合并**不影响**你对 `b00ab7e` 的判定——按 SHA 检出即可。
- **本轮整改 diff = `e0da318..b00ab7e`**，恰 2 文件 +72/−5：`core/loop-manifest-yaml.ts`、`tests/loop-manifest-ls-ps-matrix.test.ts`。**R9 评审对象 = `e0da318`**。累积交付面 = `89e0b95..b00ab7e`（10 文件 +2607/−146）。
- 环境前置：Node v24.x 且 ABI=137（`require('better-sqlite3')` 可加载）；**ruby 3.3.12 + Psych 5.1.2**。默认 node v22 时 store 测试 `STORE_FAILURE` 属 ABI 不匹配，不得上报。
- `npx tsc --noEmit` 退出 0；`npm test` 全绿（实施方声称 166 文件 / 1767 断言）；三 Ruby 校验器 exit 0——独立复跑，未复跑项标注。
- 复审工作区：`git worktree add /tmp/g5-t5-review-r10 b00ab7e`；全程只读，探针放副本内 untracked 或 `/tmp`。⚠️ D05 全仓纯净守卫会被并发工作区文件活动翻转为 file-level 失败（已知运维项）。
- 越界零 diff（必须为空）：`git diff 89e0b95..b00ab7e -- '00-需求资料/intake.manifest.json' 'scripts/publish-requirement-manifest.sh' 'skills' 'ai-sdlc' 'templates' 'core/loop-run-store.ts' 'core/loop-capability-execution.ts' 'core/loop-artifact-revision.ts'`。

## 2. 本轮的根因（判定主体）

- **RC-1 R9 阻塞闭合（映射头首值发射）**：首值按类型发射（序列/嵌套映射/空容器/块/折行/LS）。失守形态：分派漏掉组合（多键且首值为容器、首值为容器且带兄弟序列、depth≥4、首值与兄弟均为容器、空容器带兄弟、首值为 LS 且带兄弟）；或本轮修复使**原本正确的形态**回退（本区域已有三次前科）。
- **RC-2 零静默腐化（判据三）**：发射器/读取器对任何输入不得静默丢值或错读。攻击方式：以自建组合/fuzz 语料（容器 × 标量 × 空 × LS/PS/LF × depth × 上下文）系统性扫描，凡出现"字节可发但值丢失/错读"而无异常的，即为阻塞；同时扫描**裸异常**（非 `LoopManifestYamlError`）路径。
- **RC-3 残留表忠实性（判据二 + 判据五）**：本轮把残留族写成**可执行断言**。请逐条核验：形态边界是否精确、三向行为真相（字节 / 我方读 / Ruby 交叉读）是否与实测一致、可达性依据是否成立；并核验**是否存在未入册的分歧族**（即你在 §3 自行发现的任何族是否已在表中）。特别注意：把实现现状"断言成事实"不等于该现状已被批准——请区分"登记一致"与"应当修复"。
- **RC-4 真实面逐字节（判据一）**：真实发布器全链产物（init 无 title / 多行 title / 声明链 / finding / repair 含 CJK、折行、自由文本）发射字节与 Ruby 一致、读取值逐值等于 Psych。
- **RC-5 守恒（判据四）**：全量测试 + 三校验器 + 接管链端到端（takeover PUBLISHED → replay NO_OP → 独立篡改 MISMATCH_STOP → 还原 NO_OP）在受审头全绿；R1–R9 已 CLOSED 项逐项复跑。

## 3. 逐根因闭环验证

**RC-1**：独立构造"链头类型 × 容器状态 × 兄弟存在 × depth(2/3/4) × 值形态(标量/LS/长折行/空/null/映射/序列)"的**组合矩阵**（规模自定，不少于 40 例），逐例做**字节 + 我方读数 + Ruby 交叉读**三向判定；并对 R7/R8/R9 三头同输入做回退对照，确认本轮不再引入三连回归。

**RC-2**：自建 fuzz/组合语料系统性扫描"静默丢值/错读"与"裸异常"；报告命中清单（若有）；对每个命中判定是已入册族还是新缺口。

**RC-3**：逐条核验残留表四族（kc / N8 / 收尾 LF 前空格 + 串中 LS / 历史三项）的断言与实测一致性；并检查是否有**你发现的族未被入册**。

**RC-4**：真实发布器全链（至少 init 无 title、多行 title、声明链、finding OPEN/RESOLVED、repair 带漂移五阶段）在 `b00ab7e` 下逐值等于 Psych，并与 `0f3d4e5`（T5 分支代码头）解析结果比对确认无回归。

**RC-5**：R1–R9 已 CLOSED 项回归表（逐项保持/回归 + 锚点），含三个 core 文件零 diff 与非生产路径零行为变化。

## 4. 有意为之的事实（不得作为缺陷上报）

- **分支叠放为有意形态**（R7/R8/R9 三轮未合并、逐级叠放）；T1–T4 已收口与 R1–R9 已 CLOSED 项不重开。
- keep-chomp 族已由 R9 §6-A 批准入册；N8 与 blank-LF 族已由 R9 §6-B 裁定入册。本轮请核验**登记忠实性**，而非重新争论豁免。
- D05 全仓纯净守卫偶发 file-level 失败为已知运维项。
- G6/run8/C03-E 完成判断/C05、D091 业务收编不在授权范围；真实 CLI、业务仓、shadow、Skill 面零 diff 是预期而非成果。
- 交接文档 `docs/handoffs/2026-09-14-g5-t5-handoff/RESUME.md` 只在跨机交接时更新，其时效性不作为判定对象。

## 5. 输出要求

1. 总判定一行 **PASS / FAIL**。
2. **R9 阻塞 `T5-R9-RC1-1` 逐项 CLOSED / NOT_CLOSED** + 独立证据锚点。
3. 逐根因判定表：RC-1..RC-5 各 CLOSED / NOT_CLOSED。
4. **§6-C 五条收束判据的逐条达成判定**（这是本轮的核心交付物）：判据一（真实面逐字节）、判据二（残留全入册，三要素齐备）、判据三（零静默腐化）、判据四（守恒）、判据五（残留表随实现演进）——每条给出达成/未达成 + 证据。
5. §3 各项结果逐项列出：链头组合矩阵三向判定表、fuzz/静默腐化扫描结果、残留表逐条核验、真实发布器全链逐值比对、R7/R8/R9 回退对照。
6. 基线复跑结果表（tsc / npm test / 三 Ruby 校验器 / 越界零 diff），未复跑项明确标注。
7. R1–R9 已 CLOSED 项回归结论表（逐项"保持/回归"）。
8. 建议项单列（不阻塞），注明依据与归属。
9. 不属于本任务（明确排除）：G6/run8/C03-E 完成判断/C05；D091 业务收编；冻结稿/合同修订；收口报告与 STATE 登记；产品仓合并与发布；边界面改造。
10. **若五条判据全部达成且无阻塞：明确给出 PASS**，并声明 T5 可交付、G5 五任务全部完成、可进入 Current User 完成裁决 `G5_D09003_COMPLETION_REVIEW`；若未达成，明确指出缺哪条、缺在哪个族，以及修复边界。
