// Regression Test — Real Agent Adapter Capability Matrix (Static)
// ================================================================
// Verifies the machine-readable adapter matrix is accurate.
// Uses execution/types.ts ExecutionRequestType as source of truth.
// No runtime, no Gateway, no adapters, no network.

import * as fs from "node:fs";

const VALID_REQUEST_TYPES = [
  "llm_task",
  "code_generation",
  "review",
  "validation",
  "code_review",
  "bugfix",
];

const INVALID_REQUEST_TYPES = [
  "requirement_summary",
  "tech_design",
  "solution_review",
];

const VALID_ARTIFACT_TYPES = [
  "requirement_summary",
  "tech_design",
  "solution_review",
  "code_patch",
  "code_review",
  "bugfix_patch",
  "validation_report",
  "shadow_output",
];

async function test() {
  let passed = 0;
  let failed = 0;

  function assert(condition: boolean, message: string) {
    if (condition) {
      passed++;
      console.log(`  ✓ ${message}`);
    } else {
      failed++;
      console.error(`  ✗ ${message}`);
    }
  }

  const raw = fs.readFileSync("real-agent-adapter-capability-matrix.json", "utf-8");
  const m = JSON.parse(raw);

  console.log("Real Agent Adapter Capability Matrix Test\n");

  // ── Test 1: Basic structure ──
  console.log("Test 1: Basic structure");
  assert(m.version === 1, "version is 1");
  assert(m.default_execution_mode === "shadow", "default is shadow");
  assert(m.real_execution_default === false, "real execution not default");
  assert(m.source_of_truth_request_types === "execution/types.ts:ExecutionRequestType", "source of truth declared");
  console.log("");

  // ── Test 2: Adapter statuses ──
  console.log("Test 2: Adapter statuses");
  const shadow = m.adapters.find((a: Record<string, unknown>) => a["adapter"] === "shadow");
  const codex = m.adapters.find((a: Record<string, unknown>) => a["adapter"] === "codex");
  const kimi = m.adapters.find((a: Record<string, unknown>) => a["adapter"] === "kimi");
  const hermes = m.adapters.find((a: Record<string, unknown>) => a["adapter"] === "hermes");
  assert(shadow !== undefined && codex !== undefined && kimi !== undefined && hermes !== undefined, "all 4 adapters exist");
  assert(shadow["status"] === "implemented", "shadow implemented");
  assert(codex["status"] === "feature_flagged_partial", "codex feature_flagged_partial");
  assert(kimi["status"] === "cli_contract_stub_with_dry_run_harness", "kimi cli contract stub with dry-run harness");
  assert(kimi["dry_run_harness"] === "implemented_no_process_spawn", "kimi dry-run harness implemented");
  assert(kimi["executor_contract"] === "implemented_contract_only", "kimi executor contract implemented");
  assert(kimi["command_executor"] === "implemented_feature_flagged_isolated", "kimi command executor isolated");
  assert(kimi["command_executor_default_enabled"] === false, "kimi command executor default disabled");
  assert(kimi["command_executor_wired_to_gateway"] === false, "kimi command executor not wired to gateway");
  assert(kimi["command_executor_wired_to_runtime"] === false, "kimi command executor not wired to runtime");
  assert(kimi["gateway_integration_contract"] === "implemented_contract_only", "kimi gateway contract implemented");
  assert(kimi["gateway_integration_wired"] === false, "kimi gateway not wired");
  assert(kimi["gateway_integration_default_enabled"] === false, "kimi gateway default disabled");
  const gwFlags = kimi["gateway_integration_requires_flags"] as string[];
  assert(Array.isArray(gwFlags) && gwFlags.includes("SDLC_KIMI_GATEWAY_INTEGRATION=enabled"), "gateway requires gateway flag");
  assert(gwFlags.includes("SDLC_KIMI_CLI_COMMAND_EXECUTION=enabled"), "gateway requires command flag");
  assert(kimi["gateway_shadow_sidecar"] === "implemented_feature_flagged_sidecar", "kimi shadow sidecar implemented");
  assert(kimi["gateway_shadow_sidecar_default_enabled"] === false, "kimi shadow sidecar default disabled");
  assert(kimi["gateway_shadow_sidecar_wired_to_runtime"] === false, "kimi shadow sidecar not wired");
  assert(kimi["gateway_shadow_sidecar_changes_routing"] === false, "kimi shadow sidecar no routing");
  assert(kimi["gateway_shadow_sidecar_changes_final_status"] === false, "kimi shadow sidecar no final status");
  assert(kimi["runtime_attachment_contract"] === "implemented_contract_only", "kimi runtime attachment contract");
  assert(kimi["runtime_attachment_default_enabled"] === false, "kimi runtime attachment default disabled");
  assert(kimi["runtime_attachment_wired_to_runtime"] === false, "kimi runtime attachment not wired");
  assert(kimi["runtime_attachment_changes_routing"] === false, "kimi runtime attachment no routing");
  assert(kimi["runtime_attachment_changes_final_status"] === false, "kimi runtime attachment no final status");
  const kimiEvidence = kimi["evidence"] as string[];
  assert(kimiEvidence.includes("execution/kimi-runtime-attachment-contract.ts"), "kimi evidence includes runtime attachment contract");
  assert(kimiEvidence.includes("tests/kimi-runtime-attachment-contract.test.ts"), "kimi evidence includes runtime attachment test");
  assert(kimiEvidence.includes("core/kimi-runtime-shadow-attachment.ts"), "kimi evidence includes runtime shadow attachment helper");
  assert(kimiEvidence.includes("tests/kimi-runtime-shadow-attachment.test.ts"), "kimi evidence includes runtime shadow attachment test");
  assert(kimiEvidence.includes("tests/runtime-kimi-shadow-attachment.test.ts"), "kimi evidence includes runtime integration test");
  assert(kimi["runtime_shadow_attachment"] === "implemented_feature_flagged_runtime_sidecar", "kimi runtime shadow attachment");
  assert(kimi["runtime_shadow_attachment_default_enabled"] === false, "kimi runtime shadow attachment default disabled");
  assert(kimi["runtime_shadow_attachment_changes_routing"] === false, "kimi runtime shadow attachment no routing");
  assert(kimi["runtime_shadow_attachment_changes_final_status"] === false, "kimi runtime shadow attachment no final status");
  assert(kimi["runtime_shadow_attachment_changes_agent_selection"] === false, "kimi runtime shadow attachment no agent");
  assert(kimiEvidence.includes("execution/kimi-gateway-real-dispatch-contract.ts"), "kimi evidence includes real dispatch contract");
  assert(kimiEvidence.includes("tests/kimi-gateway-real-dispatch-contract.test.ts"), "kimi evidence includes real dispatch test");
  assert(kimi["gateway_real_dispatch_contract"] === "implemented_contract_only", "kimi real dispatch contract");
  assert(kimi["gateway_real_dispatch_default_enabled"] === false, "kimi real dispatch default disabled");
  assert(kimi["gateway_real_dispatch_wired_to_gateway"] === true, "kimi real dispatch wired");
  assert(kimi["gateway_real_dispatch_invokes_cli"] === false, "kimi real dispatch no CLI");
  assert(kimi["gateway_real_dispatch_changes_final_status"] === false, "kimi real dispatch no final status");
  const kimiRT = kimi["gateway_real_dispatch_supported_request_types"] as string[];
  assert(kimiRT.length === 1 && kimiRT.includes("llm_task"), "kimi real dispatch llm_task only");
  const kimiRF = kimi["gateway_real_dispatch_requires_flags"] as string[];
  assert(kimiRF.includes("SDLC_KIMI_GATEWAY_REAL_DISPATCH=enabled"), "kimi real dispatch requires real flag");
  assert(kimiRF.includes("SDLC_KIMI_GATEWAY_INTEGRATION=enabled") && kimiRF.includes("SDLC_KIMI_CLI_COMMAND_EXECUTION=enabled"), "kimi real dispatch 3 flags");
  assert(kimi["gateway_real_dispatch_fallback_policy"] === "implemented", "kimi fallback policy");
  assert(kimi["gateway_real_dispatch_errors_sanitized"] === true, "kimi errors sanitized");
  assert(kimi["gateway_real_dispatch_expands_request_types"] === false, "kimi no expansion");
  assert(kimiEvidence.includes("execution/kimi-gateway-real-dispatch-fallback-policy.ts"), "kimi evidence fallback policy");
  assert(kimiEvidence.includes("tests/kimi-gateway-real-dispatch-fallback-policy.test.ts"), "kimi evidence fallback test");
  assert(kimiEvidence.includes("execution/kimi-gateway-real-dispatch-observability.ts"), "kimi evidence observability");
  assert(kimiEvidence.includes("tests/kimi-gateway-real-dispatch-observability.test.ts"), "kimi evidence observability test");
  assert(kimi["gateway_real_dispatch_observability"] === "implemented", "kimi observability");
  assert(kimi["gateway_real_dispatch_observability_persisted"] === false, "kimi observability not persisted");
  assert(kimi["gateway_real_dispatch_observability_contains_raw_prompt"] === false, "kimi observability no prompt");
  assert(kimi["gateway_real_dispatch_observability_contains_raw_artifacts"] === false, "kimi observability no artifacts");
  assert(kimi["gateway_real_dispatch_observability_contains_secrets"] === false, "kimi observability no secrets");
  assert(kimi["gateway_real_dispatch_guardrails"] === "implemented", "kimi guardrails implemented");
  assert(kimi["gateway_real_dispatch_guardrails_blocks_large_prompt"] === true, "kimi guardrails block large prompt");
  assert(kimi["gateway_real_dispatch_guardrails_blocks_large_input"] === true, "kimi guardrails block large input");
  assert(kimi["gateway_real_dispatch_guardrails_validates_cli_config"] === true, "kimi guardrails validate config");
  assert(kimi["gateway_real_dispatch_guardrails_validates_timeout"] === true, "kimi guardrails validate timeout");
  assert(kimi["gateway_real_dispatch_guardrails_clamps_output_summaries"] === true, "kimi guardrails clamp summaries");
  assert(kimi["gateway_real_dispatch_guardrails_expands_request_types"] === false, "kimi guardrails no expansion");
  assert(kimiEvidence.includes("execution/kimi-gateway-real-dispatch-guardrails.ts"), "kimi evidence guardrails");
  assert(kimiEvidence.includes("tests/kimi-gateway-real-dispatch-guardrails.test.ts"), "kimi evidence guardrails test");
  assert(kimi["request_type_expansion_contract"] === "implemented_contract_only", "kimi expansion contract");
  assert(kimi["request_type_expansion_contract_status"] === "NO_EXPANSION_IN_THIS_PR", "kimi expansion NO_EXPANSION");
  const expTypes = kimi["request_type_expansion_newly_supported_types"] as string[];
  assert(Array.isArray(expTypes) && expTypes.length === 0, "kimi expansion no new types");
  const curTypes = kimi["request_type_expansion_current_supported_types"] as string[];
  assert(Array.isArray(curTypes) && curTypes.length === 1 && curTypes[0] === "llm_task", "kimi expansion current llm_task only");
  assert(kimi["request_type_expansion_defer_code_generation_to"] === "codex", "kimi expansion defer code_gen to codex");
  assert(kimi["request_type_expansion_defer_review_validation_to"] === "hermes", "kimi expansion defer review to hermes");
  assert(kimi["request_type_expansion_bugfix_requires_separate_review"] === true, "kimi expansion bugfix separate review");
  assert(kimiEvidence.includes("execution/kimi-request-type-expansion-contract.ts"), "kimi evidence expansion contract");
  assert(kimiEvidence.includes("tests/kimi-request-type-expansion-contract.test.ts"), "kimi evidence expansion test");
  assert(hermes["status"] === "cli_contract_stub_with_dry_run_harness", "hermes cli contract stub with dry-run harness");
  assert(hermes["dry_run_harness"] === "implemented_no_process_spawn", "hermes dry-run harness implemented");
  assert(hermes["executor_contract"] === "implemented_contract_only", "hermes executor contract implemented");
  assert(hermes["command_executor"] === "implemented_feature_flagged_isolated", "hermes command executor implemented");
  assert(hermes["command_executor_default_enabled"] === false, "hermes command executor default disabled");
  assert(hermes["command_executor_wired_to_gateway"] === false, "hermes command executor not wired gateway");
  assert(hermes["command_executor_wired_to_runtime"] === false, "hermes command executor not wired runtime");
  assert(hermes["command_executor_requires_flag"] === "SDLC_HERMES_CLI_COMMAND_EXECUTION=enabled", "hermes command executor flag");
  assert(hermes["command_executor_uses_fake_runner_in_tests"] === true, "hermes command executor fake runner");
  assert(hermes["command_executor_writes_files"] === false, "hermes command executor no files");
  assert(hermes["command_executor_persists_audit"] === false, "hermes command executor no persist");
  assert(hermes["command_executor_changes_final_status"] === false, "hermes command executor no final status");
  assert(hermes["command_executor_changes_routing"] === false, "hermes command executor no routing");
  const hermesEvidence = hermes["evidence"] as string[];
  assert(hermesEvidence.includes("execution/hermes-cli-command-executor.ts"), "hermes evidence command executor");
  assert(hermesEvidence.includes("tests/hermes-cli-command-executor.test.ts"), "hermes evidence command executor test");
  assert(hermes["gateway_integration_contract"] === "implemented_contract_only", "hermes gateway contract");
  assert(hermes["gateway_integration_contract_default_enabled"] === false, "hermes gateway default disabled");
  assert(hermes["gateway_integration_contract_wired_to_gateway"] === false, "hermes gateway not wired gateway");
  assert(hermes["gateway_integration_contract_wired_to_runtime"] === false, "hermes gateway not wired runtime");
  assert(hermes["gateway_integration_contract_invokes_cli"] === false, "hermes gateway no CLI");
  assert(hermes["gateway_integration_contract_spawns_process"] === false, "hermes gateway no spawn");
  const hgwSupported = hermes["gateway_integration_contract_supported_request_types"] as string[];
  assert(Array.isArray(hgwSupported) && hgwSupported.includes("review") && hgwSupported.includes("code_review") && hgwSupported.includes("validation"), "hermes gateway supported types");
  const hgwUnsupported = hermes["gateway_integration_contract_unsupported_request_types"] as string[];
  assert(Array.isArray(hgwUnsupported) && hgwUnsupported.includes("llm_task") && hgwUnsupported.includes("code_generation") && hgwUnsupported.includes("bugfix"), "hermes gateway unsupported types");
  const hgwFlags = hermes["gateway_integration_contract_requires_flags"] as string[];
  assert(Array.isArray(hgwFlags) && hgwFlags.length === 2, "hermes gateway 2 flags");
  assert(hermes["gateway_integration_contract_changes_final_status"] === false, "hermes gateway no final status");
  assert(hermes["gateway_integration_contract_changes_routing"] === false, "hermes gateway no routing");
  assert(hermesEvidence.includes("execution/hermes-gateway-integration-contract.ts"), "hermes evidence gateway contract");
  assert(hermesEvidence.includes("tests/hermes-gateway-integration-contract.test.ts"), "hermes evidence gateway test");
  assert(hermes["gateway_shadow_sidecar"] === "implemented_feature_flagged_sidecar", "hermes shadow sidecar");
  assert(hermes["gateway_shadow_sidecar_default_enabled"] === false, "hermes shadow sidecar default disabled");
  assert(hermes["gateway_shadow_sidecar_wired_to_gateway"] === false, "hermes shadow not wired gateway");
  assert(hermes["gateway_shadow_sidecar_wired_to_runtime"] === false, "hermes shadow not wired runtime");
  const hssFlags = hermes["gateway_shadow_sidecar_requires_flags"] as string[];
  assert(Array.isArray(hssFlags) && hssFlags.length === 3, "hermes shadow 3 flags");
  const hssSupported = hermes["gateway_shadow_sidecar_supported_request_types"] as string[];
  assert(Array.isArray(hssSupported) && hssSupported.includes("review") && hssSupported.includes("code_review") && hssSupported.includes("validation"), "hermes shadow supported types");
  const hssUnsupported = hermes["gateway_shadow_sidecar_unsupported_request_types"] as string[];
  assert(Array.isArray(hssUnsupported) && hssUnsupported.includes("llm_task") && hssUnsupported.includes("code_generation") && hssUnsupported.includes("bugfix"), "hermes shadow unsupported types");
  assert(hermes["gateway_shadow_sidecar_affects_primary_gateway_result"] === false, "hermes shadow no primary");
  assert(hermes["gateway_shadow_sidecar_changes_final_status"] === false, "hermes shadow no final status");
  assert(hermes["gateway_shadow_sidecar_changes_routing"] === false, "hermes shadow no routing");
  assert(hermes["gateway_shadow_sidecar_writes_files"] === false, "hermes shadow no files");
  assert(hermes["gateway_shadow_sidecar_persists_audit"] === false, "hermes shadow no persist");
  assert(hermes["gateway_shadow_sidecar_uses_fake_runner_in_tests"] === true, "hermes shadow fake runner");
  assert(hermesEvidence.includes("execution/hermes-gateway-shadow-sidecar.ts"), "hermes evidence shadow sidecar");
  assert(hermesEvidence.includes("tests/hermes-gateway-shadow-sidecar.test.ts"), "hermes evidence shadow sidecar test");
  assert(hermes["runtime_attachment_contract"] === "implemented_contract_only", "hermes runtime attachment contract");
  assert(hermes["runtime_attachment_contract_default_enabled"] === false, "hermes runtime attachment default disabled");
  assert(hermes["runtime_attachment_contract_wired_to_runtime"] === false, "hermes runtime attachment not wired");
  assert(hermes["runtime_attachment_contract_changes_final_status"] === false, "hermes runtime attachment no final status");
  assert(hermes["runtime_attachment_contract_changes_routing"] === false, "hermes runtime attachment no routing");
  assert(hermes["runtime_attachment_contract_writes_files"] === false, "hermes runtime attachment no files");
  assert(hermes["runtime_attachment_contract_persists_audit"] === false, "hermes runtime attachment no persist");
  assert(hermes["runtime_attachment_contract_contains_raw_prompt"] === false, "hermes runtime attachment no raw prompt");
  assert(hermes["runtime_attachment_contract_contains_raw_artifacts"] === false, "hermes runtime attachment no raw artifacts");
  assert(hermes["runtime_attachment_contract_contains_secrets"] === false, "hermes runtime attachment no secrets");
  assert(hermes["runtime_attachment_contract_requires_flag"] === "SDLC_HERMES_RUNTIME_ATTACHMENT=enabled", "hermes runtime attachment flag");
  assert(hermesEvidence.includes("execution/hermes-runtime-attachment-contract.ts"), "hermes evidence runtime attachment");
  assert(hermesEvidence.includes("tests/hermes-runtime-attachment-contract.test.ts"), "hermes evidence runtime attachment test");
  assert(hermes["runtime_shadow_attachment"] === "implemented_feature_flagged_helper", "hermes runtime shadow helper");
  assert(hermes["runtime_shadow_attachment_default_enabled"] === false, "hermes runtime shadow default disabled");
  assert(hermes["runtime_shadow_attachment_wired_to_runtime"] === false, "hermes runtime shadow not wired");
  assert(hermes["runtime_shadow_attachment_requires_flag"] === "SDLC_HERMES_RUNTIME_ATTACHMENT=enabled", "hermes runtime shadow flag");
  assert(hermes["runtime_shadow_attachment_invokes_sidecar_when_enabled"] === true, "hermes runtime shadow invokes sidecar");
  assert(hermes["runtime_shadow_attachment_uses_fake_runner_in_tests"] === true, "hermes runtime shadow fake runner");
  assert(hermes["runtime_shadow_attachment_changes_final_status"] === false, "hermes runtime shadow no final status");
  assert(hermes["runtime_shadow_attachment_changes_routing"] === false, "hermes runtime shadow no routing");
  assert(hermes["runtime_shadow_attachment_affects_primary_gateway_result"] === false, "hermes runtime shadow no primary");
  assert(hermes["runtime_shadow_attachment_writes_files"] === false, "hermes runtime shadow no files");
  assert(hermes["runtime_shadow_attachment_persists_audit"] === false, "hermes runtime shadow no persist");
  assert(hermes["runtime_shadow_attachment_contains_raw_prompt"] === false, "hermes runtime shadow no raw prompt");
  assert(hermes["runtime_shadow_attachment_contains_raw_artifacts"] === false, "hermes runtime shadow no raw artifacts");
  assert(hermes["runtime_shadow_attachment_contains_secrets"] === false, "hermes runtime shadow no secrets");
  assert(hermesEvidence.includes("core/hermes-runtime-shadow-attachment.ts"), "hermes evidence runtime shadow");
  assert(hermesEvidence.includes("tests/hermes-runtime-shadow-attachment.test.ts"), "hermes evidence runtime shadow test");
  assert(hermes["runtime_shadow_attachment_wiring_contract"] === "implemented_contract_only", "hermes wiring contract");
  assert(hermes["runtime_shadow_attachment_wiring_contract_default_enabled"] === false, "hermes wiring default disabled");
  assert(hermes["runtime_shadow_attachment_wiring_contract_wired_to_runtime"] === false, "hermes wiring not wired");
  assert(hermes["runtime_shadow_attachment_wiring_contract_field"] === "hermes_runtime_shadow_attachment", "hermes wiring field");
  assert(hermes["runtime_shadow_attachment_wiring_contract_conditional_field_only"] === true, "hermes wiring conditional");
  assert(hermes["runtime_shadow_attachment_wiring_contract_omit_when_disabled"] === true, "hermes wiring omit");
  assert(hermes["runtime_shadow_attachment_wiring_contract_never_undefined_key"] === true, "hermes wiring no undefined");
  assert(hermes["runtime_shadow_attachment_wiring_contract_changes_runtime_result_shape_now"] === false, "hermes wiring no shape now");
  assert(hermes["runtime_shadow_attachment_wiring_contract_changes_final_status"] === false, "hermes wiring no final status");
  assert(hermes["runtime_shadow_attachment_wiring_contract_changes_routing"] === false, "hermes wiring no routing");
  assert(hermes["runtime_shadow_attachment_wiring_contract_affects_primary_gateway_result"] === false, "hermes wiring no primary");
  assert(hermes["runtime_shadow_attachment_wiring_contract_writes_files"] === false, "hermes wiring no files");
  assert(hermes["runtime_shadow_attachment_wiring_contract_persists_audit"] === false, "hermes wiring no persist");
  assert(hermesEvidence.includes("core/hermes-runtime-shadow-attachment-wiring-contract.ts"), "hermes evidence wiring contract");
  assert(hermesEvidence.includes("tests/hermes-runtime-shadow-attachment-wiring-contract.test.ts"), "hermes evidence wiring test");
  assert(hermes["runtime_shadow_attachment_runtime_integration"] === "implemented_feature_flagged_runtime_sidecar", "hermes runtime int");
  assert(hermes["runtime_shadow_attachment_runtime_integration_default_enabled"] === false, "hermes runtime int default disabled");
  assert(hermes["runtime_shadow_attachment_runtime_integration_wired_to_runtime"] === true, "hermes runtime int wired");
  assert(hermes["runtime_shadow_attachment_runtime_integration_field"] === "hermes_runtime_shadow_attachment", "hermes runtime int field");
  assert(hermes["runtime_shadow_attachment_runtime_integration_omit_when_disabled"] === true, "hermes runtime int omit");
  assert(hermes["runtime_shadow_attachment_runtime_integration_never_undefined_key"] === true, "hermes runtime int no undefined");
  assert(hermes["runtime_shadow_attachment_runtime_integration_changes_final_status"] === false, "hermes runtime int no final status");
  assert(hermes["runtime_shadow_attachment_runtime_integration_changes_routing"] === false, "hermes runtime int no routing");
  assert(hermes["runtime_shadow_attachment_runtime_integration_affects_primary_gateway_result"] === false, "hermes runtime int no primary");
  assert(hermes["runtime_shadow_attachment_runtime_integration_writes_files"] === false, "hermes runtime int no files");
  assert(hermes["runtime_shadow_attachment_runtime_integration_persists_audit"] === false, "hermes runtime int no persist");
  assert(hermes["runtime_shadow_attachment_runtime_integration_uses_fake_builder_in_tests"] === true, "hermes runtime int fake builder");
  assert(hermes["runtime_shadow_attachment_runtime_integration_requires_flag"] === "SDLC_HERMES_RUNTIME_ATTACHMENT=enabled", "hermes runtime int flag");
  assert(hermesEvidence.includes("runtime.ts"), "hermes evidence runtime");
  assert(hermesEvidence.includes("tests/runtime-hermes-shadow-attachment.test.ts"), "hermes evidence runtime test");
  assert(hermes["runtime_shadow_attachment_audit_metadata"] === "implemented_in_memory_sidecar_metadata", "hermes audit metadata");
  assert(hermes["runtime_shadow_attachment_audit_metadata_persisted"] === false, "hermes audit no persist");
  assert(hermes["runtime_shadow_attachment_audit_metadata_writes_files"] === false, "hermes audit no files");
  assert(hermes["runtime_shadow_attachment_audit_metadata_changes_final_status"] === false, "hermes audit no final status");
  assert(hermes["runtime_shadow_attachment_audit_metadata_changes_routing"] === false, "hermes audit no routing");
  assert(hermes["runtime_shadow_attachment_audit_metadata_affects_primary_gateway_result"] === false, "hermes audit no primary");
  assert(hermes["runtime_shadow_attachment_audit_metadata_contains_raw_prompt"] === false, "hermes audit no raw prompt");
  assert(hermes["runtime_shadow_attachment_audit_metadata_contains_raw_artifacts"] === false, "hermes audit no raw artifacts");
  assert(hermes["runtime_shadow_attachment_audit_metadata_contains_secrets"] === false, "hermes audit no secrets");
  assert(hermes["runtime_shadow_attachment_audit_metadata_field"] === "auditMetadata", "hermes audit field");
  assert(hermes["runtime_shadow_attachment_observability_summary"] === "implemented_in_memory_sidecar_metadata", "hermes obs");
  assert(hermes["runtime_shadow_attachment_observability_summary_persisted"] === false, "hermes obs no persist");
  assert(hermes["runtime_shadow_attachment_observability_summary_writes_files"] === false, "hermes obs no files");
  assert(hermes["runtime_shadow_attachment_observability_summary_changes_final_status"] === false, "hermes obs no final status");
  assert(hermes["runtime_shadow_attachment_observability_summary_changes_routing"] === false, "hermes obs no routing");
  assert(hermes["runtime_shadow_attachment_observability_summary_affects_primary_gateway_result"] === false, "hermes obs no primary");
  assert(hermes["runtime_shadow_attachment_observability_summary_contains_raw_prompt"] === false, "hermes obs no raw prompt");
  assert(hermes["runtime_shadow_attachment_observability_summary_contains_raw_artifacts"] === false, "hermes obs no raw artifacts");
  assert(hermes["runtime_shadow_attachment_observability_summary_contains_secrets"] === false, "hermes obs no secrets");
  assert(hermes["runtime_shadow_attachment_observability_summary_field"] === "observabilitySummary", "hermes obs field");
  assert(hermes["runtime_shadow_attachment_final_readiness_review"] === "implemented_review_only", "hermes readiness review");
  assert(hermes["runtime_shadow_attachment_final_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes readiness verdict");
  assert(hermes["runtime_shadow_attachment_final_readiness_runtime_active_by_default"] === false, "hermes readiness not active");
  assert(hermes["runtime_shadow_attachment_final_readiness_wired_to_runtime"] === true, "hermes readiness wired runtime");
  assert(hermes["runtime_shadow_attachment_final_readiness_wired_to_gateway"] === false, "hermes readiness not wired gateway");
  assert(hermes["runtime_shadow_attachment_final_readiness_changes_final_status"] === false, "hermes readiness no final status");
  assert(hermes["runtime_shadow_attachment_final_readiness_changes_routing"] === false, "hermes readiness no routing");
  assert(hermes["runtime_shadow_attachment_final_readiness_affects_primary_gateway_result"] === false, "hermes readiness no primary");
  assert(hermes["runtime_shadow_attachment_final_readiness_persists_audit"] === false, "hermes readiness no persist");
  assert(hermes["runtime_shadow_attachment_final_readiness_writes_files"] === false, "hermes readiness no files");
  assert(hermes["runtime_shadow_attachment_final_readiness_contains_raw_prompt"] === false, "hermes readiness no raw prompt");
  assert(hermes["runtime_shadow_attachment_final_readiness_contains_raw_artifacts"] === false, "hermes readiness no raw artifacts");
  assert(hermes["runtime_shadow_attachment_final_readiness_contains_secrets"] === false, "hermes readiness no secrets");
  assert(hermes["runtime_shadow_attachment_final_readiness_recommended_next_pr"] === "Hermes Gateway Real Dispatch Contract", "hermes readiness next PR");
  assert(hermesEvidence.includes("core/hermes-runtime-shadow-attachment-readiness-review.ts"), "hermes evidence readiness ts");
  assert(hermesEvidence.includes("tests/hermes-runtime-shadow-attachment-readiness-review.test.ts"), "hermes evidence readiness test");
  assert(hermesEvidence.includes("HERMES_RUNTIME_SHADOW_ATTACHMENT_READINESS_REVIEW.md"), "hermes evidence readiness md");
  assert(hermesEvidence.includes("hermes-runtime-shadow-attachment-readiness-review.json"), "hermes evidence readiness json");
  assert(hermes["gateway_real_dispatch_contract"] === "implemented_contract_only", "hermes dispatch contract");
  assert(hermes["gateway_real_dispatch_contract_default_enabled"] === false, "hermes dispatch default disabled");
  assert(hermes["gateway_real_dispatch_contract_wired_to_gateway"] === false, "hermes dispatch not wired gateway");
  assert(hermes["gateway_real_dispatch_contract_wired_to_runtime"] === false, "hermes dispatch not wired runtime");
  const hrdSupported = hermes["gateway_real_dispatch_contract_supported_request_types"] as string[];
  assert(Array.isArray(hrdSupported) && hrdSupported.includes("review"), "hermes dispatch supported");
  const hrdUnsupported = hermes["gateway_real_dispatch_contract_unsupported_request_types"] as string[];
  assert(Array.isArray(hrdUnsupported) && hrdUnsupported.includes("llm_task"), "hermes dispatch unsupported");
  const hrdFlags = hermes["gateway_real_dispatch_contract_requires_flags"] as string[];
  assert(Array.isArray(hrdFlags) && hrdFlags.length === 3, "hermes dispatch 3 flags");
  assert(hermes["gateway_real_dispatch_contract_invokes_cli"] === false, "hermes dispatch no CLI");
  assert(hermes["gateway_real_dispatch_contract_changes_gateway_primary_dispatch_now"] === false, "hermes dispatch no gateway change");
  assert(hermes["gateway_real_dispatch_contract_changes_final_status"] === false, "hermes dispatch no final status");
  assert(hermes["gateway_real_dispatch_contract_changes_routing"] === false, "hermes dispatch no routing");
  assert(hermes["gateway_real_dispatch_contract_writes_files"] === false, "hermes dispatch no files");
  assert(hermes["gateway_real_dispatch_contract_persists_audit"] === false, "hermes dispatch no persist");
  assert(hermes["gateway_real_dispatch_contract_contains_secrets"] === false, "hermes dispatch no secrets");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-contract.ts"), "hermes evidence dispatch contract");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-contract.test.ts"), "hermes evidence dispatch test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_CONTRACT.md"), "hermes evidence dispatch md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-contract.json"), "hermes evidence dispatch json");
  assert(hermes["gateway_real_dispatch"] === "implemented_feature_flagged_helper", "hermes real dispatch");
  assert(hermes["gateway_real_dispatch_default_enabled"] === false, "hermes real dispatch default disabled");
  assert(hermes["gateway_real_dispatch_wired_to_gateway"] === false, "hermes real dispatch not wired gateway");
  assert(hermes["gateway_real_dispatch_invokes_cli_when_eligible"] === true, "hermes real dispatch invokes CLI");
  assert(hermes["gateway_real_dispatch_uses_fake_runner_in_tests"] === true, "hermes real dispatch fake runner");
  assert(hermes["gateway_real_dispatch_changes_final_status"] === false, "hermes real dispatch no final status");
  assert(hermes["gateway_real_dispatch_changes_routing"] === false, "hermes real dispatch no routing");
  assert(hermes["gateway_real_dispatch_affects_primary_gateway_result"] === false, "hermes real dispatch no primary");
  assert(hermes["gateway_real_dispatch_writes_files"] === false, "hermes real dispatch no files");
  assert(hermes["gateway_real_dispatch_contains_secrets"] === false, "hermes real dispatch no secrets");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch.ts"), "hermes evidence real dispatch");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch.test.ts"), "hermes evidence real dispatch test");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract"] === "implemented_contract_only", "hermes dispatch gateway integration contract");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_default_enabled"] === false, "hermes dispatch gateway integration default disabled");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_wired_to_gateway"] === false, "hermes dispatch gateway integration not wired gateway");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_wired_to_runtime"] === false, "hermes dispatch gateway integration not wired runtime");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_field"] === "hermes_gateway_real_dispatch", "hermes dispatch gateway integration field");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_omit_when_disabled"] === true, "hermes dispatch gateway integration omit");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_never_undefined_key"] === true, "hermes dispatch gateway integration no undefined");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_changes_gateway_primary_dispatch_now"] === false, "hermes dispatch gateway integration no primary dispatch");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_changes_gateway_final_result_now"] === false, "hermes dispatch gateway integration no final result");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_changes_final_status"] === false, "hermes dispatch gateway integration no final status");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_changes_routing"] === false, "hermes dispatch gateway integration no routing");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_affects_primary_gateway_result"] === false, "hermes dispatch gateway integration no primary effect");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_writes_files"] === false, "hermes dispatch gateway integration no files");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_persists_audit"] === false, "hermes dispatch gateway integration no persist");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_contains_raw_prompt"] === false, "hermes dispatch gateway integration no raw prompt");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_contains_raw_artifacts"] === false, "hermes dispatch gateway integration no raw artifacts");
  assert(hermes["gateway_real_dispatch_gateway_integration_contract_contains_secrets"] === false, "hermes dispatch gateway integration no secrets");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-gateway-integration-contract.ts"), "hermes evidence dispatch gateway integration contract");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-gateway-integration-contract.test.ts"), "hermes evidence dispatch gateway integration test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_GATEWAY_INTEGRATION_CONTRACT.md"), "hermes evidence dispatch gateway integration md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-gateway-integration-contract.json"), "hermes evidence dispatch gateway integration json");
  assert(hermes["gateway_real_dispatch_gateway_integration"] === "implemented_feature_flagged_sidecar_metadata", "hermes dispatch gateway sidecar integration");
  assert(hermes["gateway_real_dispatch_gateway_integration_default_enabled"] === false, "hermes dispatch gateway sidecar default disabled");
  assert(hermes["gateway_real_dispatch_gateway_integration_wired_to_gateway"] === true, "hermes dispatch gateway sidecar wired gateway");
  assert(hermes["gateway_real_dispatch_gateway_integration_wired_to_runtime"] === false, "hermes dispatch gateway sidecar not wired runtime");
  assert(hermes["gateway_real_dispatch_gateway_integration_field"] === "hermes_gateway_real_dispatch", "hermes dispatch gateway sidecar field");
  assert(hermes["gateway_real_dispatch_gateway_integration_omit_when_disabled"] === true, "hermes dispatch gateway sidecar omit");
  assert(hermes["gateway_real_dispatch_gateway_integration_never_undefined_key"] === true, "hermes dispatch gateway sidecar no undefined");
  const hgSidecarSupported = hermes["gateway_real_dispatch_gateway_integration_supported_request_types"] as string[];
  assert(Array.isArray(hgSidecarSupported) && hgSidecarSupported.includes("review") && hgSidecarSupported.includes("code_review") && hgSidecarSupported.includes("validation"), "hermes dispatch gateway sidecar supported");
  const hgSidecarUnsupported = hermes["gateway_real_dispatch_gateway_integration_unsupported_request_types"] as string[];
  assert(Array.isArray(hgSidecarUnsupported) && hgSidecarUnsupported.includes("llm_task") && hgSidecarUnsupported.includes("code_generation") && hgSidecarUnsupported.includes("bugfix"), "hermes dispatch gateway sidecar unsupported");
  assert(hermes["gateway_real_dispatch_gateway_integration_changes_gateway_primary_dispatch"] === false, "hermes dispatch gateway sidecar no primary dispatch");
  assert(hermes["gateway_real_dispatch_gateway_integration_changes_gateway_final_result"] === false, "hermes dispatch gateway sidecar no final result");
  assert(hermes["gateway_real_dispatch_gateway_integration_changes_final_status"] === false, "hermes dispatch gateway sidecar no final status");
  assert(hermes["gateway_real_dispatch_gateway_integration_changes_routing"] === false, "hermes dispatch gateway sidecar no routing");
  assert(hermes["gateway_real_dispatch_gateway_integration_affects_primary_gateway_result"] === false, "hermes dispatch gateway sidecar no primary effect");
  assert(hermes["gateway_real_dispatch_gateway_integration_writes_files"] === false, "hermes dispatch gateway sidecar no files");
  assert(hermes["gateway_real_dispatch_gateway_integration_persists_audit"] === false, "hermes dispatch gateway sidecar no persist");
  assert(hermes["gateway_real_dispatch_gateway_integration_contains_raw_prompt"] === false, "hermes dispatch gateway sidecar no raw prompt");
  assert(hermes["gateway_real_dispatch_gateway_integration_contains_raw_artifacts"] === false, "hermes dispatch gateway sidecar no raw artifacts");
  assert(hermes["gateway_real_dispatch_gateway_integration_contains_secrets"] === false, "hermes dispatch gateway sidecar no secrets");
  assert(hermes["gateway_real_dispatch_gateway_integration_uses_fake_dispatcher_in_tests"] === true, "hermes dispatch gateway sidecar fake dispatcher");
  assert(hermesEvidence.includes("execution/gateway.ts"), "hermes evidence Gateway sidecar");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-gateway-integration.test.ts"), "hermes evidence Gateway sidecar test");
  assert(hermes["gateway_real_dispatch_fallback_policy"] === "implemented_sidecar_metadata_policy", "hermes dispatch fallback policy");
  assert(hermes["gateway_real_dispatch_fallback_policy_default_enabled"] === false, "hermes dispatch fallback default disabled");
  assert(hermes["gateway_real_dispatch_fallback_policy_wired_to_gateway"] === true, "hermes dispatch fallback wired gateway");
  assert(hermes["gateway_real_dispatch_fallback_policy_wired_to_runtime"] === false, "hermes dispatch fallback not wired runtime");
  assert(hermes["gateway_real_dispatch_fallback_policy_field"] === "fallbackPolicy", "hermes dispatch fallback field");
  assert(hermes["gateway_real_dispatch_fallback_policy_parent_field"] === "hermes_gateway_real_dispatch", "hermes dispatch fallback parent field");
  const hgFallbackSupported = hermes["gateway_real_dispatch_fallback_policy_supported_request_types"] as string[];
  assert(Array.isArray(hgFallbackSupported) && hgFallbackSupported.includes("review") && hgFallbackSupported.includes("code_review") && hgFallbackSupported.includes("validation"), "hermes dispatch fallback supported");
  const hgFallbackUnsupported = hermes["gateway_real_dispatch_fallback_policy_unsupported_request_types"] as string[];
  assert(Array.isArray(hgFallbackUnsupported) && hgFallbackUnsupported.includes("llm_task") && hgFallbackUnsupported.includes("code_generation") && hgFallbackUnsupported.includes("bugfix"), "hermes dispatch fallback unsupported");
  const hgFallbackReasons = hermes["gateway_real_dispatch_fallback_policy_reasons"] as string[];
  assert(Array.isArray(hgFallbackReasons) && hgFallbackReasons.includes("dispatch_success") && hgFallbackReasons.includes("dispatch_guarded_fallback") && hgFallbackReasons.includes("dispatcher_exception"), "hermes dispatch fallback reasons");
  assert(hermes["gateway_real_dispatch_fallback_policy_attach_success"] === true, "hermes dispatch fallback attach success");
  assert(hermes["gateway_real_dispatch_fallback_policy_attach_safe_failures"] === true, "hermes dispatch fallback attach failures");
  assert(hermes["gateway_real_dispatch_fallback_policy_omit_disabled"] === true, "hermes dispatch fallback omit disabled");
  assert(hermes["gateway_real_dispatch_fallback_policy_omit_unsupported"] === true, "hermes dispatch fallback omit unsupported");
  assert(hermes["gateway_real_dispatch_fallback_policy_omit_unsafe"] === true, "hermes dispatch fallback omit unsafe");
  assert(hermes["gateway_real_dispatch_fallback_policy_omit_dispatcher_exception"] === true, "hermes dispatch fallback omit exception");
  assert(hermes["gateway_real_dispatch_fallback_policy_changes_gateway_primary_dispatch"] === false, "hermes dispatch fallback no primary dispatch");
  assert(hermes["gateway_real_dispatch_fallback_policy_changes_gateway_final_result"] === false, "hermes dispatch fallback no final result");
  assert(hermes["gateway_real_dispatch_fallback_policy_changes_final_status"] === false, "hermes dispatch fallback no final status");
  assert(hermes["gateway_real_dispatch_fallback_policy_changes_routing"] === false, "hermes dispatch fallback no routing");
  assert(hermes["gateway_real_dispatch_fallback_policy_affects_primary_gateway_result"] === false, "hermes dispatch fallback no primary effect");
  assert(hermes["gateway_real_dispatch_fallback_policy_writes_files"] === false, "hermes dispatch fallback no files");
  assert(hermes["gateway_real_dispatch_fallback_policy_persists_audit"] === false, "hermes dispatch fallback no persist");
  assert(hermes["gateway_real_dispatch_fallback_policy_contains_raw_prompt"] === false, "hermes dispatch fallback no raw prompt");
  assert(hermes["gateway_real_dispatch_fallback_policy_contains_raw_artifacts"] === false, "hermes dispatch fallback no raw artifacts");
  assert(hermes["gateway_real_dispatch_fallback_policy_contains_secrets"] === false, "hermes dispatch fallback no secrets");
  assert(hermes["gateway_real_dispatch_fallback_policy_uses_fake_dispatcher_in_tests"] === true, "hermes dispatch fallback fake dispatcher");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-fallback-policy.ts"), "hermes evidence fallback policy");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-fallback-policy.test.ts"), "hermes evidence fallback policy test");
  assert(hermes["gateway_real_dispatch_observability"] === "implemented_sidecar_metadata", "hermes dispatch observability");
  assert(hermes["gateway_real_dispatch_observability_default_enabled"] === false, "hermes dispatch observability default disabled");
  assert(hermes["gateway_real_dispatch_observability_wired_to_gateway"] === true, "hermes dispatch observability wired gateway");
  assert(hermes["gateway_real_dispatch_observability_wired_to_runtime"] === false, "hermes dispatch observability not wired runtime");
  assert(hermes["gateway_real_dispatch_observability_field"] === "observability", "hermes dispatch observability field");
  assert(hermes["gateway_real_dispatch_observability_top_level_field"] === false, "hermes dispatch observability no top-level field");
  assert(hermes["gateway_real_dispatch_observability_preserves_gateway_primary_result"] === true, "hermes dispatch observability preserves primary");
  assert(hermes["gateway_real_dispatch_observability_preserves_gateway_final_result"] === true, "hermes dispatch observability preserves gateway final");
  assert(hermes["gateway_real_dispatch_observability_preserves_runtime_final_status"] === true, "hermes dispatch observability preserves runtime final status");
  assert(hermes["gateway_real_dispatch_observability_preserves_runtime_routing"] === true, "hermes dispatch observability preserves runtime routing");
  assert(hermes["gateway_real_dispatch_observability_changes_gateway_primary_dispatch"] === false, "hermes dispatch observability no primary dispatch");
  assert(hermes["gateway_real_dispatch_observability_changes_gateway_final_result"] === false, "hermes dispatch observability no gateway final result");
  assert(hermes["gateway_real_dispatch_observability_changes_final_status"] === false, "hermes dispatch observability no final status");
  assert(hermes["gateway_real_dispatch_observability_changes_routing"] === false, "hermes dispatch observability no routing");
  assert(hermes["gateway_real_dispatch_observability_affects_primary_gateway_result"] === false, "hermes dispatch observability no primary effect");
  assert(hermes["gateway_real_dispatch_observability_writes_files"] === false, "hermes dispatch observability no files");
  assert(hermes["gateway_real_dispatch_observability_persists_observability"] === false, "hermes dispatch observability no obs persist");
  assert(hermes["gateway_real_dispatch_observability_persists_audit"] === false, "hermes dispatch observability no audit persist");
  assert(hermes["gateway_real_dispatch_observability_contains_raw_prompt"] === false, "hermes dispatch observability no raw prompt");
  assert(hermes["gateway_real_dispatch_observability_contains_raw_artifacts"] === false, "hermes dispatch observability no raw artifacts");
  assert(hermes["gateway_real_dispatch_observability_contains_secrets"] === false, "hermes dispatch observability no secrets");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-observability.ts"), "hermes evidence observability");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-observability.test.ts"), "hermes evidence observability test");
  assert(hermes["gateway_real_dispatch_guardrails"] === "implemented_sidecar_metadata_guardrails", "hermes dispatch guardrails");
  assert(hermes["gateway_real_dispatch_guardrails_default_enabled"] === false, "hermes dispatch guardrails default disabled");
  assert(hermes["gateway_real_dispatch_guardrails_wired_to_gateway"] === true, "hermes dispatch guardrails wired gateway");
  assert(hermes["gateway_real_dispatch_guardrails_wired_to_runtime"] === false, "hermes dispatch guardrails not wired runtime");
  assert(hermes["gateway_real_dispatch_guardrails_field"] === "guardrails", "hermes dispatch guardrails field");
  assert(hermes["gateway_real_dispatch_guardrails_top_level_field"] === false, "hermes dispatch guardrails no top-level field");
  assert(hermes["gateway_real_dispatch_guardrails_max_warnings_default"] === 20, "hermes dispatch guardrails max warnings");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_unsupported_request_type"] === true, "hermes dispatch guardrails reject unsupported");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_unexpected_status"] === true, "hermes dispatch guardrails reject status");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_warning_limit_exceeded"] === true, "hermes dispatch guardrails reject warnings");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_missing_fallback_policy"] === true, "hermes dispatch guardrails reject missing fallback");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_missing_observability"] === true, "hermes dispatch guardrails reject missing observability");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_raw_prompt_risk"] === true, "hermes dispatch guardrails reject raw prompt");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_raw_artifact_risk"] === true, "hermes dispatch guardrails reject raw artifacts");
  assert(hermes["gateway_real_dispatch_guardrails_rejects_secret_risk"] === true, "hermes dispatch guardrails reject secrets");
  assert(hermes["gateway_real_dispatch_guardrails_preserves_gateway_primary_result"] === true, "hermes dispatch guardrails preserves primary");
  assert(hermes["gateway_real_dispatch_guardrails_preserves_gateway_final_result"] === true, "hermes dispatch guardrails preserves gateway final");
  assert(hermes["gateway_real_dispatch_guardrails_preserves_runtime_final_status"] === true, "hermes dispatch guardrails preserves runtime final status");
  assert(hermes["gateway_real_dispatch_guardrails_preserves_runtime_routing"] === true, "hermes dispatch guardrails preserves runtime routing");
  assert(hermes["gateway_real_dispatch_guardrails_changes_gateway_primary_dispatch"] === false, "hermes dispatch guardrails no primary dispatch");
  assert(hermes["gateway_real_dispatch_guardrails_changes_gateway_final_result"] === false, "hermes dispatch guardrails no gateway final result");
  assert(hermes["gateway_real_dispatch_guardrails_changes_final_status"] === false, "hermes dispatch guardrails no final status");
  assert(hermes["gateway_real_dispatch_guardrails_changes_routing"] === false, "hermes dispatch guardrails no routing");
  assert(hermes["gateway_real_dispatch_guardrails_affects_primary_gateway_result"] === false, "hermes dispatch guardrails no primary effect");
  assert(hermes["gateway_real_dispatch_guardrails_writes_files"] === false, "hermes dispatch guardrails no files");
  assert(hermes["gateway_real_dispatch_guardrails_persists_guardrails"] === false, "hermes dispatch guardrails no guardrail persist");
  assert(hermes["gateway_real_dispatch_guardrails_persists_observability"] === false, "hermes dispatch guardrails no obs persist");
  assert(hermes["gateway_real_dispatch_guardrails_persists_audit"] === false, "hermes dispatch guardrails no audit persist");
  assert(hermes["gateway_real_dispatch_guardrails_contains_raw_prompt"] === false, "hermes dispatch guardrails no raw prompt");
  assert(hermes["gateway_real_dispatch_guardrails_contains_raw_artifacts"] === false, "hermes dispatch guardrails no raw artifacts");
  assert(hermes["gateway_real_dispatch_guardrails_contains_secrets"] === false, "hermes dispatch guardrails no secrets");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-guardrails.ts"), "hermes evidence guardrails");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-guardrails.test.ts"), "hermes evidence guardrails test");
  assert(hermes["gateway_real_dispatch_final_readiness_review"] === "implemented_review_only", "hermes dispatch readiness review");
  assert(hermes["gateway_real_dispatch_final_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch readiness verdict");
  assert(hermes["gateway_real_dispatch_final_readiness_runtime_active_by_default"] === false, "hermes dispatch readiness runtime inactive");
  assert(hermes["gateway_real_dispatch_final_readiness_gateway_active_by_default"] === false, "hermes dispatch readiness gateway inactive");
  assert(hermes["gateway_real_dispatch_final_readiness_wired_to_gateway"] === true, "hermes dispatch readiness wired gateway");
  assert(hermes["gateway_real_dispatch_final_readiness_wired_to_runtime"] === false, "hermes dispatch readiness not wired runtime");
  assert(hermes["gateway_real_dispatch_final_readiness_field"] === "hermes_gateway_real_dispatch", "hermes dispatch readiness field");
  assert(hermes["gateway_real_dispatch_final_readiness_fallback_policy_field"] === "fallbackPolicy", "hermes dispatch readiness fallback");
  assert(hermes["gateway_real_dispatch_final_readiness_observability_field"] === "observability", "hermes dispatch readiness observability");
  assert(hermes["gateway_real_dispatch_final_readiness_guardrails_field"] === "guardrails", "hermes dispatch readiness guardrails");
  assert(hermes["gateway_real_dispatch_final_readiness_default_disabled"] === true, "hermes dispatch readiness default disabled");
  assert(hermes["gateway_real_dispatch_final_readiness_feature_flagged"] === true, "hermes dispatch readiness feature flagged");
  assert(hermes["gateway_real_dispatch_final_readiness_requires_multiple_flags"] === true, "hermes dispatch readiness multiple flags");
  const hReadySupported = hermes["gateway_real_dispatch_final_readiness_supported_request_types"] as string[];
  assert(Array.isArray(hReadySupported) && hReadySupported.includes("review") && hReadySupported.includes("code_review") && hReadySupported.includes("validation"), "hermes dispatch readiness supported");
  const hReadyUnsupported = hermes["gateway_real_dispatch_final_readiness_unsupported_request_types"] as string[];
  assert(Array.isArray(hReadyUnsupported) && hReadyUnsupported.includes("llm_task") && hReadyUnsupported.includes("code_generation") && hReadyUnsupported.includes("bugfix"), "hermes dispatch readiness unsupported");
  assert(hermes["gateway_real_dispatch_final_readiness_changes_gateway_primary_dispatch"] === false, "hermes dispatch readiness no primary dispatch");
  assert(hermes["gateway_real_dispatch_final_readiness_changes_gateway_final_result"] === false, "hermes dispatch readiness no gateway final");
  assert(hermes["gateway_real_dispatch_final_readiness_changes_final_status"] === false, "hermes dispatch readiness no final status");
  assert(hermes["gateway_real_dispatch_final_readiness_changes_routing"] === false, "hermes dispatch readiness no routing");
  assert(hermes["gateway_real_dispatch_final_readiness_affects_primary_gateway_result"] === false, "hermes dispatch readiness no primary effect");
  assert(hermes["gateway_real_dispatch_final_readiness_makes_hermes_default"] === false, "hermes dispatch readiness not default");
  assert(hermes["gateway_real_dispatch_final_readiness_makes_hermes_final_review_owner"] === false, "hermes dispatch readiness not review owner");
  assert(hermes["gateway_real_dispatch_final_readiness_makes_hermes_final_validation_owner"] === false, "hermes dispatch readiness not validation owner");
  assert(hermes["gateway_real_dispatch_final_readiness_writes_files"] === false, "hermes dispatch readiness no files");
  assert(hermes["gateway_real_dispatch_final_readiness_persists_audit"] === false, "hermes dispatch readiness no audit persist");
  assert(hermes["gateway_real_dispatch_final_readiness_persists_observability"] === false, "hermes dispatch readiness no observability persist");
  assert(hermes["gateway_real_dispatch_final_readiness_persists_guardrails"] === false, "hermes dispatch readiness no guardrail persist");
  assert(hermes["gateway_real_dispatch_final_readiness_contains_raw_prompt"] === false, "hermes dispatch readiness no raw prompt");
  assert(hermes["gateway_real_dispatch_final_readiness_contains_raw_artifacts"] === false, "hermes dispatch readiness no raw artifacts");
  assert(hermes["gateway_real_dispatch_final_readiness_contains_secrets"] === false, "hermes dispatch readiness no secrets");
  assert(hermes["gateway_real_dispatch_final_readiness_recommended_next_pr"] === "Hermes Gateway Real Dispatch Controlled Rollout Plan", "hermes dispatch readiness next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-readiness-review.ts"), "hermes evidence readiness module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-readiness-review.test.ts"), "hermes evidence readiness test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_READINESS_REVIEW.md"), "hermes evidence readiness md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-readiness-review.json"), "hermes evidence readiness json");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan"] === "implemented_plan_only", "hermes dispatch rollout plan");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_status"] === "plan_only", "hermes dispatch rollout status");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_executing_now"] === false, "hermes dispatch rollout not executing");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_enables_feature_flags_now"] === false, "hermes dispatch rollout no flags");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_changes_runtime_behavior_now"] === false, "hermes dispatch rollout no runtime behavior");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_changes_gateway_behavior_now"] === false, "hermes dispatch rollout no gateway behavior");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_readiness_verdict_required"] === "READY_WITH_CONSTRAINTS", "hermes dispatch rollout required verdict");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch rollout current verdict");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_default_disabled"] === true, "hermes dispatch rollout default disabled");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_feature_flagged"] === true, "hermes dispatch rollout feature flagged");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_requires_multiple_flags"] === true, "hermes dispatch rollout multiple flags");
  const hRolloutInitial = hermes["gateway_real_dispatch_controlled_rollout_plan_initial_request_types"] as string[];
  assert(Array.isArray(hRolloutInitial) && hRolloutInitial.length === 1 && hRolloutInitial[0] === "review", "hermes dispatch rollout initial review");
  const hRolloutSupported = hermes["gateway_real_dispatch_controlled_rollout_plan_supported_request_types"] as string[];
  assert(Array.isArray(hRolloutSupported) && hRolloutSupported.includes("review") && hRolloutSupported.includes("code_review") && hRolloutSupported.includes("validation"), "hermes dispatch rollout supported");
  const hRolloutUnsupported = hermes["gateway_real_dispatch_controlled_rollout_plan_unsupported_request_types"] as string[];
  assert(Array.isArray(hRolloutUnsupported) && hRolloutUnsupported.includes("llm_task") && hRolloutUnsupported.includes("code_generation") && hRolloutUnsupported.includes("bugfix"), "hermes dispatch rollout unsupported");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_changes_gateway_primary_dispatch"] === false, "hermes dispatch rollout no primary dispatch");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_changes_gateway_final_result"] === false, "hermes dispatch rollout no gateway final");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_changes_final_status"] === false, "hermes dispatch rollout no final status");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_changes_routing"] === false, "hermes dispatch rollout no routing");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_affects_primary_gateway_result"] === false, "hermes dispatch rollout no primary effect");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_makes_hermes_default"] === false, "hermes dispatch rollout not default");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_makes_hermes_final_review_owner"] === false, "hermes dispatch rollout not review owner");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_makes_hermes_final_validation_owner"] === false, "hermes dispatch rollout not validation owner");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_writes_files"] === false, "hermes dispatch rollout no files");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_persists_rollout_logs"] === false, "hermes dispatch rollout no rollout logs");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_persists_audit"] === false, "hermes dispatch rollout no audit persist");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_persists_observability"] === false, "hermes dispatch rollout no observability persist");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_persists_guardrails"] === false, "hermes dispatch rollout no guardrail persist");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_contains_raw_prompt"] === false, "hermes dispatch rollout no raw prompt");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_contains_raw_artifacts"] === false, "hermes dispatch rollout no raw artifacts");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_contains_secrets"] === false, "hermes dispatch rollout no secrets");
  assert(hermes["gateway_real_dispatch_controlled_rollout_plan_recommended_next_pr"] === "Hermes Gateway Real Dispatch Rollout Validation Checklist", "hermes dispatch rollout next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-controlled-rollout-plan.ts"), "hermes evidence rollout module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-controlled-rollout-plan.test.ts"), "hermes evidence rollout test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_CONTROLLED_ROLLOUT_PLAN.md"), "hermes evidence rollout md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-controlled-rollout-plan.json"), "hermes evidence rollout json");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist"] === "implemented_checklist_only", "hermes dispatch validation checklist");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_status"] === "checklist_only", "hermes dispatch validation status");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_executing_now"] === false, "hermes dispatch validation not executing");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_enables_feature_flags_now"] === false, "hermes dispatch validation no flags");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_changes_runtime_behavior_now"] === false, "hermes dispatch validation no runtime behavior");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_changes_gateway_behavior_now"] === false, "hermes dispatch validation no gateway behavior");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_readiness_verdict_required"] === "READY_WITH_CONSTRAINTS", "hermes dispatch validation required verdict");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_rollout_plan_status_required"] === "plan_only", "hermes dispatch validation required rollout plan");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch validation current verdict");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_current_rollout_plan_status"] === "plan_only", "hermes dispatch validation current rollout plan");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_default_disabled"] === true, "hermes dispatch validation default disabled");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_feature_flagged"] === true, "hermes dispatch validation feature flagged");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_requires_multiple_flags"] === true, "hermes dispatch validation multiple flags");
  const hChecklistInitial = hermes["gateway_real_dispatch_rollout_validation_checklist_initial_request_types"] as string[];
  assert(Array.isArray(hChecklistInitial) && hChecklistInitial.length === 1 && hChecklistInitial[0] === "review", "hermes dispatch validation initial review");
  const hChecklistSupported = hermes["gateway_real_dispatch_rollout_validation_checklist_supported_request_types"] as string[];
  assert(Array.isArray(hChecklistSupported) && hChecklistSupported.includes("review") && hChecklistSupported.includes("code_review") && hChecklistSupported.includes("validation"), "hermes dispatch validation supported");
  const hChecklistUnsupported = hermes["gateway_real_dispatch_rollout_validation_checklist_unsupported_request_types"] as string[];
  assert(Array.isArray(hChecklistUnsupported) && hChecklistUnsupported.includes("llm_task") && hChecklistUnsupported.includes("code_generation") && hChecklistUnsupported.includes("bugfix"), "hermes dispatch validation unsupported");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_changes_gateway_primary_dispatch"] === false, "hermes dispatch validation no primary dispatch");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_changes_gateway_final_result"] === false, "hermes dispatch validation no gateway final");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_changes_final_status"] === false, "hermes dispatch validation no final status");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_changes_routing"] === false, "hermes dispatch validation no routing");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_affects_primary_gateway_result"] === false, "hermes dispatch validation no primary effect");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_makes_hermes_default"] === false, "hermes dispatch validation not default");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_makes_hermes_final_review_owner"] === false, "hermes dispatch validation not review owner");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_makes_hermes_final_validation_owner"] === false, "hermes dispatch validation not validation owner");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_writes_files"] === false, "hermes dispatch validation no files");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_persists_validation_logs"] === false, "hermes dispatch validation no validation logs");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_persists_rollout_logs"] === false, "hermes dispatch validation no rollout logs");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_persists_audit"] === false, "hermes dispatch validation no audit persist");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_persists_observability"] === false, "hermes dispatch validation no observability persist");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_persists_guardrails"] === false, "hermes dispatch validation no guardrail persist");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_contains_raw_prompt"] === false, "hermes dispatch validation no raw prompt");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_contains_raw_artifacts"] === false, "hermes dispatch validation no raw artifacts");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_contains_secrets"] === false, "hermes dispatch validation no secrets");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_operator_approval_required"] === true, "hermes dispatch validation operator approval");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_rollout_may_proceed_automatically"] === false, "hermes dispatch validation no automatic rollout");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_automatic_enablement_allowed"] === false, "hermes dispatch validation no automatic enablement");
  assert(hermes["gateway_real_dispatch_rollout_validation_checklist_recommended_next_pr"] === "Hermes Gateway Real Dispatch Operator Runbook", "hermes dispatch validation next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-rollout-validation-checklist.ts"), "hermes evidence validation checklist module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-rollout-validation-checklist.test.ts"), "hermes evidence validation checklist test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_ROLLOUT_VALIDATION_CHECKLIST.md"), "hermes evidence validation checklist md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-rollout-validation-checklist.json"), "hermes evidence validation checklist json");
  assert(hermes["gateway_real_dispatch_operator_runbook"] === "implemented_runbook_only", "hermes dispatch operator runbook");
  assert(hermes["gateway_real_dispatch_operator_runbook_status"] === "runbook_only", "hermes dispatch operator status");
  assert(hermes["gateway_real_dispatch_operator_runbook_executing_now"] === false, "hermes dispatch operator not executing");
  assert(hermes["gateway_real_dispatch_operator_runbook_enables_feature_flags_now"] === false, "hermes dispatch operator no flags");
  assert(hermes["gateway_real_dispatch_operator_runbook_changes_runtime_behavior_now"] === false, "hermes dispatch operator no runtime behavior");
  assert(hermes["gateway_real_dispatch_operator_runbook_changes_gateway_behavior_now"] === false, "hermes dispatch operator no gateway behavior");
  assert(hermes["gateway_real_dispatch_operator_runbook_adds_enablement_scripts"] === false, "hermes dispatch operator no scripts");
  assert(hermes["gateway_real_dispatch_operator_runbook_changes_ci_behavior"] === false, "hermes dispatch operator no CI");
  assert(hermes["gateway_real_dispatch_operator_runbook_readiness_verdict_required"] === "READY_WITH_CONSTRAINTS", "hermes dispatch operator required verdict");
  assert(hermes["gateway_real_dispatch_operator_runbook_rollout_plan_status_required"] === "plan_only", "hermes dispatch operator required rollout plan");
  assert(hermes["gateway_real_dispatch_operator_runbook_rollout_validation_checklist_status_required"] === "checklist_only", "hermes dispatch operator required checklist");
  assert(hermes["gateway_real_dispatch_operator_runbook_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch operator current verdict");
  assert(hermes["gateway_real_dispatch_operator_runbook_current_rollout_plan_status"] === "plan_only", "hermes dispatch operator current rollout plan");
  assert(hermes["gateway_real_dispatch_operator_runbook_current_rollout_validation_checklist_status"] === "checklist_only", "hermes dispatch operator current checklist");
  assert(hermes["gateway_real_dispatch_operator_runbook_default_disabled"] === true, "hermes dispatch operator default disabled");
  assert(hermes["gateway_real_dispatch_operator_runbook_feature_flagged"] === true, "hermes dispatch operator feature flagged");
  assert(hermes["gateway_real_dispatch_operator_runbook_requires_multiple_flags"] === true, "hermes dispatch operator multiple flags");
  const hRunbookInitial = hermes["gateway_real_dispatch_operator_runbook_initial_request_types"] as string[];
  assert(Array.isArray(hRunbookInitial) && hRunbookInitial.length === 1 && hRunbookInitial[0] === "review", "hermes dispatch operator initial review");
  const hRunbookSupported = hermes["gateway_real_dispatch_operator_runbook_supported_request_types"] as string[];
  assert(Array.isArray(hRunbookSupported) && hRunbookSupported.includes("review") && hRunbookSupported.includes("code_review") && hRunbookSupported.includes("validation"), "hermes dispatch operator supported");
  const hRunbookUnsupported = hermes["gateway_real_dispatch_operator_runbook_unsupported_request_types"] as string[];
  assert(Array.isArray(hRunbookUnsupported) && hRunbookUnsupported.includes("llm_task") && hRunbookUnsupported.includes("code_generation") && hRunbookUnsupported.includes("bugfix"), "hermes dispatch operator unsupported");
  assert(hermes["gateway_real_dispatch_operator_runbook_operator_approval_required"] === true, "hermes dispatch operator approval");
  assert(hermes["gateway_real_dispatch_operator_runbook_rollout_may_proceed_automatically"] === false, "hermes dispatch operator no automatic rollout");
  assert(hermes["gateway_real_dispatch_operator_runbook_automatic_enablement_allowed"] === false, "hermes dispatch operator no automatic enablement");
  assert(hermes["gateway_real_dispatch_operator_runbook_changes_gateway_primary_dispatch"] === false, "hermes dispatch operator no primary dispatch");
  assert(hermes["gateway_real_dispatch_operator_runbook_changes_gateway_final_result"] === false, "hermes dispatch operator no gateway final");
  assert(hermes["gateway_real_dispatch_operator_runbook_changes_final_status"] === false, "hermes dispatch operator no final status");
  assert(hermes["gateway_real_dispatch_operator_runbook_changes_routing"] === false, "hermes dispatch operator no routing");
  assert(hermes["gateway_real_dispatch_operator_runbook_affects_primary_gateway_result"] === false, "hermes dispatch operator no primary effect");
  assert(hermes["gateway_real_dispatch_operator_runbook_makes_hermes_default"] === false, "hermes dispatch operator not default");
  assert(hermes["gateway_real_dispatch_operator_runbook_makes_hermes_final_review_owner"] === false, "hermes dispatch operator not review owner");
  assert(hermes["gateway_real_dispatch_operator_runbook_makes_hermes_final_validation_owner"] === false, "hermes dispatch operator not validation owner");
  assert(hermes["gateway_real_dispatch_operator_runbook_writes_files"] === false, "hermes dispatch operator no files");
  assert(hermes["gateway_real_dispatch_operator_runbook_persists_runbook_logs"] === false, "hermes dispatch operator no runbook logs");
  assert(hermes["gateway_real_dispatch_operator_runbook_persists_validation_logs"] === false, "hermes dispatch operator no validation logs");
  assert(hermes["gateway_real_dispatch_operator_runbook_persists_rollout_logs"] === false, "hermes dispatch operator no rollout logs");
  assert(hermes["gateway_real_dispatch_operator_runbook_persists_audit"] === false, "hermes dispatch operator no audit persist");
  assert(hermes["gateway_real_dispatch_operator_runbook_persists_observability"] === false, "hermes dispatch operator no observability persist");
  assert(hermes["gateway_real_dispatch_operator_runbook_persists_guardrails"] === false, "hermes dispatch operator no guardrail persist");
  assert(hermes["gateway_real_dispatch_operator_runbook_contains_raw_prompt"] === false, "hermes dispatch operator no raw prompt");
  assert(hermes["gateway_real_dispatch_operator_runbook_contains_raw_artifacts"] === false, "hermes dispatch operator no raw artifacts");
  assert(hermes["gateway_real_dispatch_operator_runbook_contains_secrets"] === false, "hermes dispatch operator no secrets");
  assert(hermes["gateway_real_dispatch_operator_runbook_recommended_next_pr"] === "Hermes Gateway Real Dispatch Post-Enablement Review Template", "hermes dispatch operator next PR");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template"] === "implemented_template_only", "hermes dispatch post-enablement template");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_status"] === "template_only", "hermes dispatch post-enablement template status");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_executing_now"] === false, "hermes dispatch post-enablement template not executing");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_collects_data_now"] === false, "hermes dispatch post-enablement template no data collection");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_data_now"] === false, "hermes dispatch post-enablement template no data persistence");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_enables_feature_flags_now"] === false, "hermes dispatch post-enablement template no flags");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_changes_runtime_behavior_now"] === false, "hermes dispatch post-enablement template no runtime behavior");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_changes_gateway_behavior_now"] === false, "hermes dispatch post-enablement template no gateway behavior");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_adds_enablement_scripts"] === false, "hermes dispatch post-enablement template no scripts");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_changes_ci_behavior"] === false, "hermes dispatch post-enablement template no CI");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_readiness_verdict_required"] === "READY_WITH_CONSTRAINTS", "hermes dispatch post-enablement template required verdict");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_rollout_plan_status_required"] === "plan_only", "hermes dispatch post-enablement template required rollout plan");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_rollout_validation_checklist_status_required"] === "checklist_only", "hermes dispatch post-enablement template required checklist");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_operator_runbook_status_required"] === "runbook_only", "hermes dispatch post-enablement template required runbook");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch post-enablement template current verdict");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_current_rollout_plan_status"] === "plan_only", "hermes dispatch post-enablement template current rollout plan");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_current_rollout_validation_checklist_status"] === "checklist_only", "hermes dispatch post-enablement template current checklist");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_current_operator_runbook_status"] === "runbook_only", "hermes dispatch post-enablement template current runbook");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_default_disabled"] === true, "hermes dispatch post-enablement template default disabled");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_feature_flagged"] === true, "hermes dispatch post-enablement template feature flagged");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_requires_multiple_flags"] === true, "hermes dispatch post-enablement template multiple flags");
  const hTemplateFlags = hermes["gateway_real_dispatch_post_enablement_review_template_required_flags"] as string[];
  assert(Array.isArray(hTemplateFlags) && hTemplateFlags.length === 3, "hermes dispatch post-enablement template 3 flags");
  const hTemplateReview = hermes["gateway_real_dispatch_post_enablement_review_template_review_request_types"] as string[];
  assert(Array.isArray(hTemplateReview) && hTemplateReview.length === 1 && hTemplateReview[0] === "review", "hermes dispatch post-enablement template review request type");
  const hTemplateSupported = hermes["gateway_real_dispatch_post_enablement_review_template_supported_request_types"] as string[];
  assert(Array.isArray(hTemplateSupported) && hTemplateSupported.includes("review") && hTemplateSupported.includes("code_review") && hTemplateSupported.includes("validation"), "hermes dispatch post-enablement template supported");
  const hTemplateUnsupported = hermes["gateway_real_dispatch_post_enablement_review_template_unsupported_request_types"] as string[];
  assert(Array.isArray(hTemplateUnsupported) && hTemplateUnsupported.includes("llm_task") && hTemplateUnsupported.includes("code_generation") && hTemplateUnsupported.includes("bugfix"), "hermes dispatch post-enablement template unsupported");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_operator_approval_required"] === true, "hermes dispatch post-enablement template operator approval");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_automatic_enablement_allowed"] === false, "hermes dispatch post-enablement template no automatic enablement");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_rollout_may_proceed_automatically"] === false, "hermes dispatch post-enablement template no automatic rollout");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_changes_gateway_primary_dispatch"] === false, "hermes dispatch post-enablement template no primary dispatch");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_changes_gateway_final_result"] === false, "hermes dispatch post-enablement template no gateway final");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_changes_final_status"] === false, "hermes dispatch post-enablement template no final status");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_changes_routing"] === false, "hermes dispatch post-enablement template no routing");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_affects_primary_gateway_result"] === false, "hermes dispatch post-enablement template no primary effect");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_makes_hermes_default"] === false, "hermes dispatch post-enablement template not default");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_makes_hermes_final_review_owner"] === false, "hermes dispatch post-enablement template not review owner");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_makes_hermes_final_validation_owner"] === false, "hermes dispatch post-enablement template not validation owner");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_writes_files"] === false, "hermes dispatch post-enablement template no files");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_review_logs"] === false, "hermes dispatch post-enablement template no review logs");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_runbook_logs"] === false, "hermes dispatch post-enablement template no runbook logs");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_validation_logs"] === false, "hermes dispatch post-enablement template no validation logs");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_rollout_logs"] === false, "hermes dispatch post-enablement template no rollout logs");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_audit"] === false, "hermes dispatch post-enablement template no audit persist");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_observability"] === false, "hermes dispatch post-enablement template no observability persist");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_persists_guardrails"] === false, "hermes dispatch post-enablement template no guardrail persist");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_contains_raw_prompt"] === false, "hermes dispatch post-enablement template no raw prompt");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_contains_raw_artifacts"] === false, "hermes dispatch post-enablement template no raw artifacts");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_contains_secrets"] === false, "hermes dispatch post-enablement template no secrets");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_template_fields_are_placeholders_only"] === true, "hermes dispatch post-enablement template placeholders only");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_raw_prompt_collection_allowed"] === false, "hermes dispatch post-enablement template no raw prompt collection");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_raw_artifact_collection_allowed"] === false, "hermes dispatch post-enablement template no raw artifact collection");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_secret_collection_allowed"] === false, "hermes dispatch post-enablement template no secret collection");
  assert(hermes["gateway_real_dispatch_post_enablement_review_template_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Expansion Contract", "hermes dispatch post-enablement template next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-operator-runbook.ts"), "hermes evidence operator runbook module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-operator-runbook.test.ts"), "hermes evidence operator runbook test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_OPERATOR_RUNBOOK.md"), "hermes evidence operator runbook md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-operator-runbook.json"), "hermes evidence operator runbook json");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-post-enablement-review-template.ts"), "hermes evidence post-enablement template module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-post-enablement-review-template.test.ts"), "hermes evidence post-enablement template test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_POST_ENABLEMENT_REVIEW_TEMPLATE.md"), "hermes evidence post-enablement template md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-post-enablement-review-template.json"), "hermes evidence post-enablement template json");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-expansion-contract.ts"), "hermes evidence phase-2 expansion contract module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-expansion-contract.test.ts"), "hermes evidence phase-2 expansion contract test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_EXPANSION_CONTRACT.md"), "hermes evidence phase-2 expansion contract md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-expansion-contract.json"), "hermes evidence phase-2 expansion contract json");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist"] === "implemented_checklist_only", "hermes dispatch phase-2 validation checklist");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_status"] === "checklist_only", "hermes dispatch phase-2 validation checklist status");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_executing_now"] === false, "hermes dispatch phase-2 validation checklist not executing");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_validates_now"] === false, "hermes dispatch phase-2 validation checklist not validating");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_expands_request_types_now"] === false, "hermes dispatch phase-2 validation checklist no request type expansion");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_enables_feature_flags_now"] === false, "hermes dispatch phase-2 validation checklist no flags");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_readiness_verdict_required"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 validation checklist required verdict");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_phase_2_expansion_contract_status_required"] === "contract_only", "hermes dispatch phase-2 validation checklist required phase-2 contract");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_default_disabled"] === true, "hermes dispatch phase-2 validation checklist default disabled");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_feature_flagged"] === true, "hermes dispatch phase-2 validation checklist feature flagged");
  const hgVChecklistTargets = hermes["gateway_real_dispatch_phase_2_validation_checklist_phase_2_validation_targets"] as string[];
  assert(Array.isArray(hgVChecklistTargets) && hgVChecklistTargets.includes("code_review") && hgVChecklistTargets.includes("validation"), "hermes dispatch phase-2 validation checklist targets");
  const hgVChecklistSupported = hermes["gateway_real_dispatch_phase_2_validation_checklist_supported_request_types"] as string[];
  assert(Array.isArray(hgVChecklistSupported) && hgVChecklistSupported.includes("review") && hgVChecklistSupported.includes("code_review") && hgVChecklistSupported.includes("validation"), "hermes dispatch phase-2 validation checklist supported");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_operator_approval_required"] === true, "hermes dispatch phase-2 validation checklist operator approval");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_automatic_enablement_allowed"] === false, "hermes dispatch phase-2 validation checklist no automatic enablement");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_phase_2_may_proceed_automatically"] === false, "hermes dispatch phase-2 validation checklist no automatic phase-2");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_changes_gateway_primary_dispatch"] === false, "hermes dispatch phase-2 validation checklist no primary dispatch");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_makes_hermes_default"] === false, "hermes dispatch phase-2 validation checklist not default");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_makes_hermes_final_code_review_owner"] === false, "hermes dispatch phase-2 validation checklist not code review owner");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_makes_hermes_final_validation_owner"] === false, "hermes dispatch phase-2 validation checklist not validation owner");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_persists_phase_2_validation_logs"] === false, "hermes dispatch phase-2 validation checklist no phase-2 validation logs");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_contains_raw_prompt"] === false, "hermes dispatch phase-2 validation checklist no raw prompt");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_contains_secrets"] === false, "hermes dispatch phase-2 validation checklist no secrets");
  assert(hermes["gateway_real_dispatch_phase_2_validation_checklist_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Operator Runbook", "hermes dispatch phase-2 validation checklist next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-validation-checklist.ts"), "hermes evidence phase-2 validation checklist module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-validation-checklist.test.ts"), "hermes evidence phase-2 validation checklist test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_VALIDATION_CHECKLIST.md"), "hermes evidence phase-2 validation checklist md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-validation-checklist.json"), "hermes evidence phase-2 validation checklist json");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook"] === "implemented_runbook_only", "hermes dispatch phase-2 operator runbook");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_status"] === "runbook_only", "hermes dispatch phase-2 operator runbook status");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_executing_now"] === false, "hermes dispatch phase-2 operator runbook not executing");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_validates_now"] === false, "hermes dispatch phase-2 operator runbook not validating");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_expands_request_types_now"] === false, "hermes dispatch phase-2 operator runbook no request type expansion");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_enables_feature_flags_now"] === false, "hermes dispatch phase-2 operator runbook no flags");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_readiness_verdict_required"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 operator runbook required verdict");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_phase_2_expansion_contract_status_required"] === "contract_only", "hermes dispatch phase-2 operator runbook required phase-2 contract");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_phase_2_validation_checklist_status_required"] === "checklist_only", "hermes dispatch phase-2 operator runbook required phase-2 validation checklist");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 operator runbook current verdict");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_rollout_plan_status"] === "plan_only", "hermes dispatch phase-2 operator runbook current rollout plan");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_rollout_validation_checklist_status"] === "checklist_only", "hermes dispatch phase-2 operator runbook current checklist");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_operator_runbook_status"] === "runbook_only", "hermes dispatch phase-2 operator runbook current runbook");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_post_enablement_review_template_status"] === "template_only", "hermes dispatch phase-2 operator runbook current template");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_phase_2_expansion_contract_status"] === "contract_only", "hermes dispatch phase-2 operator runbook current phase-2 contract");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_phase_2_validation_checklist_status"] === "checklist_only", "hermes dispatch phase-2 operator runbook current phase-2 validation checklist");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_current_phase_2_operator_runbook_status"] === "runbook_only", "hermes dispatch phase-2 operator runbook current phase-2 runbook");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_default_disabled"] === true, "hermes dispatch phase-2 operator runbook default disabled");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_feature_flagged"] === true, "hermes dispatch phase-2 operator runbook feature flagged");
  const hgPhase2RunbookTargets = hermes["gateway_real_dispatch_phase_2_operator_runbook_phase_2_operator_targets"] as string[];
  assert(Array.isArray(hgPhase2RunbookTargets) && hgPhase2RunbookTargets.includes("code_review") && hgPhase2RunbookTargets.includes("validation"), "hermes dispatch phase-2 operator runbook targets");
  const hgPhase2RunbookSupported = hermes["gateway_real_dispatch_phase_2_operator_runbook_supported_request_types"] as string[];
  assert(Array.isArray(hgPhase2RunbookSupported) && hgPhase2RunbookSupported.includes("review") && hgPhase2RunbookSupported.includes("code_review") && hgPhase2RunbookSupported.includes("validation"), "hermes dispatch phase-2 operator runbook supported");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_operator_approval_required"] === true, "hermes dispatch phase-2 operator runbook operator approval");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_automatic_enablement_allowed"] === false, "hermes dispatch phase-2 operator runbook no automatic enablement");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_phase_2_may_proceed_automatically"] === false, "hermes dispatch phase-2 operator runbook no automatic phase-2");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_changes_gateway_primary_dispatch"] === false, "hermes dispatch phase-2 operator runbook no primary dispatch");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_makes_hermes_default"] === false, "hermes dispatch phase-2 operator runbook not default");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_makes_hermes_final_code_review_owner"] === false, "hermes dispatch phase-2 operator runbook not code review owner");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_makes_hermes_final_validation_owner"] === false, "hermes dispatch phase-2 operator runbook not validation owner");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_persists_phase_2_runbook_logs"] === false, "hermes dispatch phase-2 operator runbook no phase-2 runbook logs");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_contains_raw_prompt"] === false, "hermes dispatch phase-2 operator runbook no raw prompt");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_contains_secrets"] === false, "hermes dispatch phase-2 operator runbook no secrets");
  assert(hermes["gateway_real_dispatch_phase_2_operator_runbook_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Post-Validation Review Template", "hermes dispatch phase-2 operator runbook next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-operator-runbook.ts"), "hermes evidence phase-2 operator runbook module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-operator-runbook.test.ts"), "hermes evidence phase-2 operator runbook test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_OPERATOR_RUNBOOK.md"), "hermes evidence phase-2 operator runbook md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-operator-runbook.json"), "hermes evidence phase-2 operator runbook json");

  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template"] === "implemented_template_only", "hermes dispatch phase-2 post-validation review template");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_status"] === "template_only", "hermes dispatch phase-2 post-validation review template status");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_executing_now"] === false, "hermes dispatch phase-2 post-validation review not executing");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_collects_data_now"] === false, "hermes dispatch phase-2 post-validation review not collecting");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_expands_request_types_now"] === false, "hermes dispatch phase-2 post-validation review no type expand");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_readiness_verdict_required"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 post-validation review required verdict");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_phase_2_operator_runbook_status_required"] === "runbook_only", "hermes dispatch phase-2 post-validation review required phase-2 runbook");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_default_disabled"] === true, "hermes dispatch phase-2 post-validation review default disabled");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_feature_flagged"] === true, "hermes dispatch phase-2 post-validation review feature flagged");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_makes_hermes_default"] === false, "hermes dispatch phase-2 post-validation review not default");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_makes_hermes_final_code_review_owner"] === false, "hermes dispatch phase-2 post-validation review not code review owner");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_makes_hermes_final_validation_owner"] === false, "hermes dispatch phase-2 post-validation review not validation owner");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_contains_raw_prompt"] === false, "hermes dispatch phase-2 post-validation review no raw prompt");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_contains_secrets"] === false, "hermes dispatch phase-2 post-validation review no secrets");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_template_fields_are_placeholders_only"] === true, "hermes dispatch phase-2 post-validation review placeholders only");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_raw_prompt_collection_allowed"] === false, "hermes dispatch phase-2 post-validation review no prompt collection");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_secret_collection_allowed"] === false, "hermes dispatch phase-2 post-validation review no secret collection");
  assert(hermes["gateway_real_dispatch_phase_2_post_validation_review_template_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Final Readiness Review", "hermes dispatch phase-2 post-validation review next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-post-validation-review-template.ts"), "hermes evidence phase-2 post-validation review template module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-post-validation-review-template.test.ts"), "hermes evidence phase-2 post-validation review template test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_POST_VALIDATION_REVIEW_TEMPLATE.md"), "hermes evidence phase-2 post-validation review template md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-post-validation-review-template.json"), "hermes evidence phase-2 post-validation review template json");

  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review"] === "implemented_review_only", "hermes dispatch phase-2 final readiness review");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_status"] === "review_only", "hermes dispatch phase-2 final readiness review status");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 final readiness review verdict");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_executing_now"] === false, "hermes dispatch phase-2 final readiness review not executing");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_default_disabled"] === true, "hermes dispatch phase-2 final readiness review default disabled");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_feature_flagged"] === true, "hermes dispatch phase-2 final readiness review feature flagged");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_makes_hermes_default"] === false, "hermes dispatch phase-2 final readiness review not default");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_makes_hermes_final_code_review_owner"] === false, "hermes dispatch phase-2 final readiness review not cr owner");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_makes_hermes_final_validation_owner"] === false, "hermes dispatch phase-2 final readiness review not val owner");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_contains_raw_prompt"] === false, "hermes dispatch phase-2 final readiness review no raw prompt");
  assert(hermes["gateway_real_dispatch_phase_2_final_readiness_review_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Controlled Enablement Plan", "hermes dispatch phase-2 final readiness review next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-final-readiness-review.ts"), "hermes evidence phase-2 final readiness review module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-final-readiness-review.test.ts"), "hermes evidence phase-2 final readiness review test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_FINAL_READINESS_REVIEW.md"), "hermes evidence phase-2 final readiness review md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-final-readiness-review.json"), "hermes evidence phase-2 final readiness review json");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan"] === "implemented_plan_only", "hermes dispatch phase-2 controlled enablement plan");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_status"] === "plan_only", "hermes dispatch phase-2 controlled enablement plan status");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_executing_now"] === false, "hermes dispatch phase-2 controlled enablement plan not executing");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_enables_feature_flags_now"] === false, "hermes dispatch phase-2 controlled enablement plan no flags now");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_expands_request_types_now"] === false, "hermes dispatch phase-2 controlled enablement plan no expansion now");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_validates_now"] === false, "hermes dispatch phase-2 controlled enablement plan not validating");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_changes_runtime_behavior_now"] === false, "hermes dispatch phase-2 controlled enablement plan no runtime behavior");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_changes_gateway_behavior_now"] === false, "hermes dispatch phase-2 controlled enablement plan no gateway behavior");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_adds_enablement_scripts"] === false, "hermes dispatch phase-2 controlled enablement plan no scripts");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_changes_ci_behavior"] === false, "hermes dispatch phase-2 controlled enablement plan no CI");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 controlled enablement plan verdict");
  const hPhase2PlanCurrent = hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_current_validated_request_types"] as string[];
  assert(Array.isArray(hPhase2PlanCurrent) && hPhase2PlanCurrent.length === 1 && hPhase2PlanCurrent[0] === "review", "hermes dispatch phase-2 controlled enablement plan current validated review");
  const hPhase2PlanTargets = hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_phase_2_enablement_targets"] as string[];
  assert(Array.isArray(hPhase2PlanTargets) && hPhase2PlanTargets.includes("code_review") && hPhase2PlanTargets.includes("validation"), "hermes dispatch phase-2 controlled enablement plan targets");
  const hPhase2PlanSupported = hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_supported_request_types"] as string[];
  assert(Array.isArray(hPhase2PlanSupported) && hPhase2PlanSupported.includes("review") && hPhase2PlanSupported.includes("code_review") && hPhase2PlanSupported.includes("validation"), "hermes dispatch phase-2 controlled enablement plan supported");
  const hPhase2PlanUnsupported = hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_unsupported_request_types"] as string[];
  assert(Array.isArray(hPhase2PlanUnsupported) && hPhase2PlanUnsupported.includes("llm_task") && hPhase2PlanUnsupported.includes("code_generation") && hPhase2PlanUnsupported.includes("bugfix"), "hermes dispatch phase-2 controlled enablement plan unsupported");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_default_disabled"] === true, "hermes dispatch phase-2 controlled enablement plan default disabled");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_feature_flagged"] === true, "hermes dispatch phase-2 controlled enablement plan feature flagged");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_requires_multiple_flags"] === true, "hermes dispatch phase-2 controlled enablement plan multiple flags");
  const hPhase2PlanFlags = hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_required_flags"] as string[];
  assert(Array.isArray(hPhase2PlanFlags) && hPhase2PlanFlags.length === 3, "hermes dispatch phase-2 controlled enablement plan 3 flags");
  assert(hPhase2PlanFlags.includes("SDLC_HERMES_GATEWAY_REAL_DISPATCH=enabled") && hPhase2PlanFlags.includes("SDLC_HERMES_GATEWAY_INTEGRATION=enabled") && hPhase2PlanFlags.includes("SDLC_HERMES_CLI_COMMAND_EXECUTION=enabled"), "hermes dispatch phase-2 controlled enablement plan flags correct");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_operator_approval_required"] === true, "hermes dispatch phase-2 controlled enablement plan operator approval");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_automatic_enablement_allowed"] === false, "hermes dispatch phase-2 controlled enablement plan no automatic enablement");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_rollout_may_proceed_automatically"] === false, "hermes dispatch phase-2 controlled enablement plan no automatic rollout");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_phase_2_may_proceed_automatically"] === false, "hermes dispatch phase-2 controlled enablement plan no automatic phase-2");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_changes_gateway_primary_dispatch"] === false, "hermes dispatch phase-2 controlled enablement plan no primary dispatch");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_changes_gateway_final_result"] === false, "hermes dispatch phase-2 controlled enablement plan no gateway final");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_changes_final_status"] === false, "hermes dispatch phase-2 controlled enablement plan no final status");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_changes_routing"] === false, "hermes dispatch phase-2 controlled enablement plan no routing");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_affects_primary_gateway_result"] === false, "hermes dispatch phase-2 controlled enablement plan no primary effect");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_makes_hermes_default"] === false, "hermes dispatch phase-2 controlled enablement plan not default");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_makes_hermes_final_review_owner"] === false, "hermes dispatch phase-2 controlled enablement plan not review owner");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_makes_hermes_final_code_review_owner"] === false, "hermes dispatch phase-2 controlled enablement plan not code review owner");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_makes_hermes_final_validation_owner"] === false, "hermes dispatch phase-2 controlled enablement plan not validation owner");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_writes_files"] === false, "hermes dispatch phase-2 controlled enablement plan no files");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_persists_enablement_logs"] === false, "hermes dispatch phase-2 controlled enablement plan no enablement logs");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_persists_validation_logs"] === false, "hermes dispatch phase-2 controlled enablement plan no validation logs");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_persists_review_logs"] === false, "hermes dispatch phase-2 controlled enablement plan no review logs");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_persists_audit"] === false, "hermes dispatch phase-2 controlled enablement plan no audit persist");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_persists_observability"] === false, "hermes dispatch phase-2 controlled enablement plan no observability persist");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_persists_guardrails"] === false, "hermes dispatch phase-2 controlled enablement plan no guardrail persist");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_contains_raw_prompt"] === false, "hermes dispatch phase-2 controlled enablement plan no raw prompt");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_contains_raw_artifacts"] === false, "hermes dispatch phase-2 controlled enablement plan no raw artifacts");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_contains_secrets"] === false, "hermes dispatch phase-2 controlled enablement plan no secrets");
  assert(hermes["gateway_real_dispatch_phase_2_controlled_enablement_plan_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Enablement Guard Contract", "hermes dispatch phase-2 controlled enablement plan next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-controlled-enablement-plan.ts"), "hermes evidence phase-2 controlled enablement plan module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-controlled-enablement-plan.test.ts"), "hermes evidence phase-2 controlled enablement plan test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_CONTROLLED_ENABLEMENT_PLAN.md"), "hermes evidence phase-2 controlled enablement plan md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-controlled-enablement-plan.json"), "hermes evidence phase-2 controlled enablement plan json");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract"] === "implemented_contract_only", "hermes dispatch phase-2 enablement guard contract");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_status"] === "contract_only", "hermes dispatch phase-2 enablement guard contract status");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_contract_only"] === true, "hermes dispatch phase-2 enablement guard contract contract only");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_executing_now"] === false, "hermes dispatch phase-2 enablement guard contract not executing");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_enables_feature_flags_now"] === false, "hermes dispatch phase-2 enablement guard contract no flags now");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_expands_request_types_now"] === false, "hermes dispatch phase-2 enablement guard contract no expansion now");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_validates_now"] === false, "hermes dispatch phase-2 enablement guard contract not validating");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_changes_runtime_behavior_now"] === false, "hermes dispatch phase-2 enablement guard contract no runtime behavior");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_changes_gateway_behavior_now"] === false, "hermes dispatch phase-2 enablement guard contract no gateway behavior");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_adds_enablement_scripts"] === false, "hermes dispatch phase-2 enablement guard contract no scripts");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_changes_ci_behavior"] === false, "hermes dispatch phase-2 enablement guard contract no CI");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 enablement guard contract verdict");
  const hGuardCurrent = hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_current_validated_request_types"] as string[];
  assert(Array.isArray(hGuardCurrent) && hGuardCurrent.length === 1 && hGuardCurrent[0] === "review", "hermes dispatch phase-2 enablement guard contract current validated review");
  const hGuardTargets = hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_phase_2_guard_targets"] as string[];
  assert(Array.isArray(hGuardTargets) && hGuardTargets.includes("code_review") && hGuardTargets.includes("validation"), "hermes dispatch phase-2 enablement guard contract targets");
  const hGuardSupported = hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_supported_request_types"] as string[];
  assert(Array.isArray(hGuardSupported) && hGuardSupported.includes("review") && hGuardSupported.includes("code_review") && hGuardSupported.includes("validation"), "hermes dispatch phase-2 enablement guard contract supported");
  const hGuardUnsupported = hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_unsupported_request_types"] as string[];
  assert(Array.isArray(hGuardUnsupported) && hGuardUnsupported.includes("llm_task") && hGuardUnsupported.includes("code_generation") && hGuardUnsupported.includes("bugfix"), "hermes dispatch phase-2 enablement guard contract unsupported");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_default_disabled"] === true, "hermes dispatch phase-2 enablement guard contract default disabled");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_feature_flagged"] === true, "hermes dispatch phase-2 enablement guard contract feature flagged");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_requires_multiple_flags"] === true, "hermes dispatch phase-2 enablement guard contract multiple flags");
  const hGuardFlags = hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_required_flags"] as string[];
  assert(Array.isArray(hGuardFlags) && hGuardFlags.length === 3, "hermes dispatch phase-2 enablement guard contract 3 flags");
  assert(hGuardFlags.includes("SDLC_HERMES_GATEWAY_REAL_DISPATCH=enabled") && hGuardFlags.includes("SDLC_HERMES_GATEWAY_INTEGRATION=enabled") && hGuardFlags.includes("SDLC_HERMES_CLI_COMMAND_EXECUTION=enabled"), "hermes dispatch phase-2 enablement guard contract flags correct");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_operator_approval_required"] === true, "hermes dispatch phase-2 enablement guard contract operator approval");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_automatic_enablement_allowed"] === false, "hermes dispatch phase-2 enablement guard contract no automatic enablement");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_rollout_may_proceed_automatically"] === false, "hermes dispatch phase-2 enablement guard contract no automatic rollout");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_phase_2_may_proceed_automatically"] === false, "hermes dispatch phase-2 enablement guard contract no automatic phase-2");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_changes_gateway_primary_dispatch"] === false, "hermes dispatch phase-2 enablement guard contract no primary dispatch");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_changes_gateway_final_result"] === false, "hermes dispatch phase-2 enablement guard contract no gateway final");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_changes_final_status"] === false, "hermes dispatch phase-2 enablement guard contract no final status");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_changes_routing"] === false, "hermes dispatch phase-2 enablement guard contract no routing");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_affects_primary_gateway_result"] === false, "hermes dispatch phase-2 enablement guard contract no primary effect");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_makes_hermes_default"] === false, "hermes dispatch phase-2 enablement guard contract not default");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_makes_hermes_final_review_owner"] === false, "hermes dispatch phase-2 enablement guard contract not review owner");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_makes_hermes_final_code_review_owner"] === false, "hermes dispatch phase-2 enablement guard contract not code review owner");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_makes_hermes_final_validation_owner"] === false, "hermes dispatch phase-2 enablement guard contract not validation owner");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_writes_files"] === false, "hermes dispatch phase-2 enablement guard contract no files");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_persists_enablement_logs"] === false, "hermes dispatch phase-2 enablement guard contract no enablement logs");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_persists_validation_logs"] === false, "hermes dispatch phase-2 enablement guard contract no validation logs");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_persists_review_logs"] === false, "hermes dispatch phase-2 enablement guard contract no review logs");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_persists_audit"] === false, "hermes dispatch phase-2 enablement guard contract no audit persist");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_persists_observability"] === false, "hermes dispatch phase-2 enablement guard contract no observability persist");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_persists_guardrails"] === false, "hermes dispatch phase-2 enablement guard contract no guardrail persist");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_contains_raw_prompt"] === false, "hermes dispatch phase-2 enablement guard contract no raw prompt");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_contains_raw_artifacts"] === false, "hermes dispatch phase-2 enablement guard contract no raw artifacts");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_contains_secrets"] === false, "hermes dispatch phase-2 enablement guard contract no secrets");
  assert(hermes["gateway_real_dispatch_phase_2_enablement_guard_contract_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Shadow Enablement Implementation Plan", "hermes dispatch phase-2 enablement guard contract next PR");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-enablement-guard-contract.ts"), "hermes evidence phase-2 enablement guard contract module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-enablement-guard-contract.test.ts"), "hermes evidence phase-2 enablement guard contract test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_ENABLEMENT_GUARD_CONTRACT.md"), "hermes evidence phase-2 enablement guard contract md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-enablement-guard-contract.json"), "hermes evidence phase-2 enablement guard contract json");
  assert(hermesEvidence.includes("execution/hermes-gateway-real-dispatch-phase-2-shadow-enablement-implementation-plan.ts"), "hermes evidence phase-2 shadow enablement implementation plan module");
  assert(hermesEvidence.includes("tests/hermes-gateway-real-dispatch-phase-2-shadow-enablement-implementation-plan.test.ts"), "hermes evidence phase-2 shadow enablement implementation plan test");
  assert(hermesEvidence.includes("HERMES_GATEWAY_REAL_DISPATCH_PHASE_2_SHADOW_ENABLEMENT_IMPLEMENTATION_PLAN.md"), "hermes evidence phase-2 shadow enablement implementation plan md");
  assert(hermesEvidence.includes("hermes-gateway-real-dispatch-phase-2-shadow-enablement-implementation-plan.json"), "hermes evidence phase-2 shadow enablement implementation plan json");
  console.log("");

  // ── Hermes Gateway Real Dispatch Phase-2 Shadow Enablement Implementation Plan ──
  console.log("Hermes Gateway Real Dispatch Phase-2 Shadow Enablement Implementation Plan");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan"] === "implemented_plan_only", "hermes dispatch phase-2 shadow enablement implementation plan");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_status"] === "plan_only", "hermes dispatch phase-2 shadow enablement implementation plan status");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_executing_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan not executing");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_implements_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan not implementing");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_enables_feature_flags_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no flags now");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_expands_request_types_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no expansion now");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_validates_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan not validating");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_runtime_behavior_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no runtime behavior");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_gateway_behavior_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no gateway behavior");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_hermes_dispatch_eligibility_now"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no dispatch eligibility change");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_adds_enablement_scripts"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no scripts");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_ci_behavior"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no CI");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_current_readiness_verdict"] === "READY_WITH_CONSTRAINTS", "hermes dispatch phase-2 shadow enablement implementation plan verdict");
  const hShadowCurrent = hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_current_validated_request_types"] as string[];
  assert(Array.isArray(hShadowCurrent) && hShadowCurrent.length === 1 && hShadowCurrent[0] === "review", "hermes dispatch phase-2 shadow enablement implementation plan current validated review");
  const hShadowTargets = hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_phase_2_shadow_targets"] as string[];
  assert(Array.isArray(hShadowTargets) && hShadowTargets.includes("code_review") && hShadowTargets.includes("validation"), "hermes dispatch phase-2 shadow enablement implementation plan targets");
  const hShadowSupported = hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_supported_request_types"] as string[];
  assert(Array.isArray(hShadowSupported) && hShadowSupported.includes("review") && hShadowSupported.includes("code_review") && hShadowSupported.includes("validation"), "hermes dispatch phase-2 shadow enablement implementation plan supported");
  const hShadowUnsupported = hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_unsupported_request_types"] as string[];
  assert(Array.isArray(hShadowUnsupported) && hShadowUnsupported.includes("llm_task") && hShadowUnsupported.includes("code_generation") && hShadowUnsupported.includes("bugfix"), "hermes dispatch phase-2 shadow enablement implementation plan unsupported");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_default_disabled"] === true, "hermes dispatch phase-2 shadow enablement implementation plan default disabled");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_feature_flagged"] === true, "hermes dispatch phase-2 shadow enablement implementation plan feature flagged");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_requires_multiple_flags"] === true, "hermes dispatch phase-2 shadow enablement implementation plan multiple flags");
  const hShadowFlags = hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_required_flags"] as string[];
  assert(Array.isArray(hShadowFlags) && hShadowFlags.length === 3, "hermes dispatch phase-2 shadow enablement implementation plan 3 flags");
  assert(hShadowFlags.includes("SDLC_HERMES_GATEWAY_REAL_DISPATCH=enabled") && hShadowFlags.includes("SDLC_HERMES_GATEWAY_INTEGRATION=enabled") && hShadowFlags.includes("SDLC_HERMES_CLI_COMMAND_EXECUTION=enabled"), "hermes dispatch phase-2 shadow enablement implementation plan flags correct");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_operator_approval_required"] === true, "hermes dispatch phase-2 shadow enablement implementation plan operator approval");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_automatic_enablement_allowed"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no automatic enablement");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_rollout_may_proceed_automatically"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no automatic rollout");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_phase_2_may_proceed_automatically"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no automatic phase-2");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_gateway_primary_dispatch"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no primary dispatch");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_gateway_final_result"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no gateway final");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_final_status"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no final status");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_changes_routing"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no routing");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_affects_primary_gateway_result"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no primary effect");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_makes_hermes_default"] === false, "hermes dispatch phase-2 shadow enablement implementation plan not default");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_makes_hermes_final_review_owner"] === false, "hermes dispatch phase-2 shadow enablement implementation plan not review owner");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_makes_hermes_final_code_review_owner"] === false, "hermes dispatch phase-2 shadow enablement implementation plan not code review owner");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_makes_hermes_final_validation_owner"] === false, "hermes dispatch phase-2 shadow enablement implementation plan not validation owner");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_writes_files"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no files");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_persists_enablement_logs"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no enablement logs");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_persists_validation_logs"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no validation logs");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_persists_review_logs"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no review logs");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_persists_audit"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no audit persist");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_persists_observability"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no observability persist");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_persists_guardrails"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no guardrail persist");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_contains_raw_prompt"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no raw prompt");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_contains_raw_artifacts"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no raw artifacts");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_contains_secrets"] === false, "hermes dispatch phase-2 shadow enablement implementation plan no secrets");
  assert(hermes["gateway_real_dispatch_phase_2_shadow_enablement_implementation_plan_recommended_next_pr"] === "Hermes Gateway Real Dispatch Phase-2 Shadow Enablement Contract", "hermes dispatch phase-2 shadow enablement implementation plan next PR");
  console.log("");

  // ── Test 8: Recommended next PR ──
  console.log("Test 8: Recommended next PR");
  assert(m.recommended_next_pr.title === "Hermes Gateway Real Dispatch Phase-2 Shadow Enablement Contract", "next PR phase-2 shadow enablement contract");
  console.log("");

  console.log(`\nResults: ${passed} passed, ${failed} failed`);
  process.exit(failed > 0 ? 1 : 0);
}

test();
